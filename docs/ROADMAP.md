# MotoDiag — Full Roadmap

**Project:** moto-diag — AI-Powered Motorcycle Diagnostic Tool (Hybrid: Software + Hardware)
**Repo:** `Kubanjaze/moto-diag`
**Local:** `C:\Users\Kerwyn\PycharmProjects\moto-diag\`
**Started:** 2026-04-15
**Target Fleet:** Harley-Davidson (all years), Honda, Yamaha, Kawasaki, Suzuki, BMW, Ducati, KTM, Triumph, Aprilia, MV Agusta, Electric (Zero/LiveWire/Energica/Damon), Scooters & small-displacement (all classes — sport, standard, cruiser, dual-sport, vintage, adventure, electric, scooter)
**Target Users:** Motorcycle mechanics, shops (solo → multi-location)
**Total Phases:** 352

---

## Track A — Core Infrastructure (Phases 01–12)

Foundation: monorepo scaffold, config, database, base data models.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 01 | Project scaffold + monorepo setup | ✅ | pyproject.toml, package structure, CLI entry point |
| 02 | Configuration system | ✅ | Profiles, validators, ensure_directories, config CLI |
| 03 | Database schema + SQLite setup | ✅ | 6 tables, WAL mode, connection manager, schema versioning |
| 04 | Vehicle registry data model | ✅ | CRUD operations (add/get/list/update/delete/count) |
| 05 | DTC (Diagnostic Trouble Code) schema + loader | ✅ | DTC repo, JSON loader, 40 codes, code CLI functional |
| 06 | Symptom taxonomy + data model | ✅ | 40 symptoms, 12+ categories, related_systems linking |
| 07 | Diagnostic session model | ✅ | 9 functions, full lifecycle, accumulation, 16 tests |
| 08 | Knowledge base schema | ✅ | Issues repo, 10 Harley known issues with forum tips, loader |
| 09 | Search + query engine | ✅ | Unified search across 5 stores, search CLI command |
| 10 | Logging + audit trail | ✅ | Structured logging, session audit trail, file handler |
| 11 | Test framework + fixtures | ✅ | Shared conftest.py, populated_db, 136/136 regression pass |
| 12 | Gate 1 — Core infrastructure integration test | ✅ | Full workflow E2E, db init CLI, 140/140 pass, **GATE PASSED** |

## Track B — Vehicle Knowledge Base (Phases 13–62)

Deep domain knowledge for target fleet. Each phase populates the knowledge base with real diagnostic data — common failures, DTCs, symptoms, repair procedures.

### Harley-Davidson (Phases 13–22)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 13 | Harley Evo Big Twin (1984–1999) | ✅ | 10 issues: base gasket, starter clutch, CV carb, ignition, wet sumping |
| 14 | Harley Twin Cam 88/88B (1999–2006) | ✅ | 10 issues: tick of death, compensator, sumping, cam bearing |
| 15 | Harley Twin Cam 96/103/110 (2007–2017) | ✅ | 10 issues: ABS, TBW, ECM tuning, fuel pump, wheel bearing |
| 16 | Harley Milwaukee-Eight (2017+) | ✅ | 10 issues: oil cooler, infotainment, intake carbon, TBW |
| 17 | Harley Sportster Evo (1986–2003) | ✅ | 10 issues: shared oil, clutch cable, carb, starter, kickstand |
| 18 | Harley Sportster Rubber-Mount (2004–2021) | ✅ | 10 issues: fuel pump, stator connector, fork seals, brake caliper |
| 19 | Harley V-Rod / VRSC (2002–2017) | ✅ | 10 issues: coolant, hydraulic clutch, fuel cell, alternator, frame, ECU, belt tensioner, starter |
| 20 | Harley Revolution Max (2021+) | ✅ | 10 issues: TFT freeze, ride-by-wire, water pump, chain drive, battery drain, ABS/TC |
| 21 | Harley electrical systems (all eras) | ✅ | 10 issues: regulator, stator, solenoid, grounds, CAN bus, ignition switch, TSSM, wiring, battery types, lighting |
| 22 | Harley common cross-era issues | ✅ | 10 issues: compensator, intake seals, heat soak, primary leak, clutch, shocks, tire wear, ethanol |

### Honda (Phases 23–34)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 23 | Honda CBR supersport: 900RR/929RR/954RR (1992–2003) | ✅ | 10 issues: reg/rec, CCT, HISS, carb sync, fork seals, starter clutch, PGM-FI |
| 24 | Honda CBR600: F2/F3/F4/F4i (1991–2006) | ✅ | 10 issues: reg/rec, carbs, CCT, F4i injectors, clutch cable, radiator fan, chain |
| 25 | Honda CBR600RR (2003–2024) | ✅ | 10 issues: HESD, reg/rec, C-ABS, CCT, fuel pump, valve clearance, stator, subframe |
| 26 | Honda CBR1000RR / RR-R (2004+) | ✅ | 10 issues: reg/rec, HSTC, CCT, HESD, quickshifter, exhaust servo, brakes, winglets |
| 27 | Honda cruisers: Shadow 600/750/1100, VTX 1300/1800 | ✅ | 10 issues: reg/rec, shaft drive, carbs, VTX starter, fuel pump, clutch drag, exhaust |
| 28 | Honda Rebel 250/300/500/1100 | ✅ | 10 issues: carb 250, reg/rec, DCT jerky, chain 300/500, battery drain, drum brake |
| 29 | Honda standards: CB750/919, CB1000R, Hornet | ✅ | 10 issues: reg/rec, CCT, ride-by-wire, carbs, chain, vibes, tank rust, rear shock |
| 30 | Honda V4: VFR800, RC51/RVT1000R, VFR1200F | ✅ | 10 issues: VTEC, reg/rec, RC51 TPS, gear cam noise, CBS, overheating, fuel pump relay |
| 31 | Honda dual-sport: XR650L, CRF250L/300L, Africa Twin | ✅ | 10 issues: XR650L jetting/valves, CRF power, AT DCT off-road, radiator, water, chain |
| 32 | Honda vintage air-cooled: CB550/650/750, Nighthawk | ✅ | 10 issues: points ignition, carb rebuild, charging, cam chain, petcock, brakes, wiring |
| 33 | Honda electrical systems + PGM-FI | ✅ | 10 issues: blink codes, HISS, reg/rec, stator, starter relay, FI sensors, grounds, fuses |
| 34 | Honda common cross-model issues | ✅ | 10 issues: CCT, starter clutch, coolant hoses, valves, chain, brake fluid, throttle, forks, tires |

### Yamaha (Phases 35–44)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 35 | Yamaha YZF-R1 (1998+) | ✅ | 10 issues: EXUP, stator, crossplane sound, YCC-T, YCC-I, fuel pump, CCT, R1M electronics |
| 36 | Yamaha YZF-R6 (1999–2020) | ✅ | 10 issues: CCT (critical), stator, valve clearance, EXUP, underseat heat, fuel pump, throttle sync, coolant, immobilizer, electronics |
| 37 | Yamaha YZF-R7 + YZF600R Thundercat (1996–2007, 2021+) | ✅ | 10 issues: Thundercat carbs/petcock/charging/chain/forks + R7 clutch/suspension/QS/heat/lean spot |
| 38 | Yamaha FZ/MT naked: FZ6/FZ8/FZ-09/FZ-10, MT-03/07/09/10 | ✅ | 10 issues: MT-09 throttle/valves/fuel, MT-07 charging/chain, FZ6 heat, MT-10 electronics, MT-03 drops, radiator |
| 39 | Yamaha cruisers: V-Star 250/650/950/1100/1300, Bolt | ✅ | 10 issues: V-Star carbs/petcock/stator/shaft drive/starter clutch/ISC + Bolt heat/belt + AIS |
| 40 | Yamaha VMAX (1985–2020) | ✅ | 10 issues: Gen 1 V-Boost/carbs/charging/shaft drive/tank + Gen 2 FI/cooling/brakes/electronics/exhaust |
| 41 | Yamaha dual-sport: WR250R/X, XT250, Ténéré 700 | ✅ | 10 issues: WR250R stator/suspension/cold start + XT250 carb/chain/valves + Tenere wind/crash/lean/shock |
| 42 | Yamaha vintage: XS650, RD350/400, SR400/500 | ✅ | 10 issues: XS650 points/charging/oil/cam chain + RD Autolube/chambers/reeds + SR kickstart/carb/valves |
| 43 | Yamaha electrical systems + diagnostics | ✅ | 10 issues: self-diagnostic/stator connector/MOSFET/grounds/harness/fuses/immobilizer/LED/batteries/Woolich |
| 44 | Yamaha common cross-model issues | ✅ | 10 issues: CCT/ethanol/valves/EXUP/brakes/chain/coolant/tires/winterization/exhaust mods |

### Kawasaki (Phases 45–56)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 45 | Kawasaki Ninja 250/300/400 (1988+) | ✅ | 10 issues: carbs/drops/charging/fuel pump/chain/valves/ABS/coolant/header crack/oil neglect |
| 46 | Kawasaki ZX-6R (1995+) | ✅ | 10 issues: 636/599 confusion, stator, CCT, KLEEN, valves, fuel pump, cooling, KTRC, forks, QS |
| 47 | Kawasaki ZX-7R / ZX-7RR (1996–2003) | ✅ | 10 issues: carbs/fuel system/charging/FCR carbs/CCT/wiring/rubber/suspension/brakes/ignition |
| 48 | Kawasaki ZX-9R (1998–2003) | ✅ | 10 issues: carbs/early FI/charging/CCT/cooling/fuel pump/forks/brakes/valves/ignition switch |
| 49 | Kawasaki ZX-10R (2004+) | ✅ | 10 issues: headshake/charging/IMU electronics/CCT/fuel pump/valves/KLEEN/crash/QS/cooling |
| 50 | Kawasaki ZX-12R / ZX-14R (2000–2020) | ✅ | 10 issues: ram air/charging/consumables/FI tuning/ABS-KTRC/chain/CCT/cooling/suspension/valves |
| 51 | Kawasaki Ninja H2 / H2R (2015+) | ✅ | 10 issues: supercharger/cooling/electronics/paint/chain/oil/fuel/H2 SX/brakes/valves |
| 52 | Kawasaki Z naked: Z400/Z650/Z750/Z800/Z900/Z1000, Z H2 | ✅ | 10 issues: radiator/throttle/connectors/charging/Z H2 heat/chain/KTRC/CCT/beginner/headlights |
| 53 | Kawasaki Vulcan cruisers: 500/750/800/900/1500/1600/1700/2000 | ✅ | 10 issues: carbs/ISC surge/shaft drive/starter/Vulcan 2000/belt/heat/1700 EFI/Vulcan 500/exhaust mods |
| 54 | Kawasaki dual-sport: KLR650, KLX250/300, Versys 650/1000 | ✅ | 10 issues: doohickey/overheating/FI lean surge/valves/suspension/TBW/subframe/jetting/chain/stator |
| 55 | Kawasaki vintage: KZ550/650/750/1000/1100, GPz series | ✅ | 10 issues: CCT/charging/carb rebuild/fuel system/points ignition/forks/oil leaks/brakes/wiring/chain |
| 56 | Kawasaki electrical systems + FI dealer mode | ✅ | 10 issues: dealer mode/stator/KLEEN/KIPASS/LED/grounds/starter/parasitic draw/KTRC-KIBS/connectors |

### Suzuki (Phases 57–67)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 57 | Suzuki GSX-R600 (1997+) | ✅ | 10 issues: stator/CCT/PAIR/fuel pump/forks/valves/cooling/S-DMS-TC/brakes/clutch |
| 58 | Suzuki GSX-R750 (1996+) | ✅ | 10 issues: 2nd gear/stator/CCT/fuel pump/SRAD coolant/linkage/TPS/chain/master cyl/headstock |
| 59 | Suzuki GSX-R1000 (2001+) | ✅ | 10 issues: K5-K8 case cracking/stator/IMU-TCS/CCT/SET servo/fuel relay/valves/QS/cooling/bearings |
| 60 | Suzuki GSX-R1100 (1986–1998) | ✅ | 10 issues: air-oil overheating/carbs/charging/CCT/suspension/CDI ignition/petcock/brakes/wiring/chain |
| 61 | Suzuki SV650/1000 + Gladius (1999+) | ✅ | 10 issues: reg-rec/carb tuning/CCT/clutch/fuel pump/suspension/SV1000 STPS/Gladius/forks/exhaust |
| 62 | Suzuki V-Strom 650/1000/1050 (2002+) | ✅ | 10 issues: lean surge/CCT/windscreen/suspension/1050 electronics/charging/chain/valves/side stand/ABS |
| 63 | Suzuki Bandit 600/1200/1250 (1995–2012) | ✅ | 10 issues: overheating/carbs/FI/charging/CCT/suspension/petcock/brakes/chain/wiring |
| 64 | Suzuki GSX-S750/1000 + Katana (2015+) | ✅ | 10 issues: TBW/CCT/Katana heat/charging/fuel relay/chain/mirrors/valves/TC-ABS/forks |
| 65 | Suzuki cruisers: Intruder/Boulevard series | ✅ | 10 issues: carbs/FI surge/shaft drive/M109R clutch/starter clutch/charging/heat/fuel pump/exhaust/brakes |
| 66 | Suzuki dual-sport: DR-Z400S/SM, DR650SE | ✅ | 10 issues: jetting/valves/SM brakes/DR650 carb-oil/chain/electrical/suspension/kick start/DR650 starter |
| 67 | Suzuki vintage: GS550/750/850/1000/1100, Katana 1100 | ✅ | 10 issues: CCT/charging/carbs/ignition/fuel system/suspension/brakes/oil leaks/wiring/chain |

### Cross-Platform Systems (Phases 68–78)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 68 | Suzuki electrical systems + FI dealer mode | ✅ | 10 issues: C-mode/stator/PAIR/grounds/starter/parasitic draw/TC-ABS/LED/connectors/FI relay |
| 69 | Suzuki common cross-model issues | ✅ | 10 issues: stator connector fire risk/CCT/fuel relay/PAIR/coolant/valves/chain/brakes/forks/exhaust |
| 70 | Carburetor troubleshooting (cross-platform) | ✅ | 10 issues: CV diaphragm/pilot jet/float height/sync/enrichment/vacuum leaks/pilot screw/overflow/rejetting |
| 71 | Fuel injection troubleshooting (cross-platform) | ✅ | 10 issues: fuel pump/TPS/MAP/O2/ECT/injectors/ISC/throttle body/fuel pressure/ECU reset |
| 72 | Charging system diagnostics (cross-platform) | ✅ | 10 issues: stator/reg-rec/connector/rotor/batteries/parasitic draw/3-step test/grounds/accessories |
| 73 | Starting system diagnostics (cross-platform) | ✅ | 10 issues: relay/motor/sprag/clutch switch/kickstand/kill switch/cables/tip-over/neutral/compression |
| 74 | Ignition system diagnostics (cross-platform) | ✅ | 10 issues: plugs/coils/CDI/pickup/wires/points/timing/kill switch/misfires/COP |
| 75 | Cooling system diagnostics (cross-platform) | ✅ | 10 issues: thermostat/fan/coolant/water pump/radiator/air-cooled/hoses/head gasket/cap/track coolant |
| 76 | Brake system diagnostics (cross-platform) | ✅ | 10 issues: fluid/calipers/pads/rotors/master cyl/stainless lines/ABS sensors/ABS bleeding/drum/bolt torque |
| 77 | Drivetrain diagnostics (cross-platform) | ✅ | 10 issues: chain/sprockets/lube/belt/shaft/clutch/cable-hydraulic/transmission/countershaft seal/alignment |
| 78 | Gate 2 — Vehicle knowledge base integration test | ✅ | 21 integration tests, 650+ issues, all 5 makes verified, **GATE 2 PASSED** |

## Track C — AI Diagnostic Engine (Phases 79–95)

Claude API integration, prompt engineering, diagnostic reasoning.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 79 | Claude API integration + base client | ✅ | DiagnosticClient, model selection, token tracking, 32 tests |
| 80 | Symptom analysis prompt engineering | ✅ | SymptomAnalyzer, categorization, urgency, two-pass approach, 28 tests |
| 81 | Fault code interpretation prompts | ✅ | FaultCodeInterpreter, 8 DTC formats, 51 local codes, 36 tests |
| 82 | Multi-step diagnostic workflows | ✅ | DiagnosticWorkflow, 3 templates (no-start/charging/overheating), 26 tests |
| 83 | Confidence scoring | ✅ | ConfidenceScore, 8 evidence types, normalization, ranking, 23 tests |
| 84 | Repair procedure generator | ✅ | RepairProcedureGenerator, SkillLevel assessment, 41 tests |
| 85 | Parts + tools recommendation | ✅ | PartsRecommender, cross-refs, 13+ brands, 35 tests |
| 86 | Cost estimation | ✅ | CostEstimator, ShopType comparison, format_estimate, 34 tests |
| 87 | Safety warnings + critical alerts | ✅ | SafetyChecker, 18 rules, 12 repair keywords, 37 tests |
| 88 | Diagnostic history + learning | ✅ | DiagnosticHistory, add/get/search/statistics, find_similar, 45 tests |
| 89 | Similar case retrieval | ✅ | CaseRetriever, Jaccard similarity, vehicle/year matching, 32 tests |
| 90 | Multi-symptom correlation | ✅ | SymptomCorrelator, 15+ predefined rules, partial match, 38 tests |
| 91 | Intermittent fault analysis | ✅ | IntermittentAnalyzer, 10+ patterns, condition extraction, 43 tests |
| 92 | Wiring diagram reference | ✅ | 5 circuit references, wire colors, test points, 29 tests |
| 93 | Torque specs + service data lookup | ✅ | 20 torque specs, 14 intervals, 8 clearances, 39 tests |
| 94 | AI evaluation + accuracy tracking | ✅ | EvaluationTracker, ADR-005 scorecard, model comparison, 21 tests |
| 95 | Gate 3 — AI diagnostic engine integration test | ✅ | 39 integration tests, 16 modules, 1163 total tests, **GATE 3 PASSED** |

## Track C2 — Media Diagnostic Intelligence (Phases 96–108)

Video and audio analysis for hands-free diagnostics. A mechanic films a bike starting/running/dying — the AI analyzes engine sound, visible symptoms, and behavior to suggest diagnostic paths.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 96 | Audio capture + preprocessing | ✅ | AudioPreprocessor, WAV file management, synthetic generators, 36 tests |
| 97 | Audio spectrogram analysis | ✅ | Pure Python DFT, 6 motorcycle frequency bands, 30 tests |
| 98 | Engine sound signature database | ✅ | 7 engine types, RPM-to-firing-frequency, profile matching, 25 tests |
| 99 | Audio anomaly detection | ✅ | 9 anomaly types (knock, misfire, valve tick, etc.), 29 tests |
| 100 | Video frame extraction | ✅ | Frame extraction plan, keyframes, timestamps, 34 tests |
| 101 | Visual symptom analysis (Claude Vision) | ✅ | Vision prompts, smoke/fluid color guides, 34 tests |
| 102 | Multimodal fusion | ✅ | Weighted evidence, conflict detection, 30 tests |
| 103 | Comparative audio analysis | ✅ | Before/after, improvement scoring, 30 tests |
| 104 | Real-time audio monitoring | ✅ | Session lifecycle, RPM estimation, alerts, 26 tests |
| 105 | Video annotation + timestamps | ✅ | Timestamped annotations, timeline, auto-annotate, 27 tests |
| 106 | Media-enhanced diagnostic reports | ✅ | Reports with media attachments, text formatting, 26 tests |
| 107 | AI audio coaching | ✅ | 5 capture protocols, quality evaluation, 30 tests |
| 108 | Gate 4 — Media diagnostics integration test | ✅ | 24 integration tests, 12 modules, 1575 total, **GATE 4 PASSED** |

## Track D — CLI + User Experience (Phases 109, 122–133) ✅ COMPLETE

The mechanic's daily driver interface. Phase 109 delivered the foundation; remaining phases shifted to 122-133 after Retrofit track insertion.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 109 | CLI foundation + command structure | ✅ | Click CLI, subcommands, 3-tier subscription system ($19/$99/$299), tier command, 41 tests |
| 122 | Vehicle garage management + photo intake | ✅ | Migration 013 + src/motodiag/intake/ package. VehicleIdentifier with Claude Haiku 4.5 vision, Sonnet escalation < 0.5 confidence, sha256 image cache, 1024px Pillow resize, tier caps 20/200/unlimited enforced from subscriptions.tier, 80% budget alert on crossing. CLI: garage add/list/remove, garage add-from-photo, intake photo, intake quota. 49 tests, all vision calls mocked (0 tokens burned), 2051 total, zero regressions. Schema v12→v13. |
| 123 | Interactive diagnostic session | ✅ | New cli/diagnose.py orchestration + 4 CLI subcommands (start/quick/list/show). Q&A loop caps at 3 rounds, stops on confidence ≥ 0.7. Tier-gated model access (individual=Haiku; shop/company=Sonnet). No migration — reuses Phase 03 substrate. 39 tests, all mocked (0 tokens), 2090 total, zero regressions. |
| 124 | Fault code lookup command | ✅ | New `cli/code.py` (392 LoC) replaces Phase 01's inline `code` command via `register_code(cli)` with legacy-eviction guard. Single command, three modes: default DB lookup with fallback chain (make-specific → generic → `classify_code()` heuristic + yellow banner), `--category <cat>` list mode (via Phase 111's `get_dtcs_by_category`), and `--explain --vehicle-id N` tier-gated AI interpretation (`FaultCodeInterpreter`). Reuses `_resolve_model/_load_vehicle/_load_known_issues/_parse_symptoms` from `cli.diagnose` — no copy-paste. Phase 05 `test_code_help` updated to use `--help`. 33 tests, all mocked (0 tokens), 2123 total, zero regressions. |
| 125 | Quick diagnosis mode | ✅ | Extended `cli/diagnose.py` (+180 LoC): `_parse_slug` + `_resolve_bike_slug` (4-tier match), `--bike SLUG` option on `diagnose quick`, new top-level `motodiag quick "<symptoms>"` via `register_quick` + Click `ctx.invoke()` delegation. Pure UX sugar — no substrate. First agent-delegated build (Builder-A). 34 tests, 2157 total, zero regressions, zero live tokens. |
| 126 | Diagnostic report output | ✅ | Extended `cli/diagnose.py` (+200 LoC): 3 pure formatters (txt/json/md) + `--format` and `--output` options on existing `diagnose show`. JSON has `format_version: "1"` for future schema evolution. Second agent-delegated phase. 22 tests, 2179 total, zero regressions, zero live tokens. |
| 127 | Session history browser | ✅ | Migration 014 adds nullable `notes` column (schema v13→v14). Extended `diagnose list` with 7 new filter options (vehicle-id/make/model/search/since/until/limit), new `diagnose reopen <id>` and `diagnose annotate <id> "note"` commands. Append-only notes with timestamp prefix. Third agent-delegated phase. 28 tests, 2207 total, zero regressions, zero live tokens. |
| 128 | Knowledge base browser | ✅ | New `cli/kb.py` (~320 LoC) + new `search_known_issues_text` repo function. 5 subcommands under `motodiag kb`: list (structured filters), show, search (free-text across title/description/symptoms), by-symptom, by-code (DTC upper-cased). Fourth agent-delegated phase. 26 tests, 2233 total, zero regressions, zero live tokens. |
| 129 | Rich terminal UI (tables, colors, progress) | ✅ | New `cli/theme.py` (~230 LoC) centralizes Console singleton + color maps (severity/status/tier) + icons + spinner context. Migrated 10+ inline `Console()` sites across 5 CLI modules. Progress spinners wrap AI calls (diagnose, code --explain, intake). Respects `NO_COLOR` + `COLUMNS`. Textual TUI explicitly deferred. 20 tests, 2253 total, zero regressions, zero live tokens. |
| 130 | Shell completions + shortcuts | ✅ | New `cli/completion.py` (~260 LoC) wraps Click's shell completion + adds 3 dynamic completers (bike slugs / DTC codes / session IDs), defensive on fresh DB. 4 hidden short aliases (d/k/g/q) via shallow-copy pattern. `shell_complete=` wired on 6 existing option sites. Sixth agent-delegated phase. 18 tests, 2271 total, zero regressions, zero live tokens. |
| 131 | Offline mode | ✅ | Migration 015 adds `ai_response_cache` (schema v14→v15). New `engine/cache.py` (SHA256 keys) + `cli/cache.py` (stats/purge/clear). `--offline` flag on `diagnose quick/start` + `code --explain`. Cache misses on `--offline` raise clear RuntimeError. Seventh agent-delegated phase. 30 tests, 2301 total, zero regressions, zero live tokens. |
| 132 | Export + sharing | ✅ | Extends Phase 126 `--format` with `html` and `pdf` on `diagnose show` + brings `kb show` to parity (md/html/pdf). New `cli/export.py` shared module (`format_as_html` via `markdown`, `format_as_pdf` via `xhtml2pdf`). `motodiag[export]` optional extras. Markdown is the pivot format. Eighth agent-delegated phase. 25 tests, 2326 total, zero regressions, zero live tokens. |
| 133 | Gate 5 — CLI integration test | ✅ | **GATE 5 PASSED.** Single new test file `test_phase133_gate_5.py` (~7 tests across 3 classes): one big end-to-end workflow test with 19 CLI invocations on shared DB + `CliRunner` + 3 AI mocks (`_default_diagnose_fn` / `_default_interpret_fn` / `_default_vision_call`), 4 CLI-surface breadth tests, 2 regression tests (Gate R still passes, schema ≥ v15, all `cli.*` submodules import). Consolidated 15-20 planned tests into 7 cohesive ones (same pattern as Gate R's 20→10). Ninth agent-delegated phase — Builder-A clean first pass, 7 tests passed in 6.04s on trust-but-verify. Zero new production code (pure observation), zero schema changes, zero live tokens. 2333 total. |

## Retrofit Track — Codebase Expansion Preparation (Phases 110–121)

Before any new tracks (Tracks K+) can build, the existing codebase needs refactoring to accommodate expanded scope: European brands, electric bikes, CRM, auth, workflows, i18n, reference data, business infrastructure. This is a non-negotiable prerequisite inserted between Track C2 and the remainder of Track D.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 110 | Vehicle registry + protocol taxonomy expansion | ✅ | Migration framework (reusable), PowertrainType/EngineType/BatteryChemistry enums, European CAN protocols, 35 tests, 1651 total |
| 111 | Knowledge base schema expansion | ✅ | Migration 004, DTCCategory enum (20), dtc_category_meta table, 6 OEM classifiers (BMW/Ducati/KTM/Triumph/Aprilia/Electric HV), 43 tests, 1694 total |
| 112 | User/auth layer introduction | ✅ | Migration 005, auth/ package (5 models, users_repo, roles_repo), 4 roles + 12 permissions + 31 mappings seeded, system user (id=1) owns pre-retrofit data, 40 tests, 1734 total |
| 113 | Customer/CRM foundation | ✅ | Migration 006, crm/ package (Customer + CustomerBike models, 14 repo functions), customers + customer_bikes tables, ownership history with transfer_ownership(), unassigned placeholder (id=1) owns pre-retrofit vehicles, 35 tests, 1769 total |
| 114 | Workflow template substrate | ✅ | Migration 007, workflows/ package (WorkflowCategory enum 13 members + 2 models + 10 repo functions), workflow_templates + checklist_items tables, 2 seed templates + 9 checklist items, 32 tests, 1801 total |
| 115 | i18n substrate | ✅ | Migration 008 + i18n/ package (Locale enum 7 codes, t() with locale→en→key fallback, string interpolation, bulk import, completeness reporter). 45 English strings across 4 namespaces. Schema v7→v8. 40 tests, 1841 total |
| 116 | Feedback/learning hooks | ✅ | Migration 009 + feedback/ package (FeedbackOutcome enum, OverrideField enum, DiagnosticFeedback + SessionOverride models, 8 repo functions, FeedbackReader read-only hook). diagnostic_feedback + session_overrides tables with FK CASCADE. Schema v8→v9. 26 tests, 1867 total |
| 117 | Reference data tables | ✅ | Migration 010 + reference/ package (4 enums, 4 models, 4 repo modules × 5 CRUD = 20 functions). 4 new tables with 8 indexes, year-range filter reused from known_issues pattern. Schema v9→v10. 28 tests, 1895 total |
| 118 | Billing/invoicing/inventory/scheduling substrate | ✅ | Migration 011 + 4 new packages (billing/accounting/inventory/scheduling). 9 tables + 14 indexes. Stripe column pre-wiring, invoice recalc with tax, low-stock alerts, calendar-ready appointments. Schema v10→v11. 37 tests, 1932 total |
| 119 | Media annotation layer | ✅ | Migration 012 + media/photo_annotation module (AnnotationShape enum 4 members, PhotoAnnotation model with 3 validators, 8 repo functions). photo_annotations table with 3 indexes. Dual-mode: FK-linked CASCADE or orphan-safe by image_ref. Schema v11→v12. 22 tests, 1954 total |
| 120 | Engine sound signature library expansion | ✅ | 4 new EngineType enum + SIGNATURES entries (ELECTRIC_MOTOR, DUCATI_L_TWIN, KTM_LC8_V_TWIN, TRIUMPH_TRIPLE). motor_rpm_to_whine_frequency helper. Dry-clutch-as-normal documented. 3 Phase 98 forward-compat test fixes. 38 tests, 1992 total |
| 121 | Gate R — Retrofit integration test | ✅ | **GATE R PASSED.** 10 integration tests: end-to-end workflow through every retrofit package, migration determinism (two-fresh-DB identical), CLI smoke + in-process package imports. One known limitation documented (migration 005 ALTER rollback). 1616 → 2002 total tests, zero regressions across the entire retrofit |

## Track E — Hardware Interface (Phases 134–147) ✅ COMPLETE

OBD adapter integration, live sensor data, ECU communication. Shifted from 122-135 due to Retrofit track insertion.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 134 | OBD protocol abstraction layer | ✅ | New `hardware/protocols/` package: `base.py` (`ProtocolAdapter` ABC — connect/disconnect/is_connected/read_dtcs/clear_dtcs/read_pid/read_vin/send_raw/get_protocol_name), `models.py` (`ProtocolConnection` frozen Pydantic model + `DTCReadResult` with `mode="before"` uppercase normalization + `PIDResponse` paired value/unit validator), `exceptions.py` (`ProtocolError` base + `ConnectionError` + `TimeoutError` + `UnsupportedCommandError(command)`). Wave 1 Builder (parallel dispatch) delivered clean first pass. No migration, no CLI. 49 tests, 2382 total. |
| 135 | ELM327 adapter communication | ✅ | New `hardware/protocols/elm327.py` (~584 LoC) — first concrete `ProtocolAdapter`. Wraps ELM327 OBD-II chip over pyserial (serial/USB/Bluetooth-SPP) with full AT-command handshake (`ATZ`/`ATE0`/`ATL0`/`ATSP0`), multi-frame response tolerance (scans for `43`/`41 XX` service echo rather than index 0), DTC parsing (mode 03 response → P/B/C/U codes), VIN assembly (mode 09 PID 02 multi-line). Unlocks ~80% of aftermarket OBD dongles (OBDLink, Vgate, generic clones). Wave 2 Builder. 52 tests. |
| 136 | CAN bus protocol implementation | ✅ | New `hardware/protocols/can.py` (~470 LoC). ISO 15765-4 over `python-can` (any backend — SocketCAN/PCAN/Vector/Kvaser/slcan/Peak). Hand-rolled ISO-TP (not `python-can-isotp` — fewer Windows-fragile deps). Services: mode 03 DTCs, mode 04 clear (`True` on positive, `False` on NRC 0x22, raises on others), mode 09 VIN, read_pid with big-endian byte combination. New `can = ["python-can>=4.0"]` optional extras. Target bikes: 2011+ Harley Touring, modern CAN-equipped Japanese/EU bikes. Wave 2 Builder. 38 tests. |
| 137 | K-line/KWP2000 protocol implementation | ✅ | New `hardware/protocols/kline.py` (~670 LoC) — ISO 14230-4 over pyserial for 90s/2000s Japanese sport-bikes (CBR/ZX/GSX-R/YZF-R1) and vintage Euro (Aprilia RSV, Ducati 996, KTM LC4). Module-level pure `_build_frame`/`_parse_frame` helpers for testability. Slow-baud wakeup, checksum validation, local-echo cancellation, strict timing windows. Services 0x10/0x11/0x14/0x18/0x1A/0x21. DTC decode (`(0x02, 0x01) → P0201`). Write services (0x27/0x2E/0x31) deliberately out of scope (tune-writing safety). Wave 2 Builder. 44 tests. |
| 138 | J1850 protocol implementation | ✅ | New `hardware/protocols/j1850.py` (~600 LoC) — J1850 VPW (10.4 kbps) for pre-2011 Harley. No direct bit-bang; talks through bridge devices (ScanGauge II, Daytona Twin Tec, Dynojet Power Commander, Digital Tech II clones). Multi-module DTC read merges ECM (P-codes) + BCM (B-codes) + ABS (C-codes) into single list, with supplementary `read_dtcs_by_module() -> dict`. Bridge variants: `daytona/scangauge/dynojet/generic`. `read_pid` raises `NotImplementedError` (Phase 141); `read_vin` raises `UnsupportedCommandError` (pre-2008 HDs lacked Mode 09). Wave 2 Builder. 27 tests. |
| 139 | ECU auto-detection + handshake | ✅ | New `hardware/ecu_detect.py` (~460 LoC). `AutoDetector` given a serial port + optional `make_hint` tries protocol adapters in priority order (Harley→J1850 first; Japanese→CAN/KWP2000; European→CAN/KWP2000; unknown→all four) until one negotiates. Per-protocol `_build_adapter` factory handles non-uniform adapter kwargs (each of CAN/K-line/J1850/ELM327 has different constructor signature). Lazy per-adapter imports so missing optional deps only surface when tried. `identify_ecu()` probes VIN + ECU part number + software version + supported OBD modes, each best-effort. `NoECUDetectedError` carries `port`, `make_hint`, and `errors: list[(name, exception)]` for introspection. No live hardware — all tests use `MagicMock` adapters. Wave 3 Builder. 31 tests. |
| 140 | Fault code read/clear operations | ✅ | First user-facing Track E phase. New `motodiag hardware` CLI group with `scan` (auto-detect + Mode 03 read + Rich table with 3-tier DTC enrichment), `clear` (Mode 04 with yellow safety warning + confirm/`--yes`), `info` (`identify_ecu()` → Rich Panel with VIN/ECU#/sw/modes). 5 new files: `hardware/mock.py` (249 LoC `MockAdapter` ABC-satisfying with configurable state), `hardware/connection.py` (255 LoC `HardwareSession` context manager — real/mock/override paths + disconnect-on-exception), `knowledge/dtc_lookup.py` (147 LoC `resolve_dtc_info` with `source=db_make/db_generic/classifier` discriminator), `cli/hardware.py` (556 LoC), `test_phase140_hardware_cli.py` (40 tests across 6 classes). `--mock` flag bypasses pyserial entirely for CI/offline dev. Tenth agent-delegated phase — Builder-A's cleanest pass (no sandbox block, 40 tests in 21.24s, zero iterative fixes). 2614 total. |
| 141 | Live sensor data streaming | ✅ | New `hardware/sensors.py` (SAE J1979 PID catalog + `SensorSpec`/`SensorReading`/`SensorStreamer` one-shot iterator) + `motodiag hardware stream --port --pids --hz --duration --output` subcommand with Rich `Live` panel + optional CSV append logging. `MockAdapter.pid_values` additive kwarg. 42 tests GREEN. Bug fix #1: hz-throttle test (generator post-yield sleep semantics). |
| 142 | Data logging + recording | ✅ | Migration 016 (schema v15→v16): `sensor_recordings` + `sensor_samples` + 4 indexes (FK CASCADE). New `hardware/recorder.py` (`RecordingManager` with SQLite/JSONL split at 1000 rows, transparent load merge, linear-interp `DiffReport` via stdlib `bisect`). `motodiag hardware log {start,stop,list,show,replay,diff,export,prune}` 8-subcommand subgroup. `parquet = [pyarrow]` optional extra. 52 tests GREEN. |
| 143 | Real-time terminal dashboard | ✅ | New `hardware/dashboard.py` (1282 LoC — `DashboardApp(textual.App)` with 3-column grid + ctrl+q/ctrl+r/d/1-6 keybindings, `GaugeWidget`/`PidChart`/`DTCPanel`/`StatusBar`, `LiveDashboardSource` wrapping SensorStreamer + `ReplayDashboardSource` walking recordings). `motodiag hardware dashboard` subcommand. `dashboard = [textual>=0.40,<1.0]` optional extra. Lazy Textual import with install-hint. 46 tests GREEN. Bug fix #1: `GaugeWidget` numeric display used unclamped value. |
| 144 | Hardware simulator | ✅ | New `hardware/simulator.py` (1212 LoC — `SimulationClock` manual-tick, 9 Pydantic discriminated-union event models, `Scenario` aggregate with cross-event validators, `ScenarioLoader` from_yaml/from_recording/list_builtins, `SimulatedAdapter(ProtocolAdapter)` sibling to MockAdapter). 10 built-in YAML scenarios under `scenarios/`. `motodiag hardware simulate {list,run,validate}` + `--simulator SCENARIO` opt-in on scan/clear/info. `pyyaml>=6` base dep + `package-data` for YAMLs. 81 tests GREEN. Bug fix #1: `_coerce_pid` bare-number → decimal (JSON round-trip identity). |
| 145 | Adapter compatibility database | ✅ | Migration 017 (schema v16→v17): `obd_adapters` + `adapter_compatibility` + `compat_notes` + 6 indexes. New `hardware/compat_repo.py` (870 LoC CRUD + ranking + `check_compatibility` specificity match + `protocols_to_skip_for_make` AutoDetector filter hook) + `compat_loader.py` (idempotent JSON seed). Seed data: 24 real adapters (OBDLink MX+/SX/LX, Dynojet PV3, Daytona TCFI, Vance & Hines FP4, Autel AP200BT, Foxwell NT301, Vgate iCar Pro, HD Digital Tech II, 24 total) + 110 compat matrix rows + 12 curated notes. `motodiag hardware compat {list,recommend,check,show,note,seed}`. AutoDetector gets `compat_repo=None` kwarg (Phase 139 backward-compat preserved). 57 tests GREEN. Bug fix #1: TestCompatCLI fixture three-layer db_path redirect. Bug fix #2: compat_notes.json `obdlink-cx` → `scantool-obdlink-cx` slug. |
| 146 | Connection troubleshooting + recovery | ✅ | `hardware/connection.py` +446 LoC: `RetryPolicy` Pydantic model (exponential-with-clamp backoff) + `ResilientAdapter` wrapper (retries transient wire ops, never retries `UnsupportedCommandError`/`NoECUDetectedError`/`clear_dtcs`) + `HardwareSession` `retry_policy`/`auto_reconnect` kwargs + `try_reconnect()` helper. AutoDetector `verbose` + `on_attempt` callback kwargs. `MockAdapter.flaky_rate` + `flaky_seed` for deterministic retry tests. `motodiag hardware diagnose` 5-step troubleshooter (port open / ATZ probe / AutoDetector negotiate / VIN read / DTC scan) with mechanic-readable Rich-panel remediation. `--retry`/`--no-retry` on scan/info (default on), clear (default off). 56 tests GREEN. Bug fix #1: `MockAdapter` lazy-import in diagnose `--mock` branch. Bug fix #2: `--retry`/`--simulator` mutex → silent retry=False. |
| 147 | Gate 6 — Hardware integration test | ✅ | **GATE 6 PASSED — Track E closed.** New `tests/test_phase147_gate_6.py` (875 LoC, 8 tests across 3 classes, zero production code). Class A: one big CliRunner workflow — `garage add` → `compat seed/recommend` → `hardware info/scan --simulator` → `hardware log start/list/show/replay/export` → `hardware stream` → `hardware diagnose --mock` → `hardware clear --simulator`. Shared DB fixture + 3 defensive AI mocks + `time.sleep` no-ops. Class B: 4 surface tests verify 9-subcommand registration + subgroup children + `--help` + submodule import graph. Class C: 3 regression tests — subprocess re-runs of Gate 5 + Gate R + tiered schema floor `>= 15/16/17`. 8 tests GREEN. Bug fix #1: `CliRunner(mix_stderr=False)` removed in Click 8.2+. Bug fix #2: `FROM recordings` → `FROM sensor_recordings`. |

## Track F — Advanced Diagnostics (Phases 148–159)

Power features for experienced mechanics. Shifted from 136-147 due to Retrofit track insertion.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 148 | Predictive maintenance | ✅ | **First Track F phase.** Promotes `advanced` package Scaffold → Active. New `advanced/models.py` (frozen Pydantic v2 `FailurePrediction` + `PredictionConfidence` enum), `advanced/predictor.py` (395 LoC — `predict_failures(vehicle, horizon_days, min_severity, db_path)` with 4-pass candidate retrieval, match-tier scoring exact_model=1.0/family=0.75/make=0.5/generic=0.3, severity-keyed heuristic onset critical=15k/high=30k/medium=50k/low=80k mi, mileage + age scoring bonuses, Forum-tip-precedence `preventive_action` extraction, `verified_by` substring heuristic, horizon/severity filters, stable sort cap 50). `motodiag advanced predict --bike SLUG \| --make/--model/--year/--current-miles [--horizon-days] [--min-severity] [--json]`. Zero migration, zero AI, zero live tokens. 44 tests GREEN. |
| 149 | Wear pattern analysis | ✅ | File-seeded 30-pattern `advanced/wear.py` with overlap-ratio scoring. Mechanic symptom vocabulary ("tick of death" / "chain slap on decel" / "dim headlight"). Substring-either-direction matching. `motodiag advanced wear --bike SLUG --symptoms ...`. 33 tests GREEN. |
| 150 | Fleet management | ✅ | Migration 018: `fleets` + `fleet_bikes` tables. `advanced/fleet_repo.py` (12 CRUD) + `fleet_analytics.py` (Phase 148/149 rollups). `motodiag advanced fleet {create,list,show,add-bike,remove-bike,rename,delete,status}` (8 subcommands). 35 tests GREEN. |
| 151 | Maintenance scheduling | ✅ | Migration 019: `service_intervals` + `service_interval_templates` (44 seeded). `advanced/schedule_repo.py` + `scheduler.py` dual-axis (miles OR months) with month-end clamp. `motodiag advanced schedule {init,list,due,overdue,complete,history}`. 37 tests GREEN. |
| 152 | Service history + monotonic mileage | ✅ | Migration 020: `service_history` + `vehicles.mileage` column. `advanced/history_repo.py` with monotonic mileage bump. `predictor.py` +0.05 bonus when `mileage_source='db'`. `motodiag advanced history {add,list,show,show-all,by-type}`. 35 tests GREEN. |
| 153 | Parts cross-reference | ✅ | Migration 021: `parts` + `parts_xref` tables. `advanced/parts_repo.py` + `parts_loader.py` with OEM ↔ aftermarket catalog. `predictor.py` populates `parts_cost_cents` on FailurePrediction. `motodiag advanced parts {search,xref,show,seed}`. 31 tests GREEN. |
| 154 | OEM Technical Service Bulletins (TSBs) | ✅ | Migration 022: `technical_service_bulletins` with SQL LIKE `model_pattern` + partial unique index. `advanced/tsb_repo.py` (466 LoC) + 44 real HD/Honda/Yamaha entries seeded. Auto-seed on `init_db()`. `predictor.py` `applicable_tsbs` via 4-char token overlap + severity bucket-adjacency. `motodiag advanced tsb {list,search,show,by-make}`. 32 tests GREEN. |
| 155 | NHTSA safety recalls | ✅ | Migration 023: extends `recalls` with nhtsa_id/vin_range/open + new `recall_resolutions` junction. `advanced/recall_repo.py` (603 LoC) with VIN validation + range check + WMI decode + resolutions. `predictor.py` `applicable_recalls` + critical-severity escalation. `motodiag advanced recall {list,check-vin,lookup,mark-resolved}`. 31 tests GREEN. |
| 156 | Comparative diagnostics | ✅ | No migration. `advanced/comparative.py` (709 LoC) two-stage reduction — per-recording summary → percentile across recordings. `--peers-min 5` noisy-stats guard. 200-row cohort cap. `motodiag advanced compare {bike,recording,fleet}`. 34 tests GREEN. |
| 157 | Healthy baselines | ✅ | Migration 024: `baselines` table with per-(make, model, year, pid) percentile statistics aggregated from mechanic-flagged-healthy recordings. `motodiag advanced baseline {show,flag-healthy,rebuild,list}`. 31 tests GREEN. |
| 158 | Sensor drift tracking | ✅ | No migration. `advanced/drift.py` (597 LoC) stdlib mean-of-products linear regression. Flags drifting >5%/30d slow / ≥10%/30d fast. O2 aging / coolant silting / battery decay. Unicode sparkline + CSV export. `predictor.py` opt-in `_apply_drift_bonus` (capped +0.1). `motodiag advanced drift {bike,show,recording,plot}`. 39 tests GREEN. |
| 159 | Gate 7 — Advanced diagnostics integration test | ✅ | **GATE 7 PASSED — Track F closed.** `tests/test_phase159_gate_7.py` (8 tests): end-to-end workflow across all 10 advanced subgroups + surface breadth + Gate 5/6 subprocess re-runs. 3349/3351 full regression passing. Zero new production code. |

## Track G — Shop Management + Optimization (Phases 160–174)

Shop-level features: log bikes in your shop, track issues across the fleet, triage what to fix first, auto-generate parts lists, optimize workflow. Shifted from 148-162 due to Retrofit track insertion.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 160 | Shop profile + multi-bike intake | ✅ | Migration 025: `shops` + `intake_visits` tables. `shop/shop_repo.py` (337 LoC, 11 fns + hours_json validator) + `shop/intake_repo.py` (481 LoC, guarded `open→closed\|cancelled→(reopen)→open` lifecycle). `cli/shop.py` (1003 LoC) new top-level `motodiag shop` group with 3 subgroups × 22 subcommands — `profile` (5), `customer` (9, first CLI for Phase 113 dormant `crm/`), `intake` (8). 44 tests GREEN. Zero AI. |
| 161 | Work order system | ✅ | Migration 026: `work_orders` table with denormalized shop_id/vehicle_id/customer_id + intake_visit_id FK SET NULL + 4 indexes. `shop/work_order_repo.py` (748 LoC, 14 fns + 7 dedicated lifecycle transitions + guarded `draft→open→in_progress→(on_hold\|completed\|cancelled)→(reopen)→open` + composable list filters). `cli/shop.py` +473 LoC = `motodiag shop work-order {create,list,show,update,start,pause,resume,complete,cancel,reopen,assign,unassign}` (12 subcommands). 47 tests GREEN. Forward-compat bug fix: Phase 160 + Phase 161 rollback tests migrated to `rollback_to_version` pattern after Phase 161 FK broke Phase 160's bare-drop test. |
| 162 | Issue logging + categorization | ✅ | Migration 027: `issues` table (work_order_id FK CASCADE + symptoms/sessions FK SET NULL + self-referencing duplicate_of FK) with **12-category CHECK** (engine/fuel/electrical/cooling/exhaust/transmission/brakes/suspension/drivetrain/tires_wheels/accessories/rider_complaint/other — Domain-Researcher override from default 7) + 4-tier severity + 4-state guarded lifecycle + 5 indexes. `shop/issue_repo.py` (720 LoC, 14 fns + crosswalk dict for Phase 163 AI categorization). `cli/shop.py` +656 LoC = `motodiag shop issue {add,list,show,update,resolve,reopen,mark-duplicate,mark-wontfix,categorize,link-dtc,link-symptom,stats}` (12 subcommands). 42 tests GREEN. mark-wontfix requires audit-trail notes; linked_dtc_code soft-validates to survive seed reloads; duplicate_of cycle prevention via one-hop canonical check. |
| 163 | Repair priority scoring | ✅ | First Track G AI phase. Composes against Phase 162.5 `shop/ai_client.py` (zero direct anthropic imports — anti-regression grep test enforced). New `shop/priority_scorer.py` (311 LoC) + `shop/priority_models.py` (82 LoC). 4-tier rubric (CRITICAL safety / HIGH ridability / MEDIUM service / LOW cosmetic) + wait-time aging + customer-history bonus baked into cached system prompt. Mechanic-intent preservation: AI overwrites work_orders.priority ONLY when confidence > 0.75; below threshold logs to ai_response_cache (kind='priority_score') without DB mutation. Safety override + `--force` escape hatch. Write-back via Phase 161 `update_work_order` whitelist (never raw SQL). Per-call cap 3¢ + session budget 50¢ default. CLI: `motodiag shop priority {score, rescore-all, show, budget}`. 26 tests GREEN; injection seam pattern (`_default_scorer_fn=None`) is canonical Track G AI test convention. Zero schema migration. |
| 164 | Automated triage queue | ✅ | Migration 028: `ALTER TABLE shops ADD COLUMN triage_weights TEXT` (nullable). Pure query-synthesis + deterministic scoring over Phase 161/162/163; soft-guards Phase 165 via `importlib.util.find_spec("motodiag.shop.parts_needs")`. New `shop/triage_queue.py` (365 LoC) — `build_triage_queue` + `ShopTriageWeights` + `TriageItem` + 5 mutators + `ShopTriageError`. Triage formula = priority + wait + parts + urgent_flag - skip_penalty (per-shop tunable). Markers ride on `work_orders.description` via `[TRIAGE_URGENT] ` / `[TRIAGE_SKIP: reason] ` prefix tokens (idempotent, parser-anchored). CLI: `motodiag shop triage {queue, next, flag-urgent, skip, weights}`. 32 tests GREEN. |
| 165 | Parts needed aggregation | ✅ | Migration 029: 3 new tables (work_order_parts + parts_requisitions + parts_requisition_items). Bridges Phase 153 parts catalog (`parts` + `parts_xref`) to Phase 161 work_orders via FK reuse — zero schema duplication. New `shop/parts_needs.py` (605 LoC, 18 fns including 5 CRUD + 3 lifecycle + critical `_recompute_wo_parts_cost` helper that writes back via Phase 161 `update_work_order` whitelist — NEVER raw SQL, verified by mock-patch audit test). **Phase 164 contract satisfied automatically:** `list_parts_for_wo(wo_id, db_path=None)` exported with the exact name 164's soft-guard imports. Immutable requisition snapshots (`build_requisition` freezes header + items at creation; validates wo_ids belong to shop_id). OEM/aftermarket cost columns via `parts_repo.get_xrefs`. CLI: `motodiag shop parts-needs {add, list, consolidate, mark-ordered, mark-received, requisition {create, list, show}}` (8 subcommands). 38 tests GREEN. |
| 166 | Parts sourcing + cost optimization | ✅ | Second Track G AI phase. Composes against Phase 162.5 ShopAIClient (zero direct anthropic imports, anti-regression grep test). Migration 030: single `sourcing_recommendations` audit table. New `shop/sourcing_models.py` (65 LoC) + `shop/parts_sourcing.py` (482 LoC, recommend_source + get_recommendation + sourcing_budget). System prompt baked from Domain-Researcher pricing brief — decision tree + 6-tier vendor taxonomy + counter-intuitive aftermarket wins (Ricks stators, EBC HH pads). Append-only audit log with cache_hit separate from cost_cents. CLI: `motodiag shop sourcing {recommend, show, budget}`. 27 tests GREEN. `optimize_requisition` + `compare` deferred to Phase 169. |
| 167 | Labor time estimation | ✅ | Third Track G AI phase. Composes against Phase 162.5 ShopAIClient (zero direct anthropic imports) + writes back via Phase 161 update_work_order whitelist (zero raw SQL) — both enforced by anti-regression grep tests. Migration 031: `labor_estimates` audit history table with full breakdown + alternatives + environment notes. Math-consistency guard rejects AI output where adjusted ≠ base*(1+skill)*(1+mileage); retry at temp 0.1 on first mismatch. System prompt baked from Domain-Researcher pricing brief (labor norms + per-platform adjustments + skill/mileage multipliers). Reconciliation is pure arithmetic at ±20% threshold. CLI: `motodiag shop labor {estimate, bulk, show, history, reconcile, budget}`. 33 tests GREEN (incl. 2 anti-regression grep tests). |
| 168 | Bay/lift scheduling | ✅ | Migration 032: `shop_bays` + `bay_schedule_slots` tables + 4 indexes. Stdlib-only deterministic scheduling engine in `shop/bay_scheduler.py` (702 LoC): greedy next-free-window auto-assign with level-loading tie-break + overrun detection at 25% buffer + sweep-line conflict detection (≥15min = error) + utilization tracking + `optimize_shop_day(random_seed)` deterministic for per-shop-per-day reproducibility. FK asymmetry: `work_order_id SET NULL` on slots preserves utilization history through WO deletion (load-bearing for Phase 171 analytics). CLI: `motodiag shop bay {add, list, show, deactivate, schedule, reschedule, conflicts, optimize, utilization, calendar}` (10 subcommands). 37 tests GREEN. **Closes Track G deterministic core (161/162/164/165/168) alongside AI phases (163/166/167) + 162.5.** |
| 169 | Revenue tracking + invoicing | ✅ | Micro-migration 033: single `invoices.work_order_id` column + index. **Zero new tables — reuses Phase 118 `invoices` + `invoice_line_items` + `accounting.invoice_repo`.** `shop/invoicing.py` (~496 LoC): `generate_invoice_for_wo` (labor from Phase 167 actual_hours fallback estimated; parts lines from Phase 165 installed+received; tax + shop-supplies pct/flat + optional diagnostic fee stacking), `mark_invoice_paid`, `void_invoice` (enables regen via `-Rn` invoice-number suffix), `get_invoice_with_items`, `list_invoices_for_shop`, `revenue_rollup` (shop-scoped or all-shops). CLI: `motodiag shop invoice {generate, list, show, mark-paid, void, revenue}` (6 subcommands). 32 tests GREEN. **Track G commercial core closes: intake→triage→WO→parts→labor→bay→completion→invoice→revenue all through `motodiag shop *`.** |
| 170 | Customer communication | ✅ | Migration 034: `customer_notifications` table with 4 FKs + 2 CHECK constraints + 3 indexes. `shop/notification_templates.py` (~265 LoC): 23 templates across 10 events × 2-3 channels (email/sms/in_app); content baked from motorcycle-mechanic feedback (first-name recipient, WO# + shop phone everywhere, plain language, prominent totals). `shop/notifications.py` (~510 LoC): 8 public APIs with guarded `pending → sent\|failed\|cancelled` lifecycle; `resend_notification` creates new pending row preserving failure audit. **Queue-only — actual email/SMS delivery deferred to Track J transport.** CLI: `motodiag shop notify {trigger, preview, list, mark-sent, mark-failed, cancel, resend, templates}` (8 subcommands). 32 tests GREEN single-pass. Total shop CLI: 13 subgroups, 96 subcommands. |
| 171 | Shop analytics dashboard | ✅ | Read-only deterministic rollups over Track G state. Zero migrations, zero AI. `shop/analytics.py` (~524 LoC): 10 rollup functions (throughput/turnaround/utilization/overrun/labor-accuracy/top-issues/top-parts/mechanic-perf/customer-repeat) + `dashboard_snapshot` composer that delegates to Phase 168 utilization + Phase 169 revenue without duplicating. Each rollup is a pure function returning a Pydantic summary — Phase 173 can use them as rule conditions. CLI: `motodiag shop analytics {snapshot, throughput, turnaround, utilization, overruns, labor-accuracy, top-issues, top-parts, mechanic, customer-repeat}` (10 subcommands). 31 tests GREEN. Total shop CLI: 14 subgroups, 106 subcommands. |
| 172 | Multi-mechanic assignment | ✅ | Migration 035: `shop_members` (per-shop role on Phase 112 global RBAC — user can own one shop + be tech at another) + `work_order_assignments` (audit log of reassignments preserving history). `shop/rbac.py` (~475 LoC): 14 public functions + 4 exceptions; `has_shop_permission` walks `shop_members.role → roles → role_permissions → permissions` with zero catalog duplication; `reassign_work_order` writes back via Phase 161 whitelist (no raw SQL — grep-test enforced) + guards (terminal WO raises, apprentice/non-shop mechanic raises). CLI: `motodiag shop member {add, list, set-role, deactivate, reactivate}` (5) + `work-order {reassign, assignments}` (2 new). Total shop CLI: 15 subgroups, 113 subcommands. 32 tests GREEN. |
| 173 | Workflow automation rules | ✅ | Migration 036: `workflow_rules` + `workflow_rule_runs` (audit). If-this-then-that engine composing every prior Track G primitive. **Rules are JSON data; engine is code** — operators author without code changes. 3 modules (~900 LoC): `workflow_conditions.py` (12 condition types), `workflow_actions.py` (8 action types; every mutation routes through Phase 161/162/164/170/172 whitelists — no raw SQL, grep-test enforced), `workflow_rules.py` (CRUD + evaluator + firing + audit). Fail-one-continue-rest action semantics. `build_wo_context` one-time context assembly feeds all condition evaluators (no N+1 queries). CLI: `motodiag shop rule {add, list, show, update, enable, disable, delete, fire, test, history}` (10 subcommands). 42 tests GREEN. Total shop CLI: 16 subgroups, 123 subcommands. **Track G automation pillar closes.** |
| 174 | Gate 8 — Shop management integration test | ✅ | **🎯 TRACK G CLOSED.** End-to-end integration test walking 19 steps through every Track G subgroup via CLI + repo: profile → member → customer → intake → WO → issue → parts → bay → start → reassign → notify → complete → invoice → revenue → paid → analytics → rule → audit. Plus 2 scenario tests (shop isolation; rule firing across lifecycle). `TRACK_G_SUMMARY.md` (~510 LoC) closure doc ships alongside with 14-phase inventory, 8 design pillars, mechanic workflow, Track H seeds. 5 tests GREEN in 3.25s. Full targeted regression: 653/653 GREEN across Phase 113 + 118 + 131 + 153 + Track G 160-174 + 162.5. Zero regressions. **Project version 0.10.5 → 0.11.0.** |

## Track H — API + Web Layer (Phases 175–184)

REST API for future mobile app / web dashboard. Paywall enforcement flips to HARD mode here. Shifted from 163-172 due to Retrofit track insertion.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 175 | FastAPI foundation + project structure | ✅ | **🚀 TRACK H OPEN.** New `src/motodiag/api/` package (~750 LoC, 8 files): `create_app(settings, db_path_override)` factory returns fresh FastAPI instance per call; `deps.py` with `get_settings`/`get_db_path`/`get_request_id` `Depends()` providers (canonical test-override seam); `errors.py` maps **30+ Track G domain exceptions** to RFC 7807 `ProblemDetail` JSON with correct HTTP status (404/409/422/403/400/500) + request-id echo; `RequestIdMiddleware` (accepts client-supplied `X-Request-ID` or generates UUID4) + `AccessLogMiddleware`; `/healthz` DB connectivity probe + `/v1/version` contract endpoint + `/v1/shops/{id}` smoke route proving full DI chain end-to-end; `motodiag serve [--host X --port N --reload --workers N]` uvicorn launcher. CORS wired with mechanic-friendly dev defaults (localhost:3000 + 5173). Lazy Track G imports keep app-creation fast. 26 tests GREEN single-pass. Targeted regression: 679/679 GREEN. **FastAPI 0.136 + uvicorn 0.45. Project version 0.11.0 → 0.11.1.** |
| 176 | Auth + API keys | ✅ | **💰 MONETIZATION GATE SHIPPED.** Stripe-style `mdk_live_*` API keys (sha256-hashed, plaintext returned once) + in-memory token-bucket rate limiter (per-tier budgets, X-RateLimit-* headers, 429 with Retry-After) + `BillingProvider` ABC with FakeBillingProvider (tests) + StripeBillingProvider (prod, lazy stripe-lib import) + Stripe webhook endpoint with HMAC + event_id idempotency + 4 FastAPI deps (`get_api_key`/`require_api_key`/`get_current_user`/`require_tier(T)`) + 402 Payment Required semantics for tier insufficient + 2 CLI subgroups (`apikey` + `subscription`, 9 subcommands total). Migration 037: new api_keys + stripe_webhook_events + extends Phase 118 subscriptions with 6 Stripe cycle columns (reuse-substrate pattern). 58 tests GREEN. Targeted regression 736/736. **Project version 0.11.1 → 0.12.0. moto-diag is now a real paywalled API service.** |
| 177 | Vehicle endpoints | ✅ | First full-CRUD Track H domain router. `GET/POST/PATCH/DELETE /v1/vehicles*` exposes Phase 04 vehicles via HTTP with owner scoping (`vehicles.owner_user_id` retrofit, migration 038) + tier-gated quotas (individual=5, shop=50, company=unlimited; 402 when exceeded). 7 new `_for_owner` repo helpers (Phase 112 retrofit pattern) + 6 HTTP endpoints + 4 Pydantic schemas with `Literal[...]` field validation. Cross-user 404 (not 403) for enumeration prevention. List response includes tier + quota_limit + quota_remaining. 33 tests GREEN single-pass. Targeted regression 784/784. Unscoped CLI + background jobs continue to work. **Project version 0.12.0 → 0.12.1.** |
| 178 | Diagnostic session endpoints | ✅ | 9 endpoints over `/v1/sessions*` exposing Phase 07 diagnostic_sessions with owner scoping + monthly quota (individual=50/mo, shop=500/mo, company=unlimited). **Zero migration** — Phase 112 retrofit already added user_id. 11 `_for_owner` repo helpers + Lifecycle transitions as dedicated POSTs (close/reopen). 35 tests GREEN single-pass. Focused regression 168/168 GREEN. **Project version 0.12.1 → 0.12.2.** |
| 179 | Knowledge base endpoints | ✅ | Smallest Track H domain router (310 LoC, 17 tests, <1hr). 7 endpoints over `/v1/kb/*`: DTC lookup + search, symptom search, known-issue search + detail, unified search (via Phase 09 `search_all`), DTC categories list. **Zero migration, zero tier gating** — KB is a core product feature; any authenticated caller gets in. Limit capped at 200. 17 tests GREEN single-pass. Project version 0.12.2 → 0.12.3. |
| 180 | Shop management endpoints | ✅ | Biggest pure-composer Track H router (838 LoC, 24 endpoints, 22 tests). **Zero migration.** Maps Track G's 16-subgroup CLI console onto `/v1/shop/*` HTTP across 9 sub-surfaces (profile/members/customers/intake/work-orders/issues/invoices/notifications/analytics). All routes require `require_tier("shop")` + Phase 172 shop membership. `TransitionAction` Literal dispatches all 7 WO lifecycle states through one POST. Cross-shop = 403 (not 404 — shops are global-registry). Pragmatic omissions: parts/sourcing/labor/bay/triage/priority/rules deferred to Phase 181+. **Track H scorecard: 6 phases done, 51 endpoints across 8 sub-surfaces.** Project version 0.12.3 → 0.12.4. |
| 181 | WebSocket live data | ✅ | First non-CRUD Track H surface. `ws /v1/live/{session_id}` streams live OBD sensor frames (rpm / coolant_c / throttle_pct / voltage_v / ts) to authenticated + paid-tier clients, session-scoped. **New `api/routes/live.py` (462 LoC)**: `LiveReadingProvider` ABC + `FakeLiveProvider` (deterministic seeded stream) + `ConnectionManager` + `get_live_provider` env-dispatched factory (`=fake` default, `=obd` deferred to Phase 140 wiring) + six custom 4xxx close-code constants (4401 invalid key / 4402 subscription / 4404 session / 4429 rate limit / 4500 provider / 1000 normal) + `_clamp_interval` (50-10000ms) + dual-path auth (`?api_key=` query OR `Sec-WebSocket-Protocol: bearer.<key>` subprotocol). Route flow: accept → validate key → check active sub → check session ownership → stream loop with non-blocking client-action polling + pause/resume/set_interval_ms handling. `try/finally` guarantees provider cleanup on disconnect. Rate-limit exemption `/v1/live` defensive (BaseHTTPMiddleware skips WS). **Build deviation**: `provider_override` kwarg removed from route signature (FastAPI coerced annotated ABC into a query-param field); override moved to module-level factory for clean monkey-patching. **24 tests GREEN in 23.09s** (6 classes: FakeProvider ×5 + ModuleHelpers ×6 + WebSocketAuth ×6 + FrameStreaming ×4 + ProviderErrors ×2 + RateLimitExemption ×1). **Track H regression 175-181: 215/215 GREEN in 5m 19s, zero regressions.** No migration, schema stays at 38. **Track H scorecard: 7 phases done, 51 HTTP + 1 WS across 9 sub-surfaces.** Project version 0.12.4 → 0.12.5. |
| 182 | Report generation endpoints | ✅ | Three distinct downloadable artifacts over one renderer pipeline + one router shape. **New `motodiag/reporting/` package (832 LoC)**: `ReportRenderer` ABC + `TextReportRenderer` (stdlib-only fallback) + `PdfReportRenderer` (reportlab 4.4.10 Platypus; already installed as transitive dep; BSD-licensed) + three builders (`build_session_report_doc` / `build_work_order_report_doc` / `build_invoice_report_doc`) that hydrate a `ReportDocument` dict and raise Phase 178/172/169 domain exceptions. **6 endpoints** (3 JSON preview + 3 PDF download): `GET /v1/reports/{session,work-order,invoice}/{id}` + `/pdf`. Session reports owner-gated (no tier — DIY users get their own diagnostics on paper); WO + invoice reports `require_tier("shop")` + shop-membership. `ReportDocument` shape: dict with title/subtitle/sections/footer; sections one of `body` / `rows` / `bullets` / `table`; unknown shapes silently skipped (forward compat). `PdfReportRenderer` XML-escapes user strings so customer names with `<`, `>`, `&` don't break Paragraph rendering. New `ReportBuildError` → 500 mapping. **Build deviations**: (1) `get_dtc` returns dict not object (one-line fix); (2) `generate_invoice_for_wo` kwarg is `wo_id=` not `work_order_id=`; (3) 962 product LoC vs planned 750; (4) 33 tests vs planned 30. **33 tests GREEN in 41.62s** (7 classes). **Full Track H regression 175-182: 248/248 GREEN in 6m 36s, zero regressions.** No migration, schema stays at 38, zero AI. **Track H scorecard: 8 phases done, 57 HTTP + 1 WS across 10 sub-surfaces.** Project version 0.12.5 → 0.12.6. |
| 183 | API documentation + OpenAPI spec | ✅ | Pure spec polish — zero runtime-behavior change; only `/openapi.json` + `/docs` output changes. **New `api/openapi.py` (459 LoC)** overrides FastAPI's `openapi()` to inject `info.contact` + `info.license` (MIT + SPDX) + `servers` list + 10-entry tag catalog + `components.securitySchemes` (apiKey `X-API-Key` header + bearerAuth HTTP Bearer) + 7 reusable error responses in `components.responses.*` (401/402/403/404/422/429 with `Retry-After`/500, all `$ref` to ProblemDetail) + explicit `PROBLEM_DETAIL_SCHEMA` fallback. **Per-operation walker** attaches security + error-response refs based on tags (public `{"meta"}` + `/v1/billing/webhooks*` stay open; tier-gated `{"shop-management", "reports", "live", "vehicles", "sessions"}` get 402), path params (→ 404), and request bodies (→ 422). Result cached on `app.openapi_schema`. **New `Settings.api_servers`** (pipe/comma `url|description` syntax) + `api_servers_list` parser. **Build deviations**: (1) 459 product LoC vs planned 300 — catalogs carry informative descriptions for Swagger UI readability; (2) `TAG_CATALOG` passed via `get_openapi(tags=...)` at construction time (post-hook `app.openapi_tags` is ignored by custom overrides); (3) `api_servers` as string not JSON (pydantic-settings limitation); (4) 43 tests vs planned 20 — each catalog member + enrichment rule got isolated coverage. **43 tests GREEN in 23.16s** (8 classes). **Full Track H regression 175-183: 291/291 GREEN in 7m 01s, zero regressions.** No migration, schema unchanged at 38, zero AI. **Track H scorecard: 9 phases done, 57 HTTP + 1 WS across 10 sub-surfaces, fully-documented OpenAPI 3.1.** Project version 0.12.6 → 0.12.7. |
| 184 | Gate 9 — API integration test | ✅ | **🎯 TRACK H CLOSED.** End-to-end HTTP integration test walking intake → invoice using only Phase 175-183 endpoints. Same shape as Gate 8 but via API instead of CLI. **New `tests/test_phase184_gate9.py` (618 LoC, 10 tests, 5 classes)**: `TestGate9HappyPath` (27-step single walk: `/v1/version` → `/v1/shop/profile` → customer → vehicle → session with symptoms/DTCs/diagnosis → session PDF → KB lookups → session close → WO open/start → issue → WO complete + actual_hours → invoice generate @ $100/hr + 8.25% tax → WO PDF + invoice PDF + invoice JSON preview → analytics snapshot → listings → openapi.json sanity, asserts status + invariants at every step); `TestGate9CrossUserIsolation` (User B gets 404 on User A's vehicles + session reports); `TestGate9CrossShopIsolation` (Shop B gets 403 on Shop A's WO/invoice reports + list + create); `TestGate9OpenAPIContract` (×4 — 10 tags + 7 error responses + 2 security schemes + apiKey attached); `TestGate9AntiRegression` (SCHEMA_VERSION=38, TRACK_H_SUMMARY.md exists with "Track H closed" phrase, Phase 184 doc exists). **New `docs/phases/completed/TRACK_H_SUMMARY.md` (214 LoC)** — closure doc: 10-phase inventory + sub-surface breakdown + **8 design pillars** (auto-mapped domain exceptions / compose-don't-duplicate routers / scope-as-code / renderer ABC / spec-as-SSOT / factory-pattern transports / hard-paywall-soft-discovery / RFC 7807 + correlation IDs) + mechanic-facing workflow map + **known limitations organized by resolving future track** (Track I: signup/login/uploads/photo-ID; Track J: Redis multi-worker state + IP rate limit + notification worker + production uvicorn; post-Gate-9: OBD hardware wiring + OpenAPI docs site + PDF branding). **Build deviations**: (1) 10 tests vs planned 20 — integration-sweep shape matches Gate 8; (2) `/v1/version` shape is `{api_version, package, schema_version}` not `{version}`; (3) bootstrap via direct repo calls (Track I scope); (4) closure doc 214 LoC vs planned 180 — 8-pillar + limitations-by-future-track handoff detail. **10 tests GREEN single-pass in 18.22s.** **Full Track H regression 175-184: 301/301 GREEN in 6m 02s, zero regressions. 🎯 Track H closes green.** **Zero production code, zero migration, schema stays at 38.** **Track H final scorecard: 10 phases, 57 HTTP + 1 WS across 10 sub-surfaces, 301 tests, fully-documented OpenAPI 3.1 spec, 2 migrations (037+038), 8 codified design pillars.** Project version 0.12.7 → 0.13.0. |

## Track I — Mobile App (iOS + Android) (Phases 185–204)

> **Track I status is owned by the mobile repo.** Per-phase status for
> Phases 185–204 lives in `Kubanjaze/moto-diag-mobile/docs/ROADMAP.md`,
> which is the single source of truth for this track. See
> `ROADMAP_AUTHORITY.md` (root of both repos) for the ownership boundary.
>
> **Headline status (coarse, may lag the mobile ROADMAP):** Phases 185–195
> closed. 195 substrate-half + 195B feature-half both ✅. 195C reserved
> (F37 Track 2 meta-tooling). 196 next (iOS-blocked, needs device). Track I
> closes at Phase 204 / Gate 10.
>
> Track I per-phase doc files remain in this repo at
> `docs/phases/completed/` per the established convention — unchanged.

## Track J — Ship + Scale (Phases 205–210)

Polish, package, launch the core product (pre-expansion). Shifted from 193-198 due to Retrofit track insertion.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 205 | End-to-end integration testing | ✅ | All tracks working together (desktop + mobile) (was 193). **CLOSED 2026-09-07 — Gate 11, zero production code.** Targeted the DESKTOP half, since Gate 10 had just verified the mobile path on hardware. **Finding: the desktop and the phone are not the same product** — a desktop-only shop cannot log labour, print a report, or send a customer a link, and can only *assert* hours at completion. Five asymmetries pinned as tests that FAIL when each gap closes (F59, F65-F68). Also found that **Gate 8 did not gate what its row claims**: 3 `runner.invoke` calls against 54 direct repo calls, so Track G's ~96 shop subcommands were verified through the layer beneath the surface they were meant to prove. Gate 11 drives the real `cli` root. Contract snapshot (mobile `openapi.json` vs live spec) checked both directions for the first time — in sync. 16 tests; regression 4799/0; Gates 5/6/7/8/9 re-run as subprocesses. |
| 206 | Performance optimization | ✅ | Query speed, API response time, memory usage (was 194). **CLOSED 2026-09-07 — six defects, no tuning.** Measured first: every endpoint answered in 2-10ms against 6 work orders, so there was nothing to tune, and tuning anyway would have meant big percentages on sub-millisecond queries while real defects survived. Fixed: KB routes fetched all 660 rows to return 50 (now 50, with a shared WHERE feeding both page and COUNT so `total` stays honest) · report builder's per-fault-code DTC lookup (11 queries at 1 code and at 20) · a per-part query that had computed nothing since Phase 153 (F58, removed) · **14 `async def` handlers doing blocking sqlite**, which stalled the event loop for unrelated callers · `known_issues`, the only table outside the migration system, sorting all rows for every page (migration 049; EXPLAIN now an index walk) · conditional GET on the KB export (21,628 bytes → bodyless 304), with mobile declining to revalidate a wedged cache so Phase 198's self-heal survives. Harness asserts query SHAPE, never duration. 15 tests; regression 4814/0. |
| 207 | Security audit | ✅ | API keys, input validation, SQL injection prevention (was 195). **CLOSED 2026-09-07 — six defects, one of them a cross-tenant data leak.** The first pass reported one defect and an otherwise clean bill of health; a second sweep of the shop and billing families found that verdict was too generous. **Critical:** `GET /v1/shop/{shop_id}/customers` listed the table unfiltered, so any shop-tier member of any one shop got every customer in the database — name, email, phone, address, private notes — from one request on a legitimate paid account. The first pass missed it by checking that the route calls `require_shop_access` (it does) and not what the query underneath did. The column meant to stop it, `owner_user_id`, was never set by any caller, so every row held its DEFAULT of 1 while its docstring claimed it prevented exactly this; it was also the wrong key, since a shop has many members (migration 050 adds `customers.shop_id`, scoped in-query, NULL matching no shop). Also fixed: a forged webhook could grant a paid tier because `billing_provider` defaults to `"fake"` on an unauthenticated rate-limit-exempt route and nothing stopped that default reaching prod (`create_app` now refuses to start) · the 422 handler logged rejected input, which under pydantic v2 is the whole request body on a model-level failure · both upload routes buffered the entire body before consulting caps that are per-session *aggregates* and cannot bound one request (photos had no byte ceiling at all) · push deregistration deleted by token alone · oversized ids returned 500 rather than 422. Verified sound and recorded so nobody "improves" it: 192-bit keys looked up BY HASH, parameterised SQL throughout, id-derived file paths, complete HTML escaping on the share renderer. Every fix re-run against the pre-fix code to confirm it fails without it. Filed F71-F75 (proxy-collapsed rate limiting is a launch item). 40 tests; regression 4854/0; F9 lint clean. The doc states plainly that a static review cannot establish absence, and that no dependency CVE scan was run. |
| 208 | Documentation + user guide | ✅ | README, usage guide, mechanic quickstart, app store listing (was 196). **CLOSED 2026-09-07 — the docs were rewritten and five code defects fell out of writing them.** Step 0 found reshape territory: both READMEs described a Windows-hosted CLI that had not existed for 200 phases, and the backend quick start opened with `.venv/Scripts/activate`, which cannot run on the only host this project has. It documented 3 of **255** commands, listed 8 of ~20 packages, never mentioned the iOS app, work orders, invoicing or share links, and spent over half its length on pre-commit-hook minutiae. Ships both READMEs rewritten, `docs/guide/{quickstart,shop-workflow,api}.md`, `docs/contributing.md`, and the mobile `docs/app-store-listing.md` (metadata, privacy answers, review notes, blockers). **Defects found by running what was about to be described:** `motodiag --version` reported **0.1.0 against a 0.6.0 package** — two literals neither tracking `pyproject.toml`, with `/v1/version` and the OpenAPI field equally stale, and a Phase 01 pin that had been *agreeing with the bug* · **the CLI wrote customers the API could never read** (Phase 207 scoped the API on `customers.shop_id`; `shop customer add` kept inserting NULL, which the API serves to no shop — every terminal-added customer invisible in the app, silently, both sides passing their own tests) · `list_shops()` defaults to `owner_user_id=1`, so the fix's helper saw no shops for any operator who was not user 1 · the iOS Info.plist declared a **location permission for a feature that does not exist** · the App Store listing's own stated character counts were all three wrong. Two drift guards, each proven to fail before being trusted by injecting a fabricated command and route into a real doc: `test_phase208_docs.py` (18) reads the live Click registry and live OpenAPI schema, `test_phase208_cli_api_parity.py` (7) holds the CLI↔API seam. Also documented rather than "fixed": invoices bill only `received`/`installed` parts, so a part left at `open` silently misses the invoice while the total looks right. iOS `RCT_NEW_ARCH_ENABLED=0` removed and verified inert via `pod install` (xcconfigs still carry `-DRCT_NEW_ARCH_ENABLED=1`); the Android twin annotated, not flipped, because no Android build has been run since iOS became the shipped target. Honest gaps stated in the doc: the two paid AI commands and the share-link curl calls were not executed, and no full Xcode build was re-run. Regression 4879/0; F9 lint clean; mobile `tsc` clean. **Key finding: two of the five defects were invisible to 4,854 passing tests** because every test exercised one side of a seam — a docs phase near launch is the first time anyone uses the product the way a stranger would. |
| 209 | Packaging + distribution | ✅ | pip install, standalone binary, Docker, App Store, Play Store (was 197). **CLOSED 2026-09-07 — `pip install motodiag` produced a CLI that could not start.** Step 0 built a wheel, installed it into a clean venv and ran `motodiag --version`: `ModuleNotFoundError: No module named 'fastapi'`. Not one broken command — all of them. **Five defects, none visible to 4,879 tests that all run against the source tree with every extra installed:** `cli.main → cli.apikey → auth.__init__ → auth.deps → fastapi` made the whole product depend on the optional `api` extra (deferred behind a PEP 562 `__getattr__`) · **the seed knowledge base was never in the package at all** — `db init` loads ~660 known issues from the repo's `data/`, which no wheel has contained, so an installed copy silently loaded nothing and still printed "Database ready"; the knowledge base IS the product, so 69 files moved into `src/motodiag/knowledge/seed/` and `db init` now fails loudly instead · `motodiag serve` could not start on an `[api]` install because `photo_pipeline` imported Pillow (the `vision` extra) at module level and `api/errors.py` imports it at load time (now resolved at call time behind `PillowUnavailableError`, which names the extra) · `PROJECT_ROOT` resolved to `<venv>/lib/python3.14` on an install, so a shop's database would have lived inside their virtualenv (now a per-platform user data dir; source checkouts deliberately unchanged) · `package-data` omitted `hardware.compat_data`, so a test now walks every non-`.py` file under `src/` and asserts it reaches the wheel (16 data files → 88). 23 packaging tests build a wheel, install it into a throwaway venv with **no extras** and run the product; proven to fail by restoring the eager import. Ships `Dockerfile`, `.dockerignore` and `docker-compose.yml` — **written but never built**, Docker is not installed on this machine; each file says so and it is filed as **F76**, with everything the image does verified outside a container. Also `docs/guide/install.md` and `docs/releasing.md`, both added to the Phase 208 drift guard. **Standalone binary answered as `pipx`, not PyInstaller**, reasoning written into the install guide; **Play Store answered as unscoped** — Android has not been built since iOS became the shipped target. Filed F77 (dependency floors: the dev venv pins fastapi 0.136 while a fresh install resolves 0.141, whose `include_router` change already broke one of my assertions). Regression 4902/0; F9 lint clean. **Key finding: the suite proved the code worked and said nothing about the product** — every test ran in an environment no user will ever have. |
| 210 | Launch readiness + full integration | 🔲 | Everything together — the complete MotoDiag platform (was 198) |

---

# Expansion Tracks (Phases 211–352)

The following tracks extend MotoDiag beyond the original 210-phase core product into a complete commercial platform. Planning-only; these phases unlock once the core platform has launched and generated customer feedback to prioritize expansion.

## Track K — European Brand Coverage (Phases 211–240)

European motorcycle brands — BMW, Ducati, KTM, Triumph, Aprilia, MV Agusta. Each brand gets dedicated phases for its model lines, electrical systems, and cross-model common issues, matching the pattern established in Tracks B.

### BMW (Phases 211–215)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 211 | BMW R-series boxer twin (1969+) | ✅ | R nineT, R1200GS/R1250GS, R1200/1250RT, oilhead/hexhead/wethead generations. **CLOSED 2026-09-07 — Track K opens with provenance, not content.** Thirty phases of repair knowledge authored from training data were about to land in a table with no record of where any of its 660 existing claims came from. **Migration 051** adds `known_issues.source`, CHECK-constrained (`unverified · model-generated · forum · service-manual · mechanic-verified`); every existing row reads `unverified`, a true statement about its origin. `kb show` prints the source and warns under the fix procedure for anything short of a manual or a mechanic — and stays silent for `service-manual`, which is what makes the warning mean something. The API carries it as a `Literal` after the F9 lint flagged a `str` against the CHECK *before* regeneration; mobile codegen produced a typed union. **Twelve boxer entries** airhead → wethead (final drive bearing, oilhead surging, dry-clutch contamination, input splines, Hall sensor, Integral ABS, fuel strip, alternator belt, water-pump seal, diode board, rotor, Paralever), each tagged `model-generated` in the JSON and admitting it in its own text; entries needing an invented figure were dropped. The Phase 208 count guard fired on 660 → 672 and four docs were corrected. Step 0 also caught the docs claiming 6,600 known issues against a real 660 — fixed on master with a guard before this branch was cut. Schema 50 → 51. 26 tests; regression 4930/0; F9 clean. **Key finding: the honest label cost one migration, one parameter and a render function, and is what lets the remaining 29 phases proceed without pretending.** |
| 212 | BMW GS adventure line | ✅ | F650GS/F800GS/F750GS/F850GS/F900GS, ADV accessories, **chain final drive** (row corrected — see below). **CLOSED 2026-09-07 — the roadmap premise was wrong and two independent checkers caught it.** This row read "paralever final drive"; Paralever is the boxer/K-series shaft swingarm and no F-series GS has ever had one — every F650GS, F700GS, F750GS, F800GS, F850GS and F900GS is chain-driven. One agent was told to confirm the row, another to refute the opposite claim; they agreed without seeing each other. Consequence: almost no Phase 211 boxer topic ports (no final-drive housing, dry clutch, Paralever, diode board on these bikes). Content drafted by six independent lenses, then **three refuters per candidate** — attribution / invented figures / safety, each defaulting to refuted when uncertain: **17 drafted → 14 survived → 12 kept**, 0 undecided. The adversarial pass paid for itself: a fuel-pump-controller entry for the 798cc twins carried correct model strings and years while describing a potted FPC module that exists on Hexhead R1200/K1200 and not on an F — **the drafter held an explicit "do not port R-series hardware" instruction and did it anyway** — and safety independently killed it for a heat gun beside an under-seat fuel tank. Two more dropped for undeclared specification-grade figures and a coolant drain with no cold-engine warning. Symptoms were normalised from sentences to short phrases because `find_issues_by_symptom` is a SQL LIKE match. Tests assert ten boxer-only components appear nowhere in the F file, with a counter-assertion that "paralever" still appears in the R file so the check cannot pass vacuously; the 211 fuel-strip entry was widened to the F800 family, not duplicated. 672 → 684 known issues. 34 tests; regression 4964/0; F9 clean. **Key finding: one drafting pass would have shipped a confidently wrong entry — what killed it was a second agent whose only job was to disbelieve the first.** |
| 213 | BMW S1000RR / S1000R / S1000XR | ✅ | Inline-4 superbike line, electronics (**shift assist pro deliberately NOT written — already covered**, see below). **CLOSED 2026-09-07 — six entries, and the twelve rejections are the finding.** Step 0 found the row naming a topic Phase 212 already shipped (Gear Shift Assist Pro for the F850GS, plus CBR1000RR and cross-platform drivetrain quickshifter entries) and generic electronics saturated across ~48 files including four litre-class peers. 18 candidates drafted across six lenses, three refuters each: **6 survived, 12 dropped, every drop fatal on attribution** — no other lens cast a fatal verdict. The rejections were not wrong, they were unspecific: a K46 second-gear entry killed as "the archetypal sport-bike transmission complaint — equally true of a ZX-10R, R1 or GSX-R1000", and **four separate DDC semi-active-suspension entries** killed with attribution mechanics *clean* but "strip the BMW badge and the entry is the standard semi-active-suspension failure story". Two more rested on NHTSA campaign numbers unconfirmable for this model, one also porting a cracked fuel-pump-flange story from the boxer family onto the K67 sportbike tank. **Two whole lenses produced no survivor** — RR 2015-2018 and S1000R are absent rather than invented, and the synthesizer said so instead of padding. The symptom-format rule moved into the JSON schema with its reason stated and produced compliant output first time (mean 28 chars, zero manual edits, vs ~60 hand-rewritten in 212). 61 agents, 0 errors, 0 undecided. 684 → 690. 36 tests; regression 5000/0; F9 clean. **Key finding: in a saturated corpus, "specific enough to be worth adding" is a far harder bar than "true" — all twelve rejects were plausibly correct, just correct about motorcycles in general.** |
| 214 | BMW K-series touring | ✅ | K1200RS, K1200GT, K1300S, K1600GT/GTL inline-6, Duolever front (note: the K1200RS is **Telelever**; Duolever arrives with the transverse K1200S in 2005). **CLOSED 2026-09-07 — the attribution guardrail inverted, and held.** Phases 212-213 policed misattribution (Paralever on a chain-driven bike is an error); the K-series genuinely **is** shaft-driven with Paralever and genuinely did carry servo Integral ABS, so a refuter briefed as before would have killed every valid entry. The attribution prompt was rewritten to state outright that those references are correct on a K, moving scepticism to **generation errors** and **duplication of the R-series file**. Result: **20 drafted → 14 survived → 11 kept, 67 agents, 0 errors, 0 undecided, and not one entry refuted for naming shared hardware.** All six fatal drops were attribution-for-genericness — an in-tank fuel entry rejected because "a Gold Wing, an FJR1300 and a Concours all have it"; a gearbox entry because its procedure "reads identically on any wet-clutch bike" and `cross_platform_drivetrain` covers it three times. A new `widen_existing_instead` refuter field (the Phase 212 fuel-strip precedent made first-class) produced **40 opinions and zero actions** — every BMW-facing one said do NOT widen, including "Paralever pivot bearing wear must NOT be widened to cover [Duolever] — Paralever is the rear suspension". **Duolever and Telelever are covered for the first time**; nothing in the corpus previously described either front end, and the file states neither has stanchions to pit or fork seals to weep. 690 → 701. 32 tests; regression 5032/0; F9 clean. **Key finding: a guardrail that is right for one platform is a liability on the next — rules from one phase must be re-derived against the next phase's hardware, not inherited.** |
| 215 | BMW electrical + FI dealer mode | ✅ | ZFE (Central Electronics), GS-911 diagnostic tool integration, BMW-specific DTC format. **CLOSED 2026-09-07 — the BMW block closes without a fabricated code.** Three surfaces: `dtc_codes/bmw.json` (10 codes, seed 55 → 65), `known_issues_bmw_electrical.json` (3 entries, 701 → 704), GS-911 in the adapter catalog (24 → 25) with 5 BMW compat rows. **Governing constraint: a wrong fault code is worse than a missing one** — every shipped code is a standard P0xxx with its real OBD-II meaning and BMW content only in causes/fix; no P1xxx invented; BMW proprietary codes documented as a *format* (an ELM327 reading "no codes" is not "no fault") rather than invented as data. **The fabrication lens found nothing; the shadowing lens found the real hazard:** make-specific rows resolve before generic, so a mediocre BMW row *hides* a better generic one — P0135 dropped as "strictly weaker than the generic row it would shadow". A CAN-bus entry dropped for triple duplication (generic playbook + `harley_electrical` CAN entry + diluted Phase 212 ZFE trio), with a scope error caught: its 2004+ span swept in the pre-CAN F650GS single and longitudinal K1200RS. **Acted on the GS-911 draft's own fifteen declared uncertainties**: `supports_mode22` flipped true → false ("arguably wrong" — proprietary service requests, not Mode 22; the flag feeds the recommender) and the self-described "most speculative … NOT verified" R1300GS row dropped. Shadowing verified live. 44 BMW entries across five files. 24 tests; regression 5056/0; F9 clean. **Key finding: precedence turns "adds nothing" into "actively subtracts" — a property of any make→generic lookup.** |

### Ducati (Phases 216–220)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 216 | Ducati Monster / Streetfighter (1993+) | ✅ | Air-cooled and liquid-cooled generations; **trellis frame 1993–2020 only** (the 2021+ Monster 937 uses a cast-aluminium Front Frame); **Desmodue / Desmoquattro / Testastretta** — the row's "Testastretta evolution" covers neither the air-cooled 2V line nor the S4/early S4R, which are Desmoquattro. **CLOSED 2026-09-07.** Both premise checkers found the row wrong independently. **The Streetfighter V4/V2 are Panigale-derived and move to row 217.** The guardrail inverted against the BMW block: three BMW entries cover dry clutches, so the rule was **split rather than relaxed** — attribution permits the component (dry clutch, desmo, belts, trellis are legitimately Ducati), duplication polices the mechanism (no bellhousing, no spline greasing, no gearbox split). 24 drafted → 15 survived → 12 kept, 0 undecided, **0 refuted for naming Ducati hardware**. Six drops mostly for genericness (a carb fuel entry that "reads identically on a Bandit 600, GS500, XJ600 or Hornet"); the dry-vs-wet year trap fired as predicted on an S2R clutch claim. Deferrals honoured: valve service → 219, ECU/DDS/codes → 220, cam belts in scope here. 704 → 716. 24 tests; regression 5080/0; F9 clean. **Key finding: a shared component name is not shared hardware — split the guardrail rather than relaxing it.** |
| 217 | Ducati Panigale superbike line | ✅ | 899/959/1199/1299/V4, Superquadro, V4 engine, Ohlins electronic suspension, **+ Streetfighter V4/V2** (moved here by the Phase 216 audit). **CLOSED 2026-09-07 — the cam-drive guardrail inverted inside its own block.** Phase 216 asserted "Ducatis are belt driven"; verified by search, the **Superquadro drives its cams by a chain to a gear train** between the camshafts — adopted to remove belt service — and the V4 Desmosedici Stradale is chain driven too. 216's rule was scoped to its own file so nothing broke, but the comment was a false generalisation and was corrected in place. This phase asserts the opposite (no cam belt on a Panigale) with a counter-assertion that the Monster file still owns belts. **Hand-drafted in lean mode, no workflow**; the refuters' checks reproduced as a mechanical validator, with the absence of an independent reader stated in the plan's Risks. 10 entries, each naming Panigale hardware (chain-and-gear drive, monocoque with no frame to straighten, V4 rear-bank deactivation misread as a misfire, counter-rotating crank misread as bad geometry, Smart EC settings vs a failed strut, side radiators, SF V4 booked in as a naked bike, wet slipper clutch with no dry rattle, 899/959 ≠ small 1199). **My validator was wrong a third consecutive phase** — it flagged the symptom "quoted for cam belts on a panigale" as a belt claim when it records the mistaken quote a shop gives; claim checks now exclude `symptoms`, which are reports and may contain mistaken beliefs. Deferrals held: valve service → 219, ECU/DDS/codes → 220. 716 → 726. 20 tests; regression 5100/0; F9 clean. **Key finding: a guardrail can be wrong against the very next phase in the same block, by the same manufacturer — and the checker needs as much scepticism as the content.** |
| 218 | Ducati Multistrada | ✅ | 1200/1260/V4, adventure-touring, skyhook suspension, Ducati Safety Pack. **CLOSED 2026-09-07 — the first conditional guardrail, and a gate that decayed by addition.** Step 0 verified the **Multistrada V4's Granturismo engine has no desmodromic valves** (conventional spring return, ~60,000 km interval vs ~15,000 mi for the desmo V4) — so **row 219 does not cover this bike** — and that the **cam drive splits inside one model name** (1200/1260/V2 belt, V4 chain), so neither 216's nor 217's rule holds. Tests resolve belt-vs-chain **per entry** from its model string, with a counter-assertion that both sibling files keep their own rules. Skyhook cleared a high bar: five electronic-suspension entries already exist, all "the unit failed", so this one covers the **load selection** on an electronically preloaded ADV bike instead. **A latent Gate 2 assertion tripped** (≥90% of issues must carry a "Forum tip"; count hit 660/734 = 89.9%) — adding tips to model-generated entries would have **fabricated provenance**, so the gate was scoped to the forum-sourced population (100%, 660/660) and given an inverse assertion that model-generated entries must NOT claim forum tips. 726 → 734. 22 tests; regression 5123/0; F9 clean. **Key finding: guardrails can be conditional, and gates can decay by addition rather than by fault.** |
| 219 | Ducati desmodromic valve service | ✅ | Desmo service intervals (generation-dependent), **opening and closing rockers, opening and closing shims, retaining collets** — row corrected: it previously read "shim-over-bucket", which is a **spring-valve** arrangement; desmodromic gear has **no buckets**. Cost-intensive. Scope: Desmodue, Desmoquattro, Testastretta, Superquadro, Desmosedici Stradale — **NOT the Multistrada V4's Granturismo**, which uses spring valve return (Phase 218 audit). **CLOSED 2026-09-07.** Fourth roadmap correction in the Ducati block. The genericness test runs **backwards** here: 22 spring-valve clearance entries already exist across four Japanese makes, so every entry must NAME desmodromic hardware rather than merely avoid a shared topic. Six procedure-shaped entries — the mechanism and why spring-valve assumptions fail; both clearances and the closing one with no analogue; collet and half-ring seating (**critical** — a released collet drops a valve); shim availability rather than price stranding a bike mid-service; intervals varying by generation; 2V vs 4V being different jobs. **Intervals deferred by test** — a regex fails any entry stating a mileage figure as fact. 734 → 740. 22 tests; regression 5145/0; F9 clean. **Key finding: a validator over prose must distinguish assertion from reference, and negation can arrive from either side.** |
| 220 | Ducati electrical + FI | ✅ | Magneti Marelli ECU, DDA as a **data logger rather than a fault reader**, Ducati-specific fault numbering, DDS diagnostic tool. **CLOSED 2026-09-07 — closes the Ducati block.** **Step 0 removed a deliverable rather than adding one**: unlike Phase 215, where the GS-911 was genuinely absent, the Ducati adapter surface was already covered — **8 compat rows across 8 adapters** including the OEM `ducati-dds-readiness-tool`, with the counterfeit-ELM327 CAN-FD failure already recorded. Nothing added to the catalog, and a test asserts its Ducati rows are **unchanged** so the finding outlives the phase. Two surfaces shipped, not three. **Six DTC rows, every one shadowing a generic row deliberately** — `get_dtcs` resolves make-specific first, so a row that restates the generic one hides it (the 215 finding that dropped P0135); each is asserted to differ in **both** causes and fix, with a live check that a Honda still resolves to generic. The content that justifies the file is **L-twin cylinder identity** — cylinder 1 horizontal/front, cylinder 2 vertical/rear — which no generic misfire row can say and which sends a mechanic to the wrong cylinder if read as an inline engine. Because both misfire rows restate the mapping, **a regex requires every statement of it to agree**; one reversed sentence is worse than silence. Five knowledge entries: proprietary codes invisible to generic scanners, TPS reset after throttle-body work, the DDA logger/reader confusion, immobiliser no-start, aftermarket maps distorting diagnosis. No proprietary number transcribed, no P1xxx invented. Five Ducati files coexist at 41 entries, no title collision. 740 → 745. 23 tests; regression 5168/0; F9 clean. **Key finding: a guard should outlive the deliverable it was written for** — this phase's most useful assertion covers something it deliberately did not build. |

### KTM (Phases 221–225)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 221 | KTM 1290 Super Duke / Super Adventure | ✅ | LC8 V-twin; **Super Duke R and GT, Super Adventure base / S / R / T** — row corrected: it previously read "R/GT variants" while naming both model lines, but **GT is a Super Duke designation and there has never been a Super Adventure GT**. Milder than the four Ducati row corrections (those named mechanisms the engines do not have; this is a conflation), but variant claims are parts claims — a Super Adventure S runs a 19-inch front and the R a 21-inch. Cornering ABS, MSC. **CLOSED 2026-09-07 — opens the KTM block.** KTM had **zero** entries beforehand, but the *topic* is the corpus's most-covered: **19 rider-electronics entries** across nine makes, the Harley RevMax "cornering ABS / TC false intervention" entry already being the generic form. So the genericness test runs **backwards** (the Phase 219 shape): every entry must **name** 1290 hardware, measured as **distinct** terms so repetition cannot satisfy it, with a counter-assertion that the RevMax entry **scores zero** on the same check. **11 candidates drafted, 6 shipped** — four cut for genericness (semi-active suspension already has four entries including the Panigale Öhlins one) and the tyre-circumference entry cut outright because its mechanism is universal and a make file is the wrong home. Two more were rewritten after the first draft scored one distinct term from the title alone. Anchored on **MSC being a Bosch supplier system rather than a KTM in-house one** — MSC appeared **zero times** in 745 entries, and the contrast with KTRC/HSTC/Motion Track is what the file exists for. **The adapter gap is left for its owner, not filled**: four KTM rows, all partial or read-only, no full-access option — a real gap, but **Phase 225 owns KTM tooling**, so the row count is guarded and 225 annotated. 745 → 751. 29 tests; regression 5197/0; F9 clean. **Key finding: a validator over prose must exempt reported speech, not just negation** — "if a customer says Super Adventure GT" quotes someone's error rather than asserting the bike exists. |
| 222 | KTM Duke naked line (125/390/**690**/790/890) | ✅ | Single + parallel twin, beginner (125/390) through advanced (790/890) — row corrected: the **690 Duke** was in neither this row's list nor row 223's, which names the 690 *Enduro R*. By body style a 690 Duke is a Duke naked and belongs here; the adapter catalog already carries a `690%` row labelled "690 Duke / Enduro", so the model was known to the system while absent from the content roadmap. **CLOSED 2026-09-07.** **Second roadmap finding: the LC8c is a parallel twin, not a V-twin** — row 224 reserves cam chain tensioner, electric start and valve clearance for the "LC8 V-twin", but the 790/890 use the LC8c, so as written 224 leaves the Duke twins' engine unowned; 224 is annotated and all three topics avoided here either way. **The specificity bar was recalibrated, not copied**: Phase 221's three-distinct-terms rule fits a vocabulary with MSC, Bosch and Super Adventure in it, and the Duke line has none of those, so the bar changed shape — every entry must name a **model or engine designation** (LC8c, LC4, 790 Duke, RC 390 …) in its **title** as well as its body, which brand-name padding cannot satisfy and which the corpus's existing parallel-twin entry scores zero against. **5 entries.** The first draft was five identification and sourcing entries with nothing diagnostic in it — one was cut for restating the others plus Phase 221's variant entry, and an **uneven-firing-beat** entry added as the file's diagnostic spine: the LC8c's offset crankpin makes a lope that reads as a misfire, and the tell is that designed character is regular in its irregularity while a misfire varies with load and temperature. The **manufacturing-origin** entry (125/390 built by Bajaj in India) is written as **parts sourcing and spec variation and explicitly not a quality claim**, guarded by a test that fails on quality vocabulary and requires the entry to disclaim the reading outright. **Phase 221's own guard was fixed here**: `test_this_is_the_first_ktm_file` asserted no sibling KTM file may exist — a true fact encoded so it could not survive the block growing. 751 → 756. 32 tests; regression 5229/0; F9 clean. **Key finding: a validator over prose must exempt comparison, not just negation and reported speech** — "the throttle feel of a V-twin" says what an engine resembles, not what it is. |
| 223 | KTM enduro (EXC / **EXC-F** / 690 Enduro R) | ✅ | Off-road single-cylinder competition bikes, hard enduro, **450/500 EXC-F**, 690 Enduro R — row corrected: it read "450/500 EXC", but KTM uses **EXC** for the two-strokes (250, 300) and **EXC-F** for the four-strokes (250, 350, 450, 500), so "450 EXC" names a machine that does not exist and the shorthand collapses the exact distinction the naming exists to make. Third naming correction in the KTM block, after the Super Adventure GT (221) and the LC8c (222). **The "adventure" half of the old title had no content and no home**: the **390 / 790 / 890 Adventure** appear in no row — 221 took the 1290 Super Adventure, 222 the Duke nakeds, 224/225 are engine and electrical rows — and putting a parallel-twin tourer in with competition two-strokes would be incoherent, so **this phase did not take them and the block as numbered (221–225) has no free row**. ⚠️ **Open: the mid-size Adventure line still needs a home.** **CLOSED 2026-09-07.** **First phase in the KTM block whose gaps were large rather than narrow**: across 756 entries, `TPI`, `transfer port`, `engine hour`, `hour meter`, `competition` and `piston replacement` each returned **zero**, and the only genuine two-stroke service content was three RD350/400 entries — 1970s road bikes. But 40 dual-sport entries exist across four makes, so the backwards genericness test still applied: every entry names an enduro designation in **title and body**, and every entry in two dual-sport files scores zero against the same check. **6 entries** built on what a trail bike cannot describe — a **competition** machine: TPI two-strokes running an oil pump rather than premix (rated **critical**; both directions of the error destroy engines), EXC versus EXC-F, service counted in **engine hours** rather than miles, a top end being **maintenance rather than failure** and the harm being in how it is quoted, the exhaust power valve as a service item, and the 690 Enduro R sitting between competition and road schedules. **Interval figures deferred by test** as in 219, and more sharply — a missed top end is an engine, not a wrong quote; a second assertion requires any entry mentioning intervals to point at the manual, which caught a real gap in the naming entry. 756 → 762. 32 tests; regression 5261/0; F9 clean. **Key finding: a validator over prose must exempt quotation** — the check flagged `a request for a top end on a "450 EXC"`, a sentence whose whole job is naming the error and which already marked it as a citation. |
| 224 | KTM engine families (LC8 / LC8c / LC4) | ✅ | **Rescoped from "LC8 V-twin common issues" — decision recorded.** Phase 222 found the row said LC8 *V-twin* while the 790/890 use the LC8c parallel twin, leaving that engine unowned. Widened to the KTM engine families for three reasons: the three reserved topics — cam chain tensioner, electric start, valve clearance — were **already written 37 / 23 / 26 times** across the corpus, with the procedures in generic form (starter work in `cross_platform_starting`, CCT and valve clearance in the Honda and Yamaha cross-model files with `model: "All"`), so a V-twin-only phase had almost nothing to write; the corpus's own precedent is a **per-make cross-engine file** (`honda_cross_model`, `yamaha_crossmodel`), which this row structurally is; and the block as numbered has no free row for an LC8c one. **CLOSED 2026-09-07.** **None of the three reserved topics is written here**, and a test asserts no KTM-specific failure claim about them exists — Honda's cross-model entry can call the inline-four tensioner a documented weakness because it is, and I have no equivalent confident LC8 knowledge; inventing it would be fabrication in the costume of specificity. **4 entries**, each carrying what only a KTM cross-engine file can: three architectures under two prefixes (LC8 V-twin / LC8c parallel twin / LC4 single); **generation span within the LC8** — 950 through 1290 over two decades, the error that survives getting the architecture right; cylinder identification on a 75-degree vee, **without asserting a numbering I cannot support** (the entry sends the reader to the manual, and a test forbids "cylinder 1"); and routing for the three universal topics — diagnosed like any engine's, with KTM changing only which family and which schedule. **The routing entry's first draft overstated the corpus** — it called all three procedures cross-platform when only the starter ones are; corrected, and a test now requires the entry to distinguish cross-model from cross-platform. Duplication checked by **symptom overlap across all four KTM files** — the same mechanic query must not resolve to two files. Coexistence asserted as an invariant, not a constant. 762 → 766. 25 tests; regression 5286/0; F9 clean. **Key finding: a row can be structurally right while its stated content is already written — and not knowing a make-specific failure pattern is a result to record, not a gap to fill.** |
| 225 | KTM electrical + engine management (Keihin / Bosch / Continental / Vitesco) | ✅ | **Row corrected three ways, every correction from researched and refuted sources, not memory.** (1) "Keihin FI" is right for the LC8 V-twins from the 990, the LC4 690 and the EXC-F four-strokes — and wrong for the rest: the LC8c 790/890/990 Duke and every Bajaj-built 125–390 are **Bosch**, the 2018–23 TPI two-strokes **Continental** (Synerject), the 2024-on TBI two-strokes **Vitesco**. Fourth naming correction in the block. (2) "Tuneboy" lists a dozen older KTMs but as a tune-editing kit; **TuneECU** is the diagnostic tool, and only for Keihin-era, pre-Euro 5 bikes — its own list says unlisted bikes are incompatible. (3) "PowerParts cross-platform" is not a KTM term; it means shared article numbers across KTM, Husqvarna and GasGas. **CLOSED 2026-09-07 — closes the KTM block.** **First electrical phase whose external facts were researched and adversarially refuted before writing**: five questions to web-researching agents, each finding refuted by two lenses, 15/15 agents returned, 0 died. Four findings survived; the tooling finding was refuted on function detail only and the refuters supplied the corrected per-model list. **The cylinder mapping was settled from KTM's own document**: the 950/990 repair manual's error table (p. 162) lists injector, coil and lambda codes for the **rear** cylinder first — P0201/P0351/P0130 rear, P0202/P0352/P0150 front — confirmed independently by the fifth refuter, who downloaded the same PDF. For the LC8c the side is **unknown and the rows say so**. **A refuter caught an overreach in my draft**: I had the TuneECU reset sequence as the fix after throttle-body work; no source ties it to that — it is documented after a map load, on the 990/RC8 only, and the CAN-bus 1050–1290 get **no adjustments at all** through TuneECU. Corrected before the test was written. **Three surfaces shipped**: `dtc_codes/ktm.json` (8 rows, 6 shadowing and every one differing from generic, blink codes asserted against the manual's set, P0560 not P0562 because that is the code KTM sets, the Dynojet orange-to-front harness trap named); `known_issues_ktm_electrical.json` (7 entries: the four-supplier map, the tool landscape, blink codes and the Warnings menu, KTM's own P1590 side-stand definition against a generic scanner's wrong label, immobiliser no-start with battery first because a lock set needs dealer pairing, the narrow documented reset sequence, PowerParts maps and warranty); and **the adapter gap 221 left is filled** — `tuneecu-ktm-android` with 14 compat rows, every one citing TuneECU's own list, 3 full / 5 partial / 6 **incompatible** so a shop checking a 790 is told no rather than finding nothing, the 990% row year-bounded so the Bosch 990 Duke cannot fall into it, and the price labelled as an EUR conversion rather than a made-up USD figure. 221's guard inverted deliberately: the four original rows survive unchanged and the gap is asserted closed. 766 → 773. 45 tests; regression 5331/0; F9 clean. **Key finding: research changes what a phase can honestly say, in both directions** — it put a cylinder mapping on the table that 224 had rightly refused to guess, and it took a throttle-body claim off the table that I would otherwise have shipped. |
| 225B | KTM mid-size Adventure (390 / 790 / 890 Adventure) | ✅ | **Closes the KTM block’s open item, carried since 223/225.** The gap was not a tracking omission with content behind it: `390/790/890 Adventure` appeared **zero times in 855 entries** while `Super Adventure` appeared **25 times** and was all Phase 221’s 1290. **CLOSED 2026-09-08.** **A model line was invisible because of one word** — a corpus or web search for "Adventure" returns the 1290 confidently and in volume, so every search that would have revealed the gap returned plausible, wrong results. That is an entry in its own right, not just a fix. **Axis chosen against the existing corpus**: 222 owns the Duke naked models, 224 the LC8c family, 225 engine management — so engine-internal content would shadow three phases. What this phase owns is **everything around the engine**. **10 entries.** The anchor is the **split saddle tank**: the catalogue shows **one** pump (left lobe) and **one** level sender (**right** lobe) joined by a low-flow balance hose and two screw-type shut-off cocks, so the gauge reports a lobe the pump does not draw from and the bike **starves with fuel showing** — two documented causes with different fixes (a cock not fully open, including machines delivered backed off; or tank vacuum from a blocked vent, field-tested by opening the filler cap and listening). Also from KTM’s own manual: the gauge **reading full for the first half tank is designed behaviour** and a **flashing** display is the actual sender fault — the distinction that stops a sender being condemned; and the **ABS module sits under the fuel tank**, so every ABS or brake-line job is a tank-off job and generic ABS labour times are short. **A refutation turned "could not be determined" into a sourced fact**: the research graded 890 assembly Low/unresolved, and a refuter opened the very KTM page it had cited as unhelpful to find all three 890 Adventures listed under **Mattighofen**, plus KTM’s own statement that the **CFMoto partnership covers the 790 Duke and 790 Adventure** — two machines side by side in the showroom, different plants. **The opposite correction landed on the same topic**: a cited partner press release contains **zero** occurrences of the plant, the models or any production word, so the 2025 390 platform’s build location is recorded as **not established**, with a test that keeps it that way. **The tyre campaign is narrower than it reads and the difference is money** — the manufacturer’s own notification restricts it to the **front** tyre and only to machines still on originally fitted tyres. **A regulator declared unqueryable answered first try**: Transport Canada returned both 790 brake campaigns, the third time in this block that regulator *access* rather than data was the weak link (after 228’s index defect and 231’s misspelled make), so the entry says plainly that a clean result in one database is not a clean result. No campaign numbers, per the 231 decision. **The subframe entry exists to refuse a reputation**: KTM subframe cracking belongs to the **950/990**, and nothing supports it here — replaced by two things the catalogue and manual do say, that the subframe is a **bolt-on two-piece assembly** (left, right and rear reinforcement listed separately, so one-sided damage is not a frame job) and that the **luggage rack plate limit is 5 kg**, which most top-box installs exceed before anything goes in the box. Also: **tubeless spoked wheels seal on a rim band KTM ages out at five years regardless of wear**, invisible with a tyre fitted and the reason a "slow puncture" recurs; and the **Adventure and its Duke sibling do not share a service schedule**, including a dusty-conditions qualifier on the Adventure air-filter row that the Duke’s lacks — **interval figures deliberately not printed**, since 238 owns them. Three of my own test selectors were wrong in one family: `[Dd]rawn from` missed "Drawn **verbatim** from", a mileage guard caught an owner’s report of *when a part failed* rather than an interval, and a searchability matcher used exact substrings so "location" missed "located" — all fixed at the right level rather than loosened. BSD `sed` lacks `\b`, so the first doc-count edit silently did nothing and the Phase 208 guard caught it. 855 → 865. 31 tests; regression **5791 passed / 0 failed**; F9 clean. **Key finding: a model line can be invisible because of its name.** Not an oversight of judgement — the corpus said "Adventure" 25 times and meant a different motorcycle every time. |

**KTM block closed at Phase 225 (2026-09-07).** 33 entries across five files, 8 KTM DTC rows, 1 adapter with 14 compat rows. Four roadmap naming corrections (Super Adventure GT, LC8c, EXC-F, Keihin), one rescope (224 → engine families). ⚠️ **Still open: the 390 / 790 / 890 Adventure line has no row** — flagged at 223, not absorbed by 224 or 225; needs a deliberate home.

### Triumph (Phases 226–230)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 226 | Triumph Bonneville / T100 / Speed Twin | ✅ | Parallel twin modern classics, **790**/865/900/1200cc, classic styling modern internals — row corrected: the Hinckley Bonneville launched at **790cc** in 2001 and the base Bonneville and America stayed 790 into MY2006, a generation that appeared in neither this row's list nor row 229's. **CLOSED 2026-09-08 — opens the Triumph block.** **First phase whose entries are not `model-generated`**: ten are `service-manual`, drawn from Triumph owner's handbooks, the T120 service manual and recall records filed with national road-safety regulators; one is `forum` and says so in its own prose. **That required re-scoping Gate 2**, whose forum-tip rule excluded by denylist (`!= "model-generated"`) and so silently assumed the corpus would only ever hold two populations — it now names the population it measures (`unverified` + `forum`), and its sibling assertion was widened the same way. Same encoding bug as the 221 sibling-file guard and the 222 hardcoded count. **12/12 research agents returned, 0 died; two findings survived both refuters, two were partially refuted and the refutations changed the content.** The 790-vs-865 engine-number breakpoint is the **T100's changeover only** — the base Bonneville and America stayed 790 with engine numbers well beyond it and their breakpoint is not established, so the entry scopes the rule and admits the gap instead of publishing the number; a forum claim that Triumph advises no parts commonality between generations was **contradicted in its own thread**, so the entry keeps the documented rim that would not lace and labels the wider claim disputed; and a refuter found a **third carburetted-era recall the research had missed** — a starter cable chafing the oil cooler return pipe, fire risk — which is now an entry. **The crank angle is not the air-cooled/liquid-cooled split**: the America, Speedmaster and Scrambler are 270-degree engines while air-cooled, which this phase's own plan got wrong before the research corrected it. **11 entries**, led by a **live Do-Not-Drive campaign**: the 2024 alternator-connector recall's clip remedy failed in service and was superseded in February 2026 — a completed earlier recall is not reassurance, because that remedy is the thing that failed. Also the clutch-cable-versus-harness recall (nearly 13,000 US machines, faults that move with the bars), model names spanning three engines, EFI throttle bodies styled as carburettors, the 2016 valve-gear change (cams out vs rocker shaft out, 12,000 vs 20,000 miles — both Triumph-cited), throttles that cannot be balanced with vacuum gauges, and a cooling system with no bleed screw. Adapter catalog guarded at 5 rows and the air-cooled gap left for row 230. 773 → 784. 44 tests; regression 5375/0; F9 clean. **Key finding: provenance is a vocabulary problem as well as a discipline** — the corpus had no value for "official regulator recall record", and the closest honest bucket was `service-manual` with the precision carried in the prose. |
| 227 | Triumph Tiger adventure (**1050**/800/900/1200) | ✅ | Triple-cylinder adventure, XC/XR/XRt/GT/Rally variants — row corrected twice. **(a)** The **Tiger 1050** and Tiger Sport 1050 were in neither this row's list nor row 228's (whose "1050" is the Speed Triple, a naked sport); by body style the Tiger 1050 belongs here. Third block-opening row omission after the 690 Duke (222) and the 790 Bonneville (226). **(b)** "Ohlins on top trim" is unsupported — no Triumph handbook names a suspension manufacturer, and the spec pages give **Marzocchi** on the Tiger 900 GT/GT Pro and **Showa** on the 2022+ Tiger 1200, with WP on the Explorer and 2018–2021 Tiger 1200. Öhlins is not part of the Tiger line. **CLOSED 2026-09-08.** **Five researched claims were refuted and none shipped**: the Tiger 1050's front wheel is 17in not 19 (and 17 is what it *shares* with the Speed Triple, so the prose had inverted its own point); "Off-Road Pro" is Rally Pro exclusive, not part of the general Pro pack; the brake-pad corrosion mechanism (nickel, porosity) appears nowhere in the dealer notice cited for it, so the regulator's own wording is used; the XR/XC acronym expansion is undocumented and is **not** published; and the **18,000-mile valve interval widely repeated for the 2024 Tiger 900 is false** — a refuter extracted the handbook's maintenance table with per-word coordinates and resolved the valve row to the 12,000 and 24,000 columns, so the entry names the figure as wrong rather than staying silent. **11 entries**: the **T-plane crank** (1-3-2 firing on the Tiger 900, 850 Sport and 2022+ Tiger 1200 — and explicitly *not* the Tiger Sport 660/800 or Speed Triple 1200), "Tiger 900" naming two unrelated bikes thirty years apart, shaft drive being 1200-only with two bevel boxes becoming one in 2022, valve intervals splitting 12,000 vs 20,000 miles by family, first-service mileages splitting 500 vs 600, the **Tiger 1200 front brake pad corrosion recall** (critical — pads can retain thickness while the friction layer loses its bond, so a depth check passes a failing pad), early Tiger 800 recalls including a deceleration stall and an injector campaign shared with the Bonnevilles, Explorer throttle-butterfly and side-stand-bolt recalls, cylinder head bolts requiring oil-lubricated threads (a dry thread reaches the torque figure at a lower clamp load, silently), the variant letters describing chassis rather than engine, and a **negative finding** — the manifold-pressure-hose stall recall indexed under "Tiger" is Tiger Sport 660/Trident only and does **not** apply to the Tiger 900 or 1200. All 11 are `service-manual`. Adapter catalog guarded at 5; the 2011–2012 Tiger 800 has no row, left for 230. 784 → 795. 46 tests; regression 5421/0; F9 clean. **Key finding: a refuter's most valuable output is a claim you then do not ship.** |
| 228 | Triumph Street Triple / Speed Triple / **Daytona** | ✅ | Triple sport line, 675/765/1050/1200 (the "1200" is **1160cc**), factory race-derived — row corrected: the **Daytona** was in no row at all. This row's detail read "triple naked sport" and the Daytona is a faired supersport, so its own scope excluded it, while 226/227/229/230 cover the twins, the Tigers, the pre-Hinckley bikes and electrical. Two compat rows already named it. **Fourth consecutive block-opening row omission** after the 690 Duke (222), the 790 Bonneville (226) and the Tiger 1050 (227) — the pattern is now worth naming: the roadmap's model lists were written from model *names*, and every make has a machine whose name does not announce which list it belongs to. **CLOSED 2026-09-08.** **Research was capped, dropped and reinstated mid-phase**, and the file records the result honestly: 3 entries written from general knowledge in the gap stay `model-generated`, 8 are `service-manual`, and a test asserts no unsourced entry carries a recall number, publication number or interval figure. **Research corrected two entries I had already written** — the Daytona 675 and Street Triple 675 share an engine only to 2012 (from 2013 the Daytona took a bigger bore, shorter stroke and higher compression while the Street Triple kept the original, so they are different engines, not different tunes), and trim suffixes are *not* always engine-neutral because the **2017–2022 Street Triple S is a 660, not a 765**. Both would have sent someone to order wrong parts. **Both refuters caught the same misreading of a recall form**: the research read the Part 573's candidate model list as the population, when its unit table shows the detent-spring campaign is one model with the rest at zero. What survived is stranger and more useful — the regulator's **structured model index lists models with zero units and omits the affected one**, so a model-name query never returns that campaign; the entry says look it up by frame number. **11 entries**, led by a **Street Triple ABS campaign where anti-lock can be lost with no warning lamp** (critical: the ordinary dash check gives a false pass). Also: two separate 2013 transmission campaigns with non-overlapping frame ranges; the **Speed Triple 1200 radiator-fan recall whose second campaign exists because the first repair may not have been effective** — second superseded-remedy instance after the Bonneville alternator connector; a **negative finding** that no recall touches the 675 valve train, with three things that get blamed on it; valve intervals of 12,000 miles across the line except the Speed Triple 1200 at 20,000, with the 675 generation admitted as not established; and Daytona charging failures split between a campaign and a wider owner-reported pattern. Adapter catalog guarded at 5. **Cost after the cap: 6 agents, 827K subagent tokens, 36 min — roughly half the previous runs.** 795 → 806. 46 tests; regression 5467/0; F9 clean. **Key finding: the value of research is measured in the claims it removes and the ones it corrects, not the ones it adds** — two of my own entries were wrong before it ran. |
| 229 | Triumph vintage (pre-Hinckley Meriden + early Hinckley) | ✅ | Meriden twins and the Hinckley modular family; **T509 and T595 are factory project codes, not capacities** — the T595 Daytona is a **955** and the T509 Speed Triple an **885**, and Triumph itself renamed the T595 to "Daytona 955i" for 1999 because the badge was being read as 595cc. Row note corrected: "early Hinckley carbureted" is only half true — **carburetted and injected machines share model years** from 1997, and several models stayed carburetted to the early 2000s, so the year does not determine the fuel system. **CLOSED 2026-09-08.** **The most saturated topic in the corpus, and the audit found its shape**: four vintage files hold **40 entries**, ten each, and every make's file covers the *same ten topics* — cam chain, charging, carb rebuild, points, petcock, forks, brakes, oil leaks, harness, chain. `cross_platform_ignition` already owns points-versus-electronic **and names Boyer Bransden for British twins**, so the most obvious Triumph-vintage entry was already written by another file. **But every British-specific term returned zero** — Whitworth, BSF, Cycle thread, positive earth, oil-in-frame, Amal, 360-degree crank, dynamo — so the file is built on the axis the corpus lacks: not what is old about an old bike but what is **British** about it, with a counter-assertion sweeping all six existing vintage files for zero. **13 entries**: mixed thread standards on one machine and the near-misses that do the damage (3/16W is 11.3mm against an 11mm spanner; quarter-inch Cycle at 26tpi starts in an M6 hole then strips); **positive earth through 1978**, where assuming otherwise damages a modern regulator; **oil-in-frame, where a crack is a leak and a structural failure at once**; no oil filter but a crankshaft sludge trap that kills big ends if a rebuild skips it; **wasted spark, so one dead cylinder can never be the ignition module** — the phase's best diagnostic shortcut; the **Amal pilot screw being an AIR screw**, inverted from a Japanese pilot fuel screw; vibration as designed-in cause rather than symptom; the Hinckley **badge not being the capacity** (every 900 is 885, every 1200 is 1180); the T-number trap; carb/injection coexistence; the 1994–95 Speed Triple being a **five-speed** and possibly a 750; early Hinckley recalls including a frame headstock weld and a **fuel connector that fractures following in-service handling**; and metric fasteners as the clean 1991 break. **Refutation removed six claims** — a wrong Whitworth end-date contradicted by the finding's own source, an inverted seat height, unsourced valve and balancer specs, a wrong recall year range, a polarity symptom overreach, and the five-speed framed as retro-only when it also covers the Speed Triple. **6 agents, 661K subagent tokens, 23 min** — cheapest run yet. 806 → 819. 48 tests; regression 5515/0; F9 clean. **Key finding: when a topic is saturated, the phase's job is to find the axis the corpus lacks** — here it was Britishness, not age. |
| 230 | Triumph electrical + tooling (TuneECU / TuneBoy / DealerTool) | ✅ | **Row title corrected**: it named TuneBoy while its own notes said TuneECU. Both cover Triumph, and — unlike KTM, where Phase 225 found TuneBoy is only a tune editor — **that answer does not carry across**: on Triumph TuneBoy also ships a diagnostic module, though it publishes no function matrix for it, so its per-model diagnostic coverage is recorded as unestablished. **CLOSED 2026-09-08 — closes the Triumph block.** **First closing phase to arrive with a written brief**, three inheritances recorded on this row by earlier phases, each verified rather than trusted. **(1) The adapter gap (226) is filled, and the honest answer was two answers**: the injected air-cooled Bonneville gets full coverage, while the **carburetted 2001–2007 machines get an explicit `incompatible` row** — they have no engine control module and no connector, so no tool can exist, and an absent row would have implied merely missing tooling. Two adapters added (`tuneecu-triumph-android`, `dealertool-triumph`), 17 compat rows, Triumph rows 5 → 22. **DealerTool is third-party despite the name**; Triumph's own dealer software is the separately distributed Triumph Diagnostic Tool. **(2) P0315 (227) held completely** — a refuter re-downloaded Triumph's bulletins and confirmed verbatim that it is Euro 5 only, normally adapted at the factory, occasionally needed at PDI, **cannot be cleared with the normal erase function**, and needs a full power-down after adaption. Written as an unperformed adaption first and a sensor fault second. **(3) Cylinder numbering settled with an exception**: Triumph's own tables give "Left to Right" for the transverse triples, but the **Rocket 3's longitudinal triple is "Front to back, 1 at front"**, so all three misfire rows carry the exception and a generic row would mislead on that model. **7 DTC rows** (4 shadowing, all differing) and **5 knowledge entries**, the best of which is that **a flashing Triumph warning lamp means an identity or security mismatch while a steady one means an ordinary fault** — lamp behaviour is specified per code in the workshop manual. Also: the socket is OBD-II *shaped* but speaks Triumph K-line, so a generic car reader fits and reads nothing; and Triumph documents **no** dash diagnostic mode, so the button sequence owners share is recorded as not-factory-procedure rather than repeated. **Refutation removed an unsourced ISO standard number, corrected a misread tool-coverage column, and fixed a vendor URL that does not exist.** The 226–229 row-count guards were **inverted deliberately** — they now assert the original five rows survived and the gap is closed. **6 agents, 760K subagent tokens, 25 min.** 819 → 824. 42 tests; regression 5558/0; F9 clean. **Key finding: an inherited claim is not a verified one** — P0315 arrived with a provenance chain one refuter long and was checked again before being written. |

**Triumph block closed at Phase 230 (2026-09-08).** 51 entries across five files, 7 Triumph DTC rows, 2 adapters with 17 compat rows (Triumph rows 5 → 22). Four roadmap corrections: the **790cc** Bonneville generation (226), the **Tiger 1050** and an "Ohlins" supplier that was never there (227), the **Daytona** having no row at all (228), and "early Hinckley carbureted" being only half true since carburetted and injected machines share model years (229). **The block's recurring lesson: four consecutive block-opening row omissions** — the 790 Bonneville, the Tiger 1050, the Daytona and, before it, the 690 Duke — because the roadmap's model lists were written from model *names*, and every make has a machine whose name does not announce which list it belongs to.

### Aprilia + MV Agusta (Phases 231–235)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 231 | Aprilia RSV4 / Tuono V4 | ✅ | V4 superbike/hyper-naked, Ohlins, **aPRC = Aprilia Performance Ride Control**, factory race heritage. **CLOSED 2026-09-08 — opens the Aprilia + MV block.** Aprilia had **zero** entries (four apparent hits were the Honda CBR600F file's "F2/F3/F4" labels) while the *topic* is saturated — V4 in 10 files, traction control and Öhlins in 15 each — so the backwards genericness test ran at full strength with a corpus-wide counter-assertion. **Fifth consecutive block where a badge is not a capacity**: **no Aprilia V4 has ever displaced 1100cc** — "1100" means 1077cc on the Tuono and the 2019–20 RSV4 1100 Factory and 1099cc only from the 2021 RSV4, the 1000→1077 step is **bore only** and the 1077→1099 step **stroke only** (so cranks and rods differ across the later change and not the earlier), and Aprilia sold **two displacements of the same model concurrently**. **8 entries**, led by two rated critical. The first: an early-RSV4 connecting-rod campaign whose remedy is **engine replacement** and which the regulator files under the make spelled **"APRILLA"** — so a correctly spelled by-make search returns clean on an affected bike, the same class of finding as the Phase 228 index defect; the entry teaches the frame-number habit. The second: the front brake master cylinder as a repeat offender across several campaigns and years, plus pads whose friction material corrodes and detaches while retaining measurable thickness. Also: **cylinder 1 is the left REAR** on the 65-degree longitudinal V4, with banks alternating (1 and 3 rear, 2 and 4 front), so a cylinder-1 code sends a mechanic backwards; the cam drive is **chain to the intake cam and a gear pair across to the exhaust**, with bank service positions at **150° and 450°** rather than even increments; three **normal aPRC behaviours that read as faults** (a dyno or rear-stand run latches the rider-aid lamp, the pit-lane limiter flashes the *immobiliser* lamp, and a two-second pulse every two minutes is a track-use counter needing a dealer reset); recalibration after wheel, tyre or sprocket work; and charging failures as **owner consensus rather than a campaign**, with two generator families that are not interchangeable and cannot be told apart without pulling the cover. **Refutation removed a phantom recall number** — the cited UK reference is a *Citroën* recall, the real campaign being a Canadian one given a UK prefix — so **no campaign number appears in the file at all**; entries describe campaigns and route to the frame number. Aprilia and MV adapter rows guarded at **zero** for row 235. 824 → 832. 34 tests; regression 5591 passed / 1 failed, then fixed — the single failure was the Phase 208 doc-count guard catching **my own error**: I wrote Phase 232's content file into the tree while this phase's regression was running, so the live seed count became 837 while the docs said 832. The guard is doing exactly its job. The file was backed out, the count re-verified at 832 and the failing test re-run green; the clean full-suite figure is recorded at Phase 232, which runs over the same tree plus that file; F9 clean. **Key finding: a clean recall search can be a data artefact** — here a misspelled make hides an engine-replacement campaign. |
| 232 | Aprilia Dorsoduro / Shiver / SR Max | ✅ | V-twin supermoto (Dorsoduro), naked (Shiver), **scooter (SR Max) — a different machine class, not a model variant**: single-cylinder with a continuously variable transmission, sharing nothing with the V-twins, so it is written as its own entry rather than absorbed. **CLOSED 2026-09-08.** **Written from a paired research run** — Phase 231's capped 6-agent workflow carried one question per phase, so this phase launched none of its own. Same make as 231, **different engine**: the 90-degree *longitudinal* V-twin, so cylinders are front and rear and per-cylinder codes land on the wrong one if read with transverse habits; the 900 is the 750 **stroked**, same bore, so bore-following parts may carry across and stroke-following ones do not; and a Dorsoduro 1200 exists on a different engine entirely. **5 entries**, and the most useful is a **negative finding**: the chronic fuel-pump and charging failures widely claimed for these machines appear in **none of three national recall databases**, while two genuine safety campaigns do — a **gearbox output shaft** fault that can loosen the front sprocket fastening with the rear wheel able to lock, and a **front brake master cylinder that can drag or self-apply showing no brake light**. The entry scopes the absence carefully: no such campaign in the databases consulted, which is not the same as no such failure. Also: the Shiver's early ride-by-wire **self-learns at every key-on**, so a weak battery throws a throttle fault before the machine moves, and its twist-grip tracks are **not a mirrored pair**; a torque-monitor layer can stop the engine while storing a code the dash never displayed; and the throttle body and twist-grip sensor are **non-serviceable assemblies**, so a component-level quote is wrong before it is sent and the cheap inputs should be excluded first. No campaign numbers appear, carrying forward the Phase 231 decision. Aprilia and MV adapter rows still guarded at zero for row 235. 832 → 837. 31 tests; regression 5623/0; F9 clean. **Key finding: an absence is only useful next to the presences** — saying what is *not* recalled is worth little unless you also say what is. |
| 233 | MV Agusta 3-cylinder (F3 / Brutale 675/800 / Turismo Veloce) | ✅ | Counter-rotating crank 675/**798**cc triples (the "800" is 798cc), Italian engineering. Row phrase "known reliability quirks" deliberately not written from — it invites folklore, and Phase 232 showed what folklore is worth against regulator data, so everything here is a campaign, a manufacturer bulletin, or explicitly labelled owner report. **CLOSED 2026-09-08.** Written from a **paired research run** with 234 — second pairing in this block. Genuinely uncovered ground: `counter-rotating` returned one file corpus-wide and `radial valve` zero. **Both refuters caught the same dangerous error**: the research called the Dragster loose-rear-spoke recall UK-only and advised checking spoke tension; both found it in the US database, flagged **do-not-ride**, with the published remedy being **replacement of the rear wheel** — the spoke nipples' surface treatment is out of specification, so the tightening torque **cannot hold** and re-tensioning does not restore them. Acting on the original guidance would have left a defective wheel on a do-not-ride machine. A second correction on the same entry: the swingarm pin bolt fails **in service** from incorrect heat treatment — so a correctly torqued bolt is not evidence of safety. **This row over-corrected, and Phase 240B fixed it**: the clause originally read "not while being tightened", which denies the documented chronology. The defect was found at assembly when a bolt snapped at the prescribed 70 Nm on the line, and recalled because the same batch also fails in service — both halves are true, and the phase test that pinned the denial has been inverted. **5 entries.** The anchor is the **counter-rotating crankshaft**: the crank turns opposite to the wheels with an extra shaft keeping final drive conventional, **nothing internal is reversed** so there is nothing to re-clock — but "turn it in the direction of rotation" means the opposite of the reflex, and turning it wrong can let the one-way cam-chain tensioner slacken the chain and back-drive the starter clutch. **MV's stated direction could not be opened from any manufacturer document, so the entry sends the mechanic to the machine's own timing marks and a test asserts no direction word appears** — the Phase 224 discipline applied to a rotation. Also: the starter one-way clutch, handed for that reverse rotation, reported reassembled backwards during repair and marked `forum`; a scope entry on national versus global recall populations, where the manufacturer's own bulletin names far more machines and models than the national campaign so a clean national check on an imported bike answers a different question; and intervals deferred with the widely quoted F3 figure **not repeated** because it could not be confirmed, and clearance values omitted because sources disagreed. No campaign numbers, carrying forward the Phase 231 decision. 837 → 842. 30 tests; regression 5653/0; F9 clean. **Key finding: the obvious repair can be the wrong one, and only the campaign's own mechanism tells you.** |
| 234 | MV Agusta 4-cylinder (F4 / Brutale 750–1078) | ✅ | Radial-valve inline-4. **Row framing replaced, not repeated**: "exotic pricing, limited dealer network" is an impression, so the phase produced specifics — the workshop manual exists and names its special tools, so mechanical work **is** documented and an independent with the tools can do it; what constrains is parts, tooling with no generic substitute, and campaign status. **CLOSED 2026-09-08.** Written from the **paired run with 233**, launching no research of its own; `radial valve` returned **zero files** corpus-wide beforehand. **A refuter caught the research contaminating this phase from its sibling** — the swing-arm screw campaign was attached to the F4 when the regulator scopes it to three-cylinder models only and an F4 query returns nothing; the finding had carried its own warning against exactly that. It lives in 233's file and tests on **both** sides assert the boundary. **5 entries.** "Radial valve" is demystified: it describes valve **placement**, the cam lobes are ground at a compensating angle onto conventional bucket tappets, and MV's own manual describes an ordinary **shim-under-bucket** job — an online shim-over-bucket claim contradicts that manual and the file names it in order to correct it, because the name is why shops decline the work. **The shim diameter is deliberately absent and that absence is the entry**: sources disagreed and none was authoritative, so the file says measure an existing shim before ordering, since the default is to reach for the Japanese kit on the shelf and the mismatch is otherwise found with the camshafts already out. **Model numbers are wrong in both directions** — 1078 is genuine, **990 is a 998**, 989 is a 982, two badges share one engine, and the worst trap is **two different 998cc engines** with different bore and stroke, so the nominal capacity does not identify a piston; one circulating figure is arithmetically impossible for its own stated bore and stroke and is described rather than printed. Also: the valve interval is **short** by superbike standards rather than long, on a camshafts-out job, so low mileage does not clear a used machine. No campaign numbers, per the Phase 231 decision. MV and Aprilia adapter rows still zero for row 235. 842 → 847. 31 tests; regression 5684/0; F9 clean. **Key finding: replacing an impression with specifics is a deliverable.** |
| 235 | Aprilia + MV Agusta electrical, fault codes + dealer tools | ✅ | Magneti **Marelli** (Aprilia), **Eldor** (MV) — the row previously read "Mercuri (MV)", a string that appears nowhere else in either repo and that no source in this phase found; corrected rather than written from. **CLOSED 2026-09-08.** Three surfaces at once, like 225 and 230: knowledge, DTC rows for two makes that had **none**, and the **adapter gap 231–234 each guarded at zero** — aprilia 0→15 rows, mv-agusta 0→6, adapters 28→34. **The two makes are opposite traps and that is the finding.** Aprilia ships **no manufacturer P1xxx block at all** — every fault is an ordinary P0xxx number — but many carry proprietary meanings: **P0510 is rear wheel radius acquisition**, not a closed throttle position switch; **P0462 is the quick-shift sensor**, not a fuel level sensor; **P0217 is manifold pressure below estimate**, not engine over-temperature. MV does the reverse with a genuine P1xxx block a generic reader cannot decode. So the tool fails **loudly** on an MV and **silently** on an Aprilia. **MV’s P1xxx codes are deliberately not decoded**: the block is named in a knowledge entry so a mechanic knows the code is real, but no row is shipped, because no MV primary document could be opened and a guessed meaning would reproduce the very Aprilia failure documented here — a test asserts no P1xxx row exists, the 233/234 discipline applied to a code range. **A refuter overturned a full model generation.** The research called every pre-Euro 5 machine closed and encoded two 1997–2020 `incompatible` rows; the refuter opened the EU regulations the finding had declared unretrievable and found L3e vehicles are **OBD stage I from 1 Jan 2016**, access must be "unrestricted and standardised", and the proprietary plug is a permitted **"alternative connection interface"** whose pinout the manufacturer must publish **free of charge**, with a generic-scan-tool adapter owed "upon request to all independent operators in a non-discriminating manner". **I fetched Reg (EU) 2018/295 and confirmed those clauses verbatim** before writing them, since this is the one claim a shop would act on against a manufacturer. Generic rows are now three tiers per make — incompatible to 2015, **read-only across Euro 4**, read-only on Euro 5 — and the entitlement is its own entry, scoped honestly as real, **narrow** (no ABS, dash, immobiliser, coding, service reset) and a **type-approval obligation rather than a bench-verified response**. **Two refuters disagreed on a count and the source settled it.** The research said "fourteen" never-displayed Aprilia codes, listed fifteen, and invented P0218; one refuter said 18 omitting P0116, the other 19. The manual was on disk: **19**, the second refuter exactly right. **My own first count was 18** — the sentence wraps as "in- strument" so a plain search misses P0611, the very trap the refuter had just described; de-hyphenating gives 19. Now a module constant with the trap written beside it. **The never-displayed entry extends 232 rather than restating it**: 232 owns the torque-monitor case, and what is new is the **scope** — nineteen codes, most of them ordinary (oxygen sensor and its heater, engine and air temperature, starter switch, secondary air, battery voltage), so a routine emissions fault can be present and current with no light at all. Also: Aprilia’s hidden **service-code menu reports dashboard faults only** (DSB 01–08) and is not a substitute for a dealer tool; **MV has no software of its own** — TEXA is its official supplier, so MV’s dealer tool is sold openly while Aprilia’s **PADS** cannot be bought by an independent at all; **TuneECU covers Aprilia but not MV** and is a mapping tool whose Aprilia DTC capability is undocumented, shipped as a deliberate `incompatible` row. **A test caught a content hole**: 231 deferred `P0xxx` and `PADS` here and my prose named neither while the catalogue named both — fixed. **The guard inversion deliberately avoids the constant-for-invariant bug** from 221/222/230: 231 and 232 now assert the gap is filled and the slug is `mv-agusta`, without pinning 235’s adapter slugs. **Provenance vocabulary gap recorded, not papered over** — a verbatim EU regulation fits none of migration 051’s five values, and `unverified` would have dragged a primary legal text into Gate 2’s forum-tip rule; a `regulation` value needs its own phase. Only **four** year ranges are source-stated; every other coverage row is labelled an era estimate, and MV three- vs four-cylinder tool parity is recorded as **unconfirmed**. 847 → 855. 50 tests; regression 5734 passed / 0 failed; F9 clean. **Key finding: the make that looks readable is the dangerous one.** A generic tool on an MV fails loudly and sends you for the right tool; on an Aprilia it succeeds, formats a clean answer and names the wrong component. |
| 235B | `regulation` provenance value for known_issues | ✅ | **Substrate follow-up to 235, which explicitly deferred it.** Migration 051 CHECK-constrained `known_issues.source` to five values and none fits a **primary legal document**; Phase 235 filed its EU-regulation entry as `service-manual` because the honest-looking alternative `unverified` sits in **Gate 2’s forum-derived allowlist** and would have held a quoted regulation to the forum-tip rule. Recurring class, not a one-off — Track K leaned on regulator or legal sources at 228, 231, 233 and 235. **CLOSED 2026-09-08.** **SQLite cannot alter a CHECK in place**, so 052 is a CREATE-COPY-DROP-RENAME rebuild, and every way a rebuild silently loses something was **probed on a populated v51 database with a live FK child**, not reasoned about: `PRAGMA foreign_keys=OFF` **does** take effect inside `executescript` (without it `DROP TABLE` is refused outright, since `repair_plan_items.source_issue_id` references the table and `get_connection` sets the pragma ON); rows survive; the **autoincrement sequence** survives, which matters because repair plans reference `known_issues.id` and a reset sequence would reissue live ids; both indexes are recreated; the child FK re-points with `foreign_key_check` clean; the pragma is left ON; and the CHECK **still rejects a typo**, so widening did not weaken. Each is now a test. **Rollback maps `regulation` → `service-manual`** — without it the restored five-value CHECK would reject exactly the rows that motivated the phase; and it is not arbitrary, being the stand-in 235 used, so rollback restores the prior state rather than inventing one. **`regulation` is treated as reviewed, and the stake was concrete**: the reclassified entry renders warning-free today, so excluding it from `VERIFIED_SOURCES` would have made a pure relabelling start printing **“Origin not recorded” over verbatim EU law** — false, and a silent behavioural regression. It does not fall through to silence either: it prints a **scope note rather than a warning** — *authoritative on what is required, not on how a particular machine behaves* — the same distinction 235’s own entry draws between a type-approval obligation and a bench-verified response. `IssueSource` **had** to gain the value: it mirrors the CHECK for OpenAPI codegen, so omitting it is a validation error on a legitimate row, not a policy call. **Exactly one entry of eight reclassified, established by evidence** — three `service-manual` siblings cite Aprilia’s manual and a test pins that they were **not** swept along, because a relabelling can be wrong in both directions. **F9 was blind to the one migration shape that can change a CHECK.** The `pydantic-literal-vs-check` parser keys each constraint to the nearest preceding `CREATE TABLE`, so a rebuild files its CHECK under the **scratch table name** and the rule kept reading the superseded `ALTER TABLE ... ADD COLUMN` — reporting drift that did not exist, since the live CHECK and the Literal agreed exactly. As CREATE-COPY-DROP-RENAME is the only way to alter a CHECK, the guard was blind to precisely the migrations most likely to move an enum. **The parser now follows `ALTER TABLE <scratch> RENAME TO <real>`**; fixing the rule beat a `noqa` that would have silenced a guard which is otherwise correct. **Three anti-regression schema pins fired, which is what they are for** — Gate 9, Gate 11 and 191B, all bumped 51 → 52, and **two carried stale provenance comments** (Gate 9 still described migration 043; 191B’s opt-out said “literal `47`” while asserting 51), both corrected. Applying 235’s audit lesson, the sweep looked for **every** pin rather than only those that failed; 191D derives its fixtures from the constant and needed none. A **cross-surface guard** now parses the CHECK out of `sqlite_master` and asserts it equals `get_args(IssueSource)`, so the next phase cannot drift the API contract silently. SCHEMA_VERSION 51 → 52; vocabulary 5 → 6. 24 phase tests; regression **5760 passed / 0 failed**; F9 clean. Backend `implementation.md` 0.13.45 → 0.13.46. **Key finding: a relabelling is a behaviour change unless you check the render path.** Moving one entry between two provenance values touches no content, but `source` drives a trust branch — and both honest-looking options had teeth. |

### Cross-European (Phases 236–240)

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 236 | European electrical cross-platform | ✅ | **Row reframed against a saturated corpus**: GS-911, TuneECU and DDS were each in 3 files, CAN in 14, and 215/220/225/230/235 own each make’s own tooling — so this file holds only what is visible when the makes are compared side by side. **CLOSED 2026-09-08.** **The research’s headline cross-make finding was backwards, and the correction is the better finding.** It reported TuneECU degrading *identically* on newer Triumphs and KTMs (write survives, read does not) and built a consent scenario on it; a refuter read the table’s cells — Triumph’s degraded rows are diagnostics-and-write, **KTM’s are diagnostics only** — so the tool cannot write a 1290 Super Duke at all, and the finding had contradicted itself between its own scope and claim fields. What ships is the **asymmetry**: the same tool loses different functions on different makes. A second contradiction was internal: the finding said the red-plug Triumphs were absent from the table while its own earlier section listed them present. **13 entries.** "Supports BMW" in TuneECU means **one 449cc enduro single**; TuneECU scopes **Ducati by ECU part number**, the only make treated that way, with the Hypermotard 1100 in and the 1100 EVO out; **TEXA is the official tool vendor for Ducati and MV Agusta** by TEXA’s own brochure — so those two makes have no dealer-tool lockout, while BMW/KTM/Triumph/Aprilia/Moto Guzzi are ordinary aftermarket coverage on the same product; TEXA lists coding/adaptation for BMW and Ducati and **none for four makes**; **IDC5 BASIC contains no European make** — a purchasing trap; **GS-911’s Enthusiast tier is capped at ten VINs**, disqualifying it for a shop; **TuneECU’s adaptor B is literally named "Guzzi-Ducati-Aprilia 3pin"** — one lead, three makes — while **KTM’s single 6-pin shell takes adaptor C (K-Line) or D (CAN)**, a plug that fits but does not talk; and Triumph is the connector outlier with the 16-pin socket *before* Euro 5, moving *to* the red 6-pin — **transition year deliberately not printed**, a refuter found it off by three. **Three things not printed**: that year; a Ducati tool part number sourced from a reseller listing naming a different version; and the PADS expansion, whose cited source did not contain it. **The genericness bar caught me twice** — "the group’s platform" and "at least one make" — both rewritten with adaptor letters and make names, better entries rather than fewer. ISO 19689 and OBD stage I/II dropped as Phase 235’s. **233’s MV ownership guard fired** on this file and was exempted by `european_` **prefix** rather than filename, so 237–239 need not extend a list. 865 → 878. 30 tests; regression **5820 passed / 1 failed → fixed, targeted re-run green** (clean full figure at 237, the 231/232 precedent); F9 clean. **Key finding: a cross-make claim built from a table’s summary fails at the table’s cells.** |
| 237 | European common failure patterns | ✅ | **The row shrank by more than half, and that is the finding.** Its named topics — regulator, stator, cam chain — were in 12, 57 and 39 files, plus a generic charging file holding the causal chain; refuters scoring genericness named **19 of 34 proposed entries as duplicates, file and title**. **Reframed as differential diagnosis**: same symptom, this make, this first check, why a Japanese-trained routine gets it wrong. **CLOSED 2026-09-08.** **15 entries.** BMW oilhead: the **brush/regulator module inside the alternator**, serviceable in situ, before condemning an alternator whose removal displaces the ABS unit and ECU; BMW boxer **alternator belts come in two incompatible types** and tensioning the ELAST stretch-fit destroys it — no Japanese machine has the part; BMW water-cooled boxer: the stator is **inside the crankcase**, so the two regulator plugs under the seat come first; **KTM 950 Super Enduro R** regulator fails by **over**-charging, the under-seat whistle being electrolyte boiling through the safety valve — a refuter caught the research reading "SE-R" as a regulator type rather than the model, and the scope narrowed from every 950/990 to one machine; KTM LC8 charging fault with a rattle from the ignition cover is **freewheel bolts in the stator**, mechanical not electrical; Aprilia V4: **the flywheel is the first suspect**, and a stator-only replacement reproduces the failure; **BMW boxer startup clack is normal — the opposite call to the identical noise on a Triumph triple**; KTM 690: the **roller rocker bearing**, which a shim-under-bucket model has no place for; Moto Guzzi 8V: **flat versus roller tappets is a build-date question** that decides engine survival, the currency-mangled cost figure removed; Triumph 955i warm high idle is the **idle-valve hoses and throttle-body gasket**, not the stepper; BMW ZFE **has no fuses to pull** and an LED throws a lamp fault; hexhead final drive crown bearing **sealed pre-2010, oil-fed and vented after**; Desmoquattro rocker chrome — **glitter in the oil means the rocker faces, not the gearbox**; KTM LC8 water-pump seal as a scheduled part, its fabricated weep-hole hook removed; LC8 balance-shaft seal — **an oily front intake is a seal, not fuelling**. Three fix procedures implied an order without stating it and the test sent them back. **Five copies of the designation-bar guard, not two**: 228 and 231 fired, a sweep found 227 and 232 waiting to fire at 238/239, all exempt the `european_` prefix as an invariant — the 235 audit lesson repeated, and this time the sweep caught two before they failed. 878 → 893. 25 tests; regression 5846 passed / 0 failed; F9 clean. **Key finding: the refuters’ duplicate list is the most useful artefact this phase produced** — nineteen named pairs of proposed-to-existing is a map of what the corpus already knows, drawn by an adversary with no stake in the phase’s size. |
| 238 | European valve service intervals | ✅ | **The best-sourced phase in the block, and a refuter proved it**: the source lens re-downloaded ten manufacturer PDFs, **MD5-matched** them, and re-read every schedule table visually — every BMW, KTM, Ducati, Triumph and Guzzi interval survived. **CLOSED 2026-09-08.** **13 entries, every figure carrying its document by name.** The sibling test: on KTM, Ducati and Triumph, siblings on one engine carry one interval in the manufacturer’s tables — and the finding said no same-engine pair differs anywhere. **The contradiction lens found six MV Agusta maintenance manuals already on disk, unopened**: the 798cc triple at **12,000 km** (Brutale 800, Dragster RR, Rivale) and **30,000 km** (Turismo Veloce, F3) in overlapping years, tracking MV’s move from a 6,000 km coupon ladder to a 15,000 km one, model by model. **I shipped a false sibling claim at 225B and this phase corrects it**: its refuter compared a 373cc Adventure against a 399cc Duke; on one engine the two agree, in eleven KTM manuals. The corrected entry keeps what was true, states the correction, and **prints no figures** — my first correction put them in and 225B’s own boundary test caught it. Also: **no European valve row opened carries a time trigger** — "due every two years" is a shop convention, not a manufacturer instruction, on these makes; intervals move by **engine generation** (BMW boxer 10k→20k, Testastretta 24k→30k, KTM 373→399cc, MV’s ladders); **five job types** with provenance labelled where the method rests on owner reports; Ducati’s Desmo labour in six-minute units (Panigale V4 full Desmo 9 h 06); KTM’s service minutes; Triumph’s own sheet billing check and adjustment separately; **Aprilia’s official maintenance sheets omit the valve row** and are consumables lists; MV’s coupon table has a **merged first cell** the research had read shifted by one; **the F4 shim diameter is still in no manufacturer document** (234’s rule stands) while **the F3 MY2020 figure is now confirmed** (234’s deferral closed); Ducati’s 60,000 / 45,000 km figures are spring-valve engines, not desmo; Guzzi’s V85 and V100 take opposite job types. **A sixth copy of the designation-bar guard fired** (226): three earlier sweeps matched the test's *name*, and this one is named differently — re-swept by **shape** (every test globbing the knowledge dir and skipping by filename), which returns exactly six and clears 221's title test as correctly scoped. 904 → 917. 24 tests; regression 5898 passed / 0 failed; F9 clean. **Key finding: the primary source may already be on disk.** The finding’s one confident negative was overturned by documents a previous agent had downloaded to the same scratchpad and no one had opened. |
| 239 | European parts sourcing + pricing | ✅ | **"Pricing" was already refuted as generic at 237, so this became rows a catalogue can hold.** `parts.json` had **zero** Aprilia, MV Agusta or Moto Guzzi rows — the 235 adapter-gap shape on a different table. **CLOSED 2026-09-08.** **62 → 125 parts rows** (aprilia 0→11, mv-agusta 0→4, moto-guzzi 0→9, ktm 4→17, ducati 4→17, bmw 6→16), **83 → 100 cross-references**, 8 new categories, **11 knowledge entries** (893 → 904). **Both refuters refuted on fitment, and fitment is where a catalogue hurts**: every part number checked out against the fiche; what was wrong was which bikes each row claimed — a KTM rim-band pattern that would have matched the R variants (different band), a BMW belt xref **paired backwards**, a Ducati belt on the wrong Scrambler years, a Hiflo filter six years late. Thirteen corrections, each a named test. **The BMW ELAST "disagreement" has an explanation** a refuter found in the same thread: BMW prints production length, Continental fitted length — two belt generations overlapping 2005–07, so the fiche by production month decides. **The loader silently took 111 of 125 rows** on the first run — `INSERT OR IGNORE` on a slug that collided across multi-fitment parts; slugs now carry a pattern token and the test loads end-to-end. **Cost 0 is the repo’s own unpriced sentinel** (`add_part` default; cost queries filter `> 0`), used on 43 rows with the fiche GBP/EUR in notes; **one row carried my own GBP-as-cents error**, caught by the phase’s no-conversion test. **A pre-existing duplicate xref pair in the Phase 153 seed surfaced** — detected and documented, left as data. Constraints that rest on documents: Guzzi 8V tappet kits released **only against a documented wear claim** ("no preventive operations"), out-of-warranty supply via help-desk ticket; the Aprilia V4 has **two charging families** and the engine-number split in circulation is aftermarket-only; BMW "final drive complete only" is **not established** — bearings carry their own numbers; **no MV rim band could be established**, the documented spoked-wheel remedy is a whole wheel; Triumph’s fiche does not itemise the hoses that fail; KTM LC8 fiche pages are **truncated**, so "not listed" is unknown, not no-fit. Three MV distribution entries dropped as generic. 26 tests; regression 5872 passed / 0 failed; F9 clean. **Key finding: a catalogue row fails at the fitment pattern, not the part number.** |
| 240 | Gate 12 — European brand coverage integration test | ✅ | **Row corrected from "Gate 11"**: Phase 205 had already closed under that number (desktop + mobile end-to-end, Track J’s opener), so Track K’s closing gate is **Gate 12** — the same class of correction as "Mercuri (MV)" at 235. **CLOSED 2026-09-08. Track K closes.** Seven makes swept **through the real front doors**, never the repo layer: `kb search` on the CLI, `/v1/kb/dtc/{code}?make=` on the API — asserting the **make’s** meaning wins over the SAE generic, which on Aprilia is the whole point of 235 — `hardware compat recommend --json` on the CLI, and the shop-scoped `/v1/shop/{id}/parts/search` with a key minted for a seeded owner. **A dry run against the pre-merge tree caught six wrong assumptions** before the gate ever ran for real: the `/v1` mount, the API-key gate, a compat catalogue the fixture had **forgotten to seed** (so `recommend` returned nothing for every make), `check` needing `--adapter` where the question is `recommend`, shop scoping, and a list-shaped response. **Moto Guzzi is the honest gap and the gate records it**: no Track K row, no DTC file, no compat rows, no make-file — seven issues and nine parts rows from 236–239 only — as five executable-documentation tests that pass today and fail the day the gap is filled. Corpus invariants now gated: no campaign number anywhere in the European files, the six-value provenance vocabulary with `regulation` in use, and the documented count equal to the live seed. Six earlier gates re-run; schema pinned at 52. 46 gate tests; regression 5945 passed / 0 failed; F9 clean. `TRACK_K_SUMMARY.md` written. **Key finding: a gate’s dry run is where the gate’s own assumptions fail** — six of them, none about the corpus, all about how the front doors are actually built. |
| 240B | Track K audit debt — the contradictions dimension re-run uncapped | ✅ | **The cap hid the majority of the dimension, and the retrieval layer is why the corpus drifts.** `TRACK_K_AUDIT_DEBT.md` warned against itself: the contradictions auditor found **16**, the workflow submitted `findings.slice(0, 8)`, and section A is "the confirmed subset, not the full set". Re-run uncapped as a six-way partitioned adversarial sweep over 30 files / 257 entries / 8 DTC files / the catalogues, every finding required to carry verbatim quotes from both sides, a same-subject argument, an operative cost, a reachability trace through the real predicate and a recorded kill attempt: **27 contradictions and 13 uncertain**, against 6 confirmed. **CLOSED 2026-09-09.** **Five structural defects surfaced that no content audit would have found**, and they are the mechanism the first audit missed. `issues_repo.py` orders `severity DESC` on a **TEXT** column, so SQLite returns `medium, low, high, critical` — **`critical` comes back last** on all three retrieval paths, and `medium`-rated stale entries outrank the `high` and `critical` corrections written to replace them. Three other modules in the same repo (`shop/issue_repo.py`, `engine/correlation.py`, `advanced/predictor.py`) map severity to a rank correctly; this one is the outlier. **Five of six slices found it independently.** Three entries carry a `make` of "All European makes"/"All makes", which `make LIKE '%X%'` can never match — and one of them is `european_intervals[4]`, **the target of contradiction A5**, which changed the fix: correcting it alone would have been invisible to a `make="KTM"` lookup, so the reachable sibling `intervals[6]` carries the correction too. `european_tooling[2]` holds a KTM *and* a Triumph correction under `make: 'KTM'` — the KTM half was back-propagated, the Triumph half was reachable from nowhere — and its `year_end` of 2026 makes it the **sole** TuneECU answer for MY2021+, implying the tool works on machines it does not support. And two machine-readable service intervals are wrong: a blanket 7,500-mile **"Desmodromic valve service" for every Ducati** including the two spring-valve engines, and a KTM 690 valve check stored as 15,000 **miles** where the corpus states kilometres — `scheduler.next_due` reads `every_miles` and never `notes`, so both caveats are unreachable by the code that books the work. All five recorded and scheduled, **not fixed**: S1 alone reorders every knowledge query in the product and 73 test files assert on `results[0]`. **The six section A contradictions fixed**, each following its verifier's `corrected_fix` rather than the auditor's recommendation, which was wrong or tree-breaking in five of six — the BMW ELAST belt made self-contained rather than rescoped (rescoping breaks a parametrised 2018 coverage test), the final drive made generation-aware **in place** (the entry count is pinned in four places), Euro 4 OBD corrected at **four sites** including the fix step that actually costs a shop work, the desmo time element phrased as a negative instruction plus routing rather than a flat positive claim the entry's 1993–2026 span cannot support, and **the indexed symptom string replaced** because it is what makes an age-based query land there. **917 → 917: no entry added, removed or split**, so every pinned count and the Phase 208 doc-count guard stayed green. **Three debt items closed as no-change after verification** — B2, B4 as framed, and two of section C's three claims — recorded with reasoning so nobody re-litigates them; roughly 60% of the original audit's findings did not survive verification and that is the layer working. **One conflict between two verifiers adjudicated**: the MV-sprag verifier rejected B1 because a test forbids the tip, but that assertion *is* the mis-scoped denylist Phase 226 identified inside Gate 2 and then reproduced locally — a guard that enforces the violation is not evidence the violation is permitted. Both entries fixed, **forum-tip guards asserting the biconditional 3 → 13**, untipped `forum` entries **2 → 0**, tips placed last so `predictor._extract_preventive_action` returns a self-contained action rather than a truncated procedure. **Constant cumulative pins 8 → 0** — totals derived from the same JSON the test loads, and the make-filtered count *counted* rather than assumed equal to the file total, which it is not. **The `_asserts` family de-vacuumed: before, stubbing it to `return False` left 180 of 181 tests passing; after, all seven files fail** — probes written with a literal term in 216–219 and a raw regex in 221–223, because the two implementations differ and the wrong kind asserts the very deadness it is meant to detect. The Phase 237 mileage guard was not merely leaky but **fully dead** (the file holds no km/mile token at all); widened to catch the uncomma'd and `12k` forms the corpus already authors, with the anti-vacuity check on the **pattern** rather than the file, since this file's job is to print no interval. Aprilia/MV shadow **zero** generic codes, so the zero is pinned as a tripwire and `dtc_repo`'s `candidates[0]` fall-through recorded. Section C's rule decided and enforced — **weakest link in the evidence chain** — demoting the eight vendor-documentation entries to `model-generated` (13/13) in the direction Phase 235 had already tested, and asserted as a rule rather than a count. **Nine mutation scenarios, all caught.** Also corrected: the debt document's index for the Bonneville forum entry (10, not 11) and ROADMAP row 233's own "not while being tightened" clause, which denies a documented chronology — the defect was found at assembly when a bolt snapped at the prescribed 70 Nm, and the phase test pinning the denial has been inverted. 7 seed files, 26 test files. Backend `implementation.md` 0.13.52 → 0.13.53. Regression ****5991 passed / 0 failed** (baseline 5954; +37 tests)**. **Key finding: the cap was not the whole problem — the retrieval layer was.** The corrective cross-make files were not merely under-propagated; where they did land, they were systematically outranked or invisible. A corpus can be internally consistent and still hand a mechanic the superseded answer first. |
| 240C | Severity sorts backwards — `critical` came back last | ✅ | **The fix that restores correctness can silently undo an optimisation, and only the query plan will tell you.** Phase 240B found `ORDER BY severity DESC` on a **TEXT** column and recorded it rather than fixing it: SQLite sorts lexicographically and the four values alphabetise to `medium, low, high, critical`, so **`critical` came back LAST**. **CLOSED 2026-09-09.** **The shape sweep found six sites, not the three the audit named** — `knowledge/issues_repo.py` ×3 plus `advanced/recall_repo.py`, `inventory/recall_repo.py` and `cli/advanced.py`, which carry the same four-value vocabulary, so an **open-recall list put the critical campaigns at the bottom**. A name-keyed sweep has missed family members three times on this project; fixing the named three would have left the safety surface broken. `shop/analytics.py` was inspected and ruled **out**: its `severity ASC` is a determinism tiebreaker on a `GROUP BY` aggregate, not a priority claim. **The feared blast radius did not materialise; a different one did.** The plan carried 240B's "73 test files assert on `results[0]`" forward as if it sized the work; a spike over the three knowledge sites returned 5991, identical to baseline, because those files overwhelmingly filter to one entry or assert on content. **But "zero blast radius" was written into the docs before the full change had been run once, and the full change came back red with three failures** — one a bug this phase introduced (the rank substitution dropped the `ORDER BY` keyword in `advanced/recall_repo.py`, and `except sqlite3.OperationalError: return []` turned the malformed SQL into "no open recalls for this bike" instead of an exception; **this phase's own guards missed it** because they covered the inventory lookup and not that one), and two the constant-for-invariant family in earlier phases' tests — `test_phase206_performance` hardcoding the old `ORDER BY severity DESC, title` it was measuring, and `test_phase235b` pinning `apply_pending_migrations(...) == [52]`. **Migration 053 exists because the obvious fix would have undone migration 206**, which added `idx_known_issues_sort` after measuring `SCAN` + `USE TEMP B-TREE FOR ORDER BY`; a `CASE` expression cannot use that index, so 053 replaces it with an index on the **same expression** the queries use — shared from `core.severity.SEVERITY_RANK_SQL` deliberately, because SQLite only uses an expression index when the `ORDER BY` matches it textually. Plan restored to `SCAN known_issues USING INDEX idx_known_issues_sort`, and **the guard asserts the plan, not the index definition** — an index that exists but is not used is worth nothing. `SCHEMA_VERSION` 52 → 53. **The mapping existed eleven times**; the six broken sites were fixed and the five already-correct copies **guarded against drift rather than rewritten**, because touching working code inside a correctness fix is how regressions arrive. **Two bugs caught by this phase's own guards, both mine**: a global replace reached into migration 053's own `rollback_sql` and made the rollback a silent no-op, and the shape guard fired on `migrations.py`, which legitimately holds the old form in migration 049 and in 053's rollback. **Six test expectations changed, each justified, none re-baselined** — five are the deliberate schema contract pins whose own annotations say "bumping requires a corresponding new migration", and the sixth pinned `apply_pending_migrations(...) == [52]`, a constant list any later migration breaks. 16 guards; 4 mutation scenarios, all caught. **Product-visible change**: `predictor`, `core/search` and `cli/diagnose` all consume `search_known_issues`, so what a diagnosis surfaces first changes. Backend `implementation.md` 0.13.53 → 0.13.54. Regression ****6007 passed / 0 failed** (baseline 5991)**. **Key finding: an index is only worth what the planner actually uses.** |

### Track K — open debt carried forward (as of Phase 240B, 2026-09-09)

Track K is closed and its audit debt is closed, but the uncapped re-run at
240B left work that is **not** Track K's to do. It is listed here rather than
only in `docs/phases/completed/TRACK_K_AUDIT_DEBT_2.md` so it is visible from
the roadmap before a later track starts building on the same substrate.

| id | open item | why it is not fixed | suggested slot |
|----|-----------|---------------------|----------------|
| ~~**S1**~~ | ~~severity sorts backwards~~ — **CLOSED at Phase 240C.** The shape sweep found **six** sites, not three; all six fixed, with migration 053 adding an expression index so the fix did not silently undo migration 206's optimisation. Measured blast radius: **zero** test changes. | — | done |
| **S2** | Three entries carry a `make` of "All European makes"/"All makes", which `make LIKE '%X%'` can never match — so they are unreachable from every make-filtered lookup. | Count-neutral data edit, but it changes make-filtered result sets and must land with a sweep of every pinned make-count. | With S3/S4 |
| **S3/S4** | `known_issues_european_tooling.json[2]` carries a Triumph correction under `make: 'KTM'`, and its `year_end` of 2026 makes it the sole TuneECU answer for MY2021+ on machines the tool does not support. | Same family as S2: the correction cannot be reached from where the error is. | With S2 |
| **S5** | `advanced/data/service_interval_templates.json` books a 7,500-mile "Desmodromic valve service" for **every** Ducati including the two spring-valve engines, and a KTM 690 valve check at 15,000 **miles** where the corpus states km. `scheduler.next_due` reads `every_miles`/`every_months` and never `notes`. | Outside Track K's boundary (`advanced/data/`), and no Track K test reads it. | With the catalogue contradictions |
| **N1–N27** | 27 cross-file contradictions from the uncapped re-run, 13 further uncertain. 6 are [VERIFIED]; the rest are [REPORTED] and **must be verified before they are fixed**. | Fixing them here would have meant abbreviating the care each entry gets. | Make-sized batches |
| **—** | Two confirmed findings the original debt document omits from its own sections: the Aprilia V4 charging contradiction (`european_differentials[5]`), and `known_issues_triumph_vintage.json` labelling all 13 entries `service-manual` while two rest on a marque club and a retailer. | The vintage one collides with the forum-tip biconditional — demoting untipped breaks it, and fabricating a tip is what Phase 226 warns against. | Needs a decision, not a patch |

**Read `TRACK_K_AUDIT_DEBT_2.md` before working any of these**, and verify any
`[REPORTED]` finding before fixing it. Roughly 60% of the original audit's
findings did not survive verification; that layer is the point.

## Track L — Electric Motorcycles (Phases 241–250)

Electric motorcycle diagnostics — fundamentally different from ICE: HV safety, BMS, motor controllers, regen, thermal management. Requires foundational safety phases before brand-specific coverage.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 241 | HV safety + lockout/tagout procedures | ✅ | **Track L opens, and on a safety file the deliberate absence is the content.** Ten entries — the service disconnect as the *first* step of isolation, live-dead-live meter proving, the discharge wait as a specified interval, custody of the disconnect as lockout on a machine that takes no padlock, rated and air-tested gloves, the second person and the rescue plan, orange cable as convention not guarantee, the damaged or submerged pack as a different job, the qualification question refused for every territory, and isolation as a state that expires. **None prints a pack voltage, a discharge wait or a torque; every one says so and routes to the manual** — the 224/234 rule where it matters most. **CLOSED 2026-09-09.** **Step 0 found this was extension, not greenfield**: Phases 110/111 already shipped `vehicles.powertrain`, six EV `DTCCategory` members and `dtc_category_meta` rows scoped electric with `hv_battery`/`motor`/`inverter` defaulting to critical. **And it found `SAFETY_RULES` — a 19-rule engine with exactly the right shape — has no production caller**: an exhaustive sweep returns only the export line and the module itself. HV rules were therefore **withheld** from it; a safety system with no delivery path is the appearance of safety, the Phase 195 integration-gap family, unacceptable on high voltage. Pinned as two tripwires that fire the day it is wired or the day an HV rule lands there. **All ten entries `model-generated` and a test pins it** — no manufacturer HV document was opened, and on this file a false `service-manual` label is dangerous. `make` carries the five electric makes verbatim so `make LIKE` returns the file from each (verified live; a prose make would be reachable from nothing — 240B's S2), and **240C's ordering fix is visible in the way it was meant for: critical entries first on a safety file.** Two of my own guards fired on correct entries — *"the point the manual specifies"* is deferral, not borrowed authority, and a space-joined `_claims()` let a cause run into the next field's step number; both mention-versus-use, both fixed in the guard not the entry. 917 → 927, four docs moved under the 208 guard. 24 guards; **7 mutation scenarios, all caught**; 18 corpus-globbing guard files swept, 635 passed. Regression ****6031 passed / 0 failed** (baseline 6007; +24 guards)**. **Key finding: ten entries that print no figure and say so are worth more to a mechanic than ten that guess.** |
| 242 | Zero Motorcycles (S/DS/SR/FX/FXE) | ✅ | **The refuter layer is only as good as the standard it applies, and one wrong clause in my own prompt silently deleted an entire evidence class.** Five finders, 75 unique claims, three adversarial lenses each. 29 survived — **every one `manufacturer-document`, zero recalls, zero community patterns.** That is not what a corpus looks like; it is what a broken filter looks like. My figure lens said *every number must trace to a named Zero document* — correct for a manufacturer claim, wrong for a recall record whose model years and build windows come from the **regulator** — so it refuted all 35 regulator and community claims on a technicality I wrote. **CLOSED 2026-09-09.** A repair run re-judged the 25 that had passed source and scope under a source-class-aware standard: **11 recovered, 8 of them recalls.** Without it this phase ships a Zero file with **no recall coverage at all**, including no stop-riding campaigns. **Every recovered claim came back corrected, and four corrections change what a mechanic does**: the 2013 water-ingress campaign covers the **FX and XU only**, not the full line, and its remedy is **module removal and return**, not sealing; the 2020 brake-switch record describes **no stays-on failure mode** but does record that **cruise control will not cancel**, which the claim had missed; the 2023-24 key-switch remedy is a **firmware update**, not the harness work the mechanism suggests; and the 2012 pack-fire remedy is the owner's **choice** of repurchase or trade-assist under a **park-it advisory**. **14 stayed rejected on judgement** — six recalls whose verifiers opened the Part 573 filings and found the claim wrong on substance, one returning simply *REMEDY IS WRONG AND SAFETY-CRITICAL*. **17 entries**: platform identification (pack, motor and charger differ **within** a model year), the Cypher II/III split, **belt tension whose window differs between street and adventure platforms** and must not be carried across, belt care, the service schedule, storage deadlines, warranty, the diagnostic route (**a Zero is not an OBD-II vehicle** — codes are on the dash, the tool is Zero's own), the app, firmware, charging accessories, five recall entries covering **eight campaigns by mechanism with no reference numbers**, and one forum entry. **The same bug bit me twice**: my own guard requiring a figure to name a Zero document would have failed every recall entry for citing a regulator. **No DTC file shipped** — Zero publishes its fault codes as a numbered table in the owner's manual, not a machine-readable list, and inventing one would invent a vocabulary. 927 → 944. 16 guards; 6 mutation scenarios caught; 654 corpus guards accept the file. **Debt: 5 claims unverified when refuters hit a session limit, and the completeness critic never ran.** Backend `implementation.md` 0.13.55 → 0.13.56. Regression ****6047 passed / 0 failed** (baseline 6031; +16 guards)**. **Key finding: a verifier that is wrong in one direction fails silently and looks rigorous while doing it.** |
| 243 | Harley LiveWire / LiveWire One | ✅ | **The cheaper cadence found the error the expensive one would have missed, because the question was assigned rather than hoped for.** Track K cadence — 2 questions x 2 adversarial lenses, **6 agents, ~980K subagent tokens against Phase 242's ~17M**; 28 claims, **21 survived, 0 unverified**. **CLOSED 2026-09-09.** **The corrected lens is confirmed working**: survivors came back 11 manufacturer / 4 regulator / 6 community, and the lens-skew warning added after 242 stayed silent — first positive evidence that `docs/phases/RESEARCH_LENSES.md` does its job rather than merely not having failed yet. **A claim in shipping content was wrong and this phase corrected it**: `known_issues_cross_platform_cooling.json[0]` listed the LiveWire among Harley liquid-cooled models taking a thermostat replaced every 40,000-50,000 miles. The research enumerated and fetched **all 108 sections** of the LiveWire owner's manual — *thermostat* appears **zero times**, neither service-interval table carries a thermostat row, and the manual's own overheating troubleshooting topic does not list one where it would have to. The 40-50k figure is a **corrupted echo of a real interval**: both manuals schedule a coolant replacement once, at 80,000 km (50,000 mi), dealer-only. LiveWire struck from the list, **and the correction states its own scope** — no claim either way about the Street 750 or Pan America, which were not examined, with a guard asserting that disclaimer survives. The entry likewise keeps saying **not established** about whether a thermostat exists as an orderable part, because the parts catalogue returns 403. **14 entries** (11 `service-manual`, 3 `forum`): the no-thermostat correction, coolant and service cadence, the single-speed gearbox and belt gauge, **a LiveWire ONE on a Level 2 charger charges at the Level 1 rate** — designed behaviour a shop would otherwise chase — charge-curve and state-of-health expectations, the 2020 onboard-charger software campaign that can shut the powertrain down while riding, an honest **negative finding that no regulator record covers the LiveWire ONE**, the badge/dealer/warranty split at 2021, TechLink 3 and the 6-pin CAN link, and owner patterns including the OBC failure that **blocks the very software recall that would have fixed it**. **The reachability guard asserts position, not presence** — `make="Harley-Davidson"` returns 110 combustion entries, so content that merely exists is buried; that assertion is only possible because **240C fixed severity ordering**. Two claims rejected for carrying campaign reference numbers — the Phase 231 rule holding across tracks unargued. 944 → 958. 19 guards; 6 mutation scenarios caught. Backend `implementation.md` 0.13.56 → 0.13.57. Regression ****6066 passed / 0 failed** (baseline 6047; +19 guards)**. **Key finding: breadth finds what you did not know to look for; a named question finds what you already suspect is wrong.** |
| 244 | Energica (Ego/Eva/Esse/Experia) | ✅ | **A guard written before the evidence is a guess about what the evidence will look like — four of five were vacuous on first write.** Opened around **two named doubts in shipped source code**, both refuted. Track K cadence: 6 agents, ~720K tokens, 28 claims, **23 survived, 0 unverified**, distribution 18 manufacturer / 2 regulator / 2 community / 1 not-established, no skew. **CLOSED 2026-09-09.** **Doubt A**: `fault_codes.py` claimed Zero, LiveWire and Energica emit `HV_`/`BMS_`-prefixed DTCs. Energica in fact publishes **110 codes in standard SAE J2012** (P/B/C/U + four alphanumerics) in its owner's manual, and supports **OBD Modes 1-4 and 9** under the EU repair-information duty — so **a generic scan tool reads and clears them**, uniquely among the three makes. Zero hits for the `HV_` family across the manual, the diagnosis sheet and an owner table. The researcher also found *how the error was made*: "HV" and "BMS" appear in the **description column**, never the code label — P1010 is "HV+ CONTACTOR SHORT CIRCUIT". Namespace kept as internal, attribution corrected, what each make actually emits recorded. **Across all three makes now examined the format matches nothing any of them emits.** **Doubt B**: `sound_signatures.py` asserted "Energica uses an oil-cooled PMSM (8 pole pairs)". **No Energica source states a pole count anywhere** — zero occurrences of "pole" in the 126-page manual, none in archived or current spec tables, none in secondary coverage. Three further defects: motor type wrong for most of the range (oil-cooled PMAC only pre-2021, **liquid-cooled HSM** on EMCE, **PMASynRM** on Experia); the formula gives the **electrical fundamental, not audible whine**, which depends on unpublished slot and carrier data; and motor rpm is not derivable from wheel speed. Withdrawn. **My plan said the figure was consumed by no code — a public function computes exactly it, and my own guard caught what my grep had hidden.** **12 entries**, two of which exist because of what is *missing*: that no pole count is published, and that Energica currently publishes **no manual, no service schedule and no warranty** following liquidation, with 7 dealers left and none in North America. 958 → 970. 21 guards; **5 mutations — 4 escaped on first write** (symbol-only price regex vs the withheld `USD 2,500`; underscore-only attribution filter; presence-of-citation instead of attribution-of-figure; a 400-char window inside a docstring full of attribution words), all caught after repair — and repairing one produced a **mention-versus-use** failure where the guard flagged the correction for quoting the claim it refutes. Backend `implementation.md` 0.13.57 → 0.13.58. Regression ****6087 passed / 0 failed** (baseline 6066; +21 guards)**. **Key finding: a guard authored before its evidence must be re-mutated after the evidence lands.** |
| 244B | Guidance mode — answer the question the technician asked | ✅ | **The answer drifted because four separate contracts each made a narrow answer impossible, and none of them was the prompt.** Opened on a real failure the user hit: they showed the product a bike, asked where a leak was most likely coming from, and got "plenty of information" that "drifted from the purpose/question posed during the recording". Step 0 found three structural causes in source, and the user reported a fourth mid-build. **(1)** `_build_user_prompt(vehicle_context, frame_count)` never took the question — it was not drifted from, it was **never an input**. **(2)** `VISION_ANALYSIS_PROMPT` is a fixed six-category sweep (smoke / leaks / damage / gauges / wear / corrosion) with findings typed from an eight-value enum; a complete sweep is the only thing that instruction can produce. **(3)** `tool_choice` hard-forced `report_video_findings`, so even had the question arrived the model **could not** answer in another shape. **(4)** — the one the user noticed — the analysis *"guessed make and model instead of pulling details already provided by the bike in the session"*: `analysis_worker._build_vehicle_context()` was a **stub returning an empty `VehicleContext()`** under a docstring promising "Commit 3 + 4 will JOIN against sessions/vehicles". Those commits never landed, and empty is worse than absent — `to_context_string()` renders it as the literal **"No vehicle context provided."**, so every video analysis since Phase 191B opened by *telling the model nothing was known about the machine*, then showed it pixels. Guessing was the only move left; the `session_id` on the video row had make, model, year and mileage the whole time. All four closed on the media path. New `GuidanceResponse` / `GuidanceCandidate` / `Grounding` contract with **no `diagnosis`, no `repair_steps`, no `parts_needed`, no `estimated_cost`** — every candidate carries its grounding, `NOT_ESTABLISHED` included, so "this project does not establish that for this machine" is expressible rather than something the model must route around. Guidance is a **second tool beside the sweep**, not a flag on it: `analyze_video_frames` is untouched and a guard pins its `tool_choice` line and signature. **Third instance this session of the integration-gap family** — `SafetyChecker` (241) built and never wired, the `HV_` format (244) asserted and never sourced, this stubbed and deferred; **a stub whose docstring promises a later commit is a checkable shape**, swept as debt. 30 guards. **Limitation stated in the phase, not hidden: relevance is asserted structurally, not empirically** — the guards prove the question arrives, that a diagnosis is inexpressible and that grounding is mandatory, but "answered the question" is not a regex; the four structural causes are closed, whether drift is gone needs the original recording re-run. Session persistence of the question deferred with the multi-turn surface; the text path's required `DiagnosisItem.diagnosis` root-cause field remains debt. Backend `implementation.md` 0.13.58 → 0.13.59. Regression **6140 passed / 0 failed** (baseline 6087; +53 guards — one run covering 244B and 244C). **Key finding: asking "why did the model drift" was the wrong question — it was never given the opportunity not to.** |
| 244C | Vehicle identity resolution — a typo must not silently empty the corpus | ✅ | **The corpus was never silent — it was unreachable, and nothing in the stack could tell the difference.** Found by re-running a real recording: the session recorded the bike as **"Homda cbrf4i"** and every knowledge lookup returned **zero rows**, while the corpus held entries for the Honda CBR600F4i. A one-character typo and a colloquial model name each returned zero through *the same code path* that returns zero for a machine nobody has documented. New `knowledge/vehicle_resolver.py`: match ladder **exact → abbreviation → fuzzy → unresolved**, vocabulary read by `SELECT DISTINCT` over `known_issues` rather than a hand-kept alias table that would drift the moment a phase adds a make. **The negative cases are the design.** Resolving a machine onto a neighbouring make would attach another bike's documented faults to it with a mechanic acting on the result — so fuzzy must clear **0.78 and beat the runner-up by 0.15**, abbreviation must match **exactly one** candidate, and inputs under 4 characters cannot fuzzy-match at all. `Ducati` scores 0.364 here and resolves to **nothing**; `zx10r` finds no match under Honda; `cbr600` is a subsequence of three CBR models and is **suggested, not applied** — ambiguity is a question for the technician, not a decision for the resolver. Corrections are **reported, never silent**: the context carries `Vehicle identity: model recorded as 'cbrf4i', read as 'CBR600F4i'`, because swapping a technician's input without saying so is just a different flavour of guessing. **Third defect found while wiring and deliberately NOT folded in**: `known_issues` holds **6,600 rows for 660 distinct issues — every entry duplicated exactly ten times**, no UNIQUE constraint, non-idempotent loader. It had been degrading retrieval all along — the run that exposed it was handed 20 rows containing **2 distinct facts**, and said so. `known_issues_for_vehicle` dedupes on retrieval (2 → 22 distinct), but the constraint needs a migration preserving migration 053's severity expression index, so it is recorded as separate work. 18 guards; **6 mutations, 5 caught on first write** — the sixth used an input the *floor* rejected rather than the length gate it claimed to test, and passed with the gate deleted; repaired with `cbz`, which scores 0.800 against `CB` with a 0.400 margin. **Second phase running where a guard tested the failure its author imagined instead of the one that exists.** Verified end-to-end on the original recording: **0 → 22** corpus rows, **0 of 15 findings about the leak → 3 ordered candidates, 2 citing real corpus entries**, marque read from the session instead of guessed as "Kawasaki ZX-series". Backend `implementation.md` 0.13.59 → 0.13.60. Regression **6140 passed / 0 failed** (baseline 6087; +53 guards — one run covering 244B and 244C). |
| 244D | `known_issues` stored ten times over, and a naive constraint would not have fixed it | ✅ | **Idempotency and integrity are different properties, and the cheapest way to get the first silently costs the second.** `known_issues` held **6,600 rows for 660 distinct issues** — every entry exactly ten times — because the table had no uniqueness constraint and `add_known_issue` was a plain INSERT, so every seed run duplicated the corpus. Not cosmetic: it degraded the Phase 244C guidance run that exposed it, where a request for 20 rows returned **2 distinct facts repeated ten times**. **Step 0 changed the design twice.** The seed files are clean (970 entries, 970 distinct keys), so the key was verified rather than assumed. And **43 entries carry `model IS NULL`** — SQLite treats NULLs as DISTINCT in a UNIQUE constraint, so the obvious `UNIQUE(make, model, title)` would have shipped looking correct while leaving 4.4% of the corpus free to keep multiplying. Demonstrated before writing the migration: the same NULL-model row inserted three times yields **3** rows under the naive form and **1** under a unique expression index over `(COALESCE(make,''), COALESCE(model,''), title)`. Migration 054 rebuilds the table keeping `MIN(id)` per key and **re-declares `idx_known_issues_sort` in its Phase 240C EXPRESSION form** — recreating the older `severity DESC` form would have undone migration 053 with no error, since SQLite only uses an expression index when the ORDER BY expression matches; `EXPLAIN QUERY PLAN` is asserted, not assumed. **The regression came back red with 9 failures and one was a real bug**: the plan's `INSERT OR IGNORE` suppresses EVERY constraint violation, CHECK included, so a typo in `source` would have been **dropped in silence** instead of raising. **This phase's own 21 guards missed it** — they tested uniqueness thoroughly and never the interaction with a neighbouring constraint; two guards from Phases 211 and 235B, written for unrelated reasons, caught it. Now `ON CONFLICT DO NOTHING`. **Fourth mention-versus-use failure of the session**: the guard added for that bug fired on the comment explaining it, repaired via AST like the SafetyChecker tripwire. Loader now reports rows actually inserted, not items walked — a re-seed announcing "970 inserted" while inserting nothing would have looked like success. Seeding all 100 files three times: **970 / 0 / 0**. Four schema contract pins bumped (Gates 9/11/12, Phase 191B); F9 then caught this phase's own pin missing its marker. 21 guards, **6/6 mutations caught**. Backend `implementation.md` 0.13.60 → 0.13.61. Regression **6161 passed / 0 failed** (baseline 6140; +21 guards). **Deliberately not done: the re-seed** — the DB holds 660 of 970 entries and 5 of 14 makes, with every European and electric make from Tracks K/L absent. |
| 244E | Resolving a model must not shrink the answer | ✅ | **Succeeding at identification made the answer fifty times worse.** Phase 244D's re-seed exposed a latent 244C bug: `BMW + "R1200GS"` returned **1 row** where `BMW` with no model returned **50** — a caller who knew *less* about the machine got *more* knowledge about it. The cause is in the data. Re-seeding revealed the corpus holds two populations: the early Japanese/American phases wrote clean model names, while **every Track K and L make has prose in the `model` column** (`"Liquid-cooled R-series boxers, R1200GS and all LC R models from 2013"`) and **none has a `model='All'` fallback row** — BMW 44/54 prose, Ducati 46/46, Triumph 55/55, Zero 17/17, all with zero `All` rows. **30.7% of the corpus is unreachable by a model-equality filter.** Ducati and Triumph escaped the bug only *by accident* — their model strings resolve as `ambiguous`, so no filter was applied; correct behaviour arriving through a failure to resolve is not a property to rely on. Retrieval now **tiers rather than filters**: rows are selected for the make and labelled `model` / `make_wide` / `make_other_model`, ordered most-specific first (severity within tier, via Phase 240C's `SEVERITY_RANK_SQL` so migration 053's expression index serves it) and filled to the limit. Stated as an invariant — **knowing more must never return less** — and asserted across the live corpus: **0 violations over 362 make/model pairs, 26 makes**. **Precision by labelling, not exclusion**: the formatter tags each entry and names the other model, and `GUIDANCE_PROMPT` states a same-make/different-model entry is `cross_platform` at best; verified live, the model cited `"…all Honda models [this make, all models]"` and grounded it `cross_platform` rather than `machine_specific`. **Phase 244C's guard was split, not edited** — it bundled "a CBR600RR issue must not reach an F4i" with "a Kawasaki issue must not reach a Honda"; widening relaxes the first, the second has a mechanic on the other end, so it now lives in **two files**. **Second defect found mid-build, and it was Phase 240C's recall_repo bug reproduced by me**: a new severity `ORDER BY` against a fixture lacking the column raised, and a blanket `except Exception` turned the malformed query into "no known issues found" — the exact silent-empty failure 244C exists to end. Catch narrowed: missing **table** = empty corpus, missing column = raise; `known_makes`/`known_models` got the same treatment after throwing outright on a fresh DB. 16 guards, **6/6 mutations caught**. Backend `implementation.md` 0.13.61 → 0.13.62. Regression **6178 passed / 0 failed** (baseline 6161; +16 new guards, +1 from splitting Phase 244C's bundled leakage guard). **Root cause untouched: LiveWire and Damon still resolve to nothing — all 24 LiveWire rows are tagged "Harley-Davidson, LiveWire", so Phase 243's entire output remains unreachable.** |
| 244F | A make column that holds prose cannot be queried | ✅ | **LiveWire and Damon were not queryable makes at all — Phase 243's entire output was unreachable.** `known_issues.make` is one free-text column doing four jobs: a marque, a list of marques, a scope phrase, and in one row **a whole sentence of findings** (`"BMW and Ducati have listed adjustments; KTM, Triumph, Aprilia, Moto Guzzi have none"`). All 24 LiveWire rows sit inside `"Harley-Davidson, LiveWire"` and all 10 Damon rows inside a five-marque string, so `make='LiveWire'` returned **nothing** while the corpus documented the machine. **Step 0 read all 12 multi-value strings individually, which mattered — three are not lists.** `All makes` and `All European makes` are genuine editorial scopes (vendor tooling tiers, valve-train job types) and the sentence is a real finding; splitting any of them on delimiters would have produced nonsense. Migration 055 adds a `known_issue_makes` junction, backfilled **inside its own transaction** via a new declarative `post_apply` hook — an index that exists but is empty is indistinguishable from a corpus that says nothing. **The `make` column is never rewritten**: the author's text stays, including the sentence, and the junction is derived beside it — rewriting 970 entries by script is a larger risk than the defect, and a derived index can always be rebuilt. **The vocabulary comes out of the strings that caused the bug**: splitting the multi-marque values yields clean tokens which, unioned with single-marque values, give 16 marques *including LiveWire and Damon, which exist nowhere else*. Matching is whole-word against that closed set — never substring, never inferred — because attaching an entry to a marque it does not name puts another machine's fault in front of a mechanic. `known_makes()` now returns 16 real marques instead of 26 raw values including a sentence. **Results: LiveWire 0→24, Damon 0→10, Zero 17→27, Energica 12→22, Triumph 55→62, Ducati 46→55, zero orphans.** **The build reintroduced the disease it was curing** — joining the junction made 11 tests go *silently empty* on pre-55 databases, the same failure as `make='Homda'` returning zero that opened this chain; a missing junction now falls back to column matching, guarded to still refuse cross-make results. Two further self-inflicted bugs: indexing derived its vocabulary from the **production** database regardless of which one was being written, and the index is **order-dependent until the corpus is complete** (a row inserted before the marque is established under-indexes), so `rebuild_make_index` is authoritative and `db init` runs it. **One guard could not be written honestly** — the fallback's error scoping is not behaviourally testable since both queries share column dependencies; mutation proved the first version vacuous, so it is a source assertion whose docstring states its own reach. 34 guards, **7/7 mutations caught**. Backend `implementation.md` 0.13.62 → 0.13.63, schema v54 → v55. Regression **6212 passed / 0 failed** (baseline 6178; +34 guards). First run **red, 16 failed** — 11 of them a silent-empty regression I introduced, see Deviations. **Recorded: `All European makes` excludes Energica** — an Italian marque shipping in its own seed file, so the derivation misses it; both affected entries concern valve-train and ICE tooling so it is defensible, but it is a derivation artefact, not a judgment. |
| 244G | A guard that reads source text will eventually read a comment | ✅ | **Four times in one session a guard matching an identifier as TEXT fired on the PROSE explaining it** — Phase 241's SafetyChecker tripwire on a docstring citing it as a cross-reference, Phase 244's attribution filter on the correction quoting the claim it refuted, 244D's `INSERT OR IGNORE` guard on the comment saying why it is not used, 244F's derivation guard on a module the code had left. Two were repaired ad hoc; this makes the property general. **The reverse case is worse and nobody had hit it**: `assert "get_session" in src` passes when the identifier survives only in a comment, so deleting the code the guard protects leaves it green — a false failure is loud and fixed in minutes, a false pass protects nothing and tells no one. New `tests/support/source_guards.py` with `code_of()`: comments and docstrings blanked, **everything else byte-for-byte intact** — same length, line count and columns, because guards here pin multi-line SQL and a `tool_choice` dict and `ast.unparse` would normalise them out of matching; ordinary string literals survive since a guard pinning an `ORDER BY` clause is reading code that happens to be a string. **18 assertions converted across 5 files.** The deliverable is the **meta-guard** — it walks the suite and fails on any new assertion testing a literal against raw Python source, naming `code_of` and a `raw-source-ok` opt-out in its message so the cheap fix is the correct one. **The meta-guard caught itself**: its first version scanned by regex and flagged its own file, because the sample offender code it builds to test itself lives inside string literals and a regex cannot tell a string containing code from code — the exact mistake the phase exists to stop, made by the tool built to stop it. Rewritten to parse the AST, it then **found 4 offenders the manual audit had missed**, two written the same hour (audit found 14, parser found 18). Phases 241 and 244D keep their own AST repairs, which are stronger for what they assert; 244G only guards they have not regressed to text matching. The ~60 files asserting over report output, API bodies and JSON corpus content are deliberately untouched — that is data, and matching it literally is correct. 19 guards, **5/5 mutations caught**. Backend `implementation.md` 0.13.63 → 0.13.64. Regression **6231 passed / 0 failed** (baseline 6212; +19 guards). **Key finding: a guard that punishes explaining a defect selects for a codebase that explains nothing** — Phase 241's tripwire fired on the sentence written to make its own gap findable, and the cheapest way to green it was to delete that sentence. |
| 244H | The test suite writes to the production database | ✅ | **Twice in one day a regression run applied a schema migration to the operator's real `data/motodiag.db`** — 054 at 06:44:46 and 055 later the same way. Neither was requested; both were found only because I checked a schema version before doing something unrelated. `get_connection(db_path=None)` falls through to `get_settings().db_path`, and several CLI command paths call `init_db()` bare. `MOTODIAG_DB_PATH` is now redirected at conftest **import** time — not in a fixture, because `get_settings` is an `lru_cache`d singleton modules may read while being imported, so a session-scoped fixture runs too late for anything resolved during collection — plus an autouse tripwire whose message explains the fall-through rather than just reporting it. **Step 0 was wrong and the tripwire disproved it in a minute**: the plan said no test depended on the seeded production corpus, and four Phase 140 tests failed instantly with `no such table: dtc_codes`. They were **reading** real data, not merely at risk of writing it — the fixture patched `init_db` (the write path) while `get_connection` resolved through the settings independently, so those tests passed for months only because the operator's database was seeded, and would fail on a clean checkout. New shared `redirect_default_db` fixture covers both paths so the next author need not rediscover that. **The same class of bug appeared while fixing it**: a guard restored its env var in a `finally`, which runs before `monkeypatch` restores, wiping the session default for every later test — the settings cache is now cleared after every test, structural rather than remembered. **The property is measured, not argued**: across a complete 6,246-test run the production database's mtime (1789052818), size (32301056) and schema version (55) were identical before and after, with a companion guard proving the probe write actually landed somewhere so it cannot pass by writing nothing. 15 guards, **5/5 mutations caught**. Test-only; no production code, no schema change (still v55). Backend `implementation.md` 0.13.64 → 0.13.65. Regression **6246 passed / 0 failed** (baseline 6231; +15 guards), green first run. **Key finding: the correct migration is the dangerous one** — both incidents were harmless, so neither surfaced, and a suite that writes to real data and gets away with it teaches nobody anything right up until the write is a DELETE. Phase 244D's migration removed 5,940 rows. |
| 245 | Damon HyperSport / HyperFighter | 🔲 | Shift smart suspension, copilot safety system, startup-phase reliability concerns |
| 246 | BMS diagnostics (battery management) | 🔲 | Cell balancing, SOH (state of health), voltage curves, thermal derating, cycle counting |
| 247 | Motor controller / inverter faults | 🔲 | IGBT failures, phase-loss detection, overcurrent faults, controller firmware |
| 848 | Regenerative braking diagnostics | 🔲 | Regen ratios, coast-down behavior, brake light trigger on regen, single-pedal mode |
| 249 | Thermal management (battery + motor) | 🔲 | Liquid cooling loops (battery), air cooling (motor), thermal derating curves, ambient temp effects |
| 250 | Gate 13 — Electric motorcycle integration test | 🔲 | **Row renumbered from "Gate 12" at Phase 240B**: Phase 240 had already closed Gate 12 as Track K's closing gate, so this row and every gate row after it were off by one — the same collision Phase 240 itself had to correct mid-phase when row 240 was written as "Gate 11" that Phase 205 already held. Gates 10 and 11 are Phases 204 and 205. Corrected before Track L opened rather than during Phase 250. Query electric bike → BMS/motor/regen/thermal analysis end-to-end |

## Track M — Scooters & Small Displacement (Phases 251–258)

50-300cc class: commuter scooters, Honda Grom/Ruckus, Vespa, learner bikes. CVT diagnostics, small-battery electrical, low-cost repair workflows.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 251 | Vespa / Piaggio (50-300cc) | 🔲 | Italian scooters, air/liquid-cooled, MP3 tilting 3-wheelers, LX/GTS/Primavera |
| 252 | Honda Ruckus / Grom / Metropolitan / PCX | 🔲 | Cult-favorite small Hondas, Grom 125 modding community, Ruckus/Metro 50cc scooters |
| 253 | Yamaha Zuma / Vino + Kymco / SYM / Genuine | 🔲 | Budget scooters, Taiwanese manufacturers, parts availability |
| 254 | Small-displacement CVT diagnostics | 🔲 | Belt wear, variator/clutch bells, roller weights, kickstart backup |
| 255 | Twist-and-go vs manual small bikes | 🔲 | Scooter vs small motorcycle diagnostic differences |
| 256 | Scooter electrical (12V minimal) | 🔲 | Stator-to-battery, no FI on older carb scooters, simple wiring |
| 257 | Small-engine carb service (single/twin-barrel) | 🔲 | Keihin/Mikuni small-bore carbs, seasonal cleaning, emission restrictions |
| 258 | Gate 14 — Scooter / small displacement integration test | 🔲 | Query scooter/small bike → CVT + electrical + carb workflow |

## Track N — Specialized Workflows (Phases 259–272)

Non-diagnostic workflows that shops perform: pre-purchase inspection, tire service, insurance claims, track prep, seasonal protocols, valve adjustments, brake service.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 259 | Pre-purchase inspection (PPI) — engine | 🔲 | Compression test, leak-down, oil sample, fuel quality, starter/charging health, visual inspection checklist |
| 260 | Pre-purchase inspection — chassis | 🔲 | Frame straightness, fork seals, steering head bearings, swingarm, wheel bearings, brake/tire condition, accident history |
| 261 | Tire service workflow | 🔲 | Wear patterns (cupping, squaring, feathering), DOT date decoding, age cracking, balance, TPMS |
| 262 | Crash / insurance claim support | 🔲 | Photo documentation standards, damage estimation, frame-straightness measurement, salvage vs repair decision |
| 263 | Track-day / race prep checklist | 🔲 | Safety wire, lockwire torque, brake bleed, coolant swap, tech inspection, number plate prep |
| 264 | Winterization protocol | 🔲 | Fuel stabilizer, battery tender, oil change, carb drain (if carbed), cover, storage position |
| 265 | De-winterization protocol | 🔲 | Battery check, brake exercise, fluid inspection, tire pressure, spring test ride |
| 266 | Engine break-in procedures | 🔲 | Rebuilt engine/top-end break-in, RPM restrictions, heat cycles, oil changes schedule |
| 267 | Emissions / smog compliance (CA-first) | 🔲 | California smog requirements, EPA compliance, cat converter status, CARB database |
| 268 | Valve adjustment workflow | 🔲 | Generic workflow + per-engine-type templates (inline-4, V-twin, single, boxer, Desmo) |
| 269 | Brake service workflow | 🔲 | Pad replacement, caliper rebuild, rotor thickness check, fluid flush, bleed procedure |
| 270 | Suspension service workflow | 🔲 | Fork seal replacement, oil change, spring rate selection, rear shock rebuild, sag setup |
| 271 | Chain / belt / shaft service workflow | 🔲 | Chain/sprocket replacement, belt tension/alignment, shaft drive oil, u-joint inspection |
| 272 | Gate 15 — Specialized workflows integration test | 🔲 | Run PPI → tire service → winterization → valve adjust → brake service end-to-end |

## Track O — Business Infrastructure (Phases 273–292)

Payment processing, CRM, booking, accounting, inventory, warranty/recall claims, vendor integrations — the business systems a shop needs beyond Track G's shop management.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 273 | Payment processing foundation (Stripe) | 🔲 | Stripe Connect, card terminals, invoicing, subscription billing |
| 274 | Customer CRM | 🔲 | Customer profiles, bike ownership history, communication log, notes |
| 275 | Appointment booking system | 🔲 | Online booking, time slots, mechanic assignments, confirmations |
| 276 | Calendar sync (iCal / Google Calendar) | 🔲 | Two-way sync, appointment blocks, mechanic calendars |
| 277 | Accounting export (QuickBooks) | 🔲 | Chart of accounts mapping, invoice export, payment reconciliation |
| 278 | Accounting export (Xero) | 🔲 | Xero-specific export format, tax handling, multi-currency |
| 279 | Parts inventory with reorder points | 🔲 | Stock levels, reorder points, automatic PO generation, distinct from per-job sourcing |
| 280 | OEM warranty claim processing | 🔲 | Warranty validity lookup, claim documentation, OEM-specific submission flows |
| 281 | NHTSA recall processing | 🔲 | VIN-based recall lookup, recall completion tracking, OEM reimbursement |
| 282 | Vendor: Parts Unlimited integration | 🔲 | API integration, parts availability, wholesale pricing, dropship |
| 283 | Vendor: NAPA integration | 🔲 | NAPA TRACS integration, parts catalog, local store inventory |
| 284 | Vendor: Drag Specialties integration | 🔲 | Drag Specialties wholesale, Harley/V-twin parts focus |
| 285 | Vendor: Dennis Kirk integration | 🔲 | Dennis Kirk wholesale, Japanese parts, off-road |
| 286 | Vendor: tire distributor integrations | 🔲 | Dunlop, Michelin, Metzeler, Bridgestone — wholesale tire ordering |
| 287 | VIN decoder service | 🔲 | NHTSA VPIC integration, make-specific VIN structure decoding, model-year lookup |
| 288 | Tax rate lookup by shop location | 🔲 | State/county/city sales tax, automated tax calculation on invoices |
| 289 | Multi-currency support | 🔲 | USD/CAD/EUR/GBP exchange rates, multi-currency invoicing, currency conversion |
| 290 | Financial reporting | 🔲 | P&L per mechanic, per bay, per customer, monthly/quarterly/annual |
| 291 | Estimate vs actual variance tracking | 🔲 | Quote accuracy, labor time variance, parts cost variance |
| 292 | Gate 16 — Business infrastructure integration test | 🔲 | Customer books → intake → warranty check → repair → invoice → payment → accounting export |

## Track P — Reference Data Library (Phases 293–302)

Deep reference database: service manual citations, exploded parts diagrams, visual failure library, video tutorials, per-model specs.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 293 | Factory service manual references | 🔲 | Clymer/Haynes/OEM page citation database, searchable by make/model/year/topic |
| 294 | Exploded parts diagram integration | 🔲 | Partzilla/BikeBandit part number → diagram lookup, visual parts browser |
| 295 | Visual failure library | 🔲 | Photo database of common failures, user-contributed + curated, failure signatures |
| 296 | Curated video tutorial index | 🔲 | YouTube embeds by symptom/repair, verified mechanic channels, offline viewing |
| 297 | Per-model torque spec database | 🔲 | Expand Phase 93 generic specs → complete per-model torque database |
| 298 | Per-model fluid capacity database | 🔲 | Oil/coolant/brake fluid/fork oil capacities by exact model/year |
| 299 | Electrical schematic references | 🔲 | Wire color codes per model/year, connector pinouts, module locations |
| 300 | Special tool database | 🔲 | Tools required per repair, OEM vs aftermarket, cost, rental availability |
| 301 | Service interval tables | 🔲 | OEM service intervals per model, normal vs severe service, expand Phase 93 |
| 302 | Gate 17 — Reference data library integration test | 🔲 | Query any bike/repair → manual page + diagram + torque + tool + video |

## Track Q — Extended UX Affordances (Phases 303–317)

Multi-user auth, voice-first mode, printing, barcode scanning, photo annotation, i18n, AR overlay, accessibility, customization.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 303 | Multi-user auth with roles | 🔲 | Owner/tech/service writer/apprentice, permission gating, session management |
| 304 | Voice-first mode (expand Phase 183) | 🔲 | Hands-free wrenching, voice commands, continuous listening mode |
| 305 | Print + label support | 🔲 | Service tags, work order printouts, parts labels, Brother/Dymo printer integration |
| 306 | Barcode / QR scanning | 🔲 | Parts inventory scanning, bike check-in via VIN barcode, mobile camera or USB scanner |
| 307 | Photo annotation | 🔲 | Draw circles on leaks, arrows to cracks, text labels, annotation persistence |
| 308 | Spanish localization | 🔲 | Priority language — large US mechanic demographic, full UI + knowledge base translation |
| 309 | French localization | 🔲 | Quebec, Europe markets, UI + knowledge base |
| 310 | German localization | 🔲 | European market, aftermarket catalogs, UI + knowledge base |
| 311 | AR overlay (research/placeholder) | 🔲 | ARKit/ARCore integration for camera-based diagnostic overlay, scope-deferred — research phase only |
| 312 | Accessibility (screen reader + high contrast) | 🔲 | WCAG 2.1 AA compliance, VoiceOver/TalkBack, keyboard navigation |
| 313 | Dark mode (desktop) | 🔲 | High contrast dark theme, complement Phase 203 mobile, user preference |
| 314 | Keyboard shortcuts + power user features | 🔲 | Desktop CLI hotkeys, vim-style navigation, command palette, custom aliases |
| 315 | Customizable dashboards | 🔲 | Drag-and-drop widgets, per-user layouts, saved views |
| 316 | Workflow recording (train-by-example) | 🔲 | Record my process → replay for training, generate workflow templates from recordings |
| 317 | Gate 18 — Extended UX integration test | 🔲 | Multi-user auth → voice input → photo annotation → Spanish UI → print work order |

## Track R — Advanced AI Capabilities (Phases 318–327)

Beyond the base AI engine: human-in-the-loop learning, tuning recommendations, expanded predictive maintenance, cross-fleet anomaly detection, knowledge graph.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 318 | Human-in-the-loop learning | 🔲 | Mechanic overrides train the system, feedback loop closes on diagnostic accuracy, retraining pipeline |
| 319 | Tuning recommendations | 🔲 | Jetting calculations (altitude/mods), Power Commander/FTECU/Dynojet map suggestions |
| 320 | Predictive maintenance expansion | 🔲 | Beyond Phase 148 — per-customer, per-fleet, per-usage-pattern failure prediction |
| 321 | Fleet anomaly detection | 🔲 | Cross-bike pattern recognition, identify "same model same issue" at scale |
| 322 | AI-assisted customer communication | 🔲 | Draft status updates, explain repairs in plain English, estimate customer questions |
| 323 | Image similarity search | 🔲 | "Show me bikes with this symptom" — visual failure similarity via embedding |
| 324 | Repair success prediction | 🔲 | "Will this fix work for this bike?" — historical outcome prediction |
| 325 | Knowledge graph construction | 🔲 | Symptom ↔ cause ↔ fix relationships, graph queries for complex diagnostics |
| 326 | Continuous learning pipeline | 🔲 | Automated model fine-tuning from accumulated diagnostic feedback |
| 327 | Gate 19 — Advanced AI integration test | 🔲 | Feedback → learning → prediction → anomaly detection → customer draft end-to-end |

## Track S — Launch + Business Layer (Phases 328–342)

Commercial launch infrastructure: billing, onboarding, data migration from competitors, legal, training/certification, community.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 328 | Subscription billing foundation | 🔲 | Stripe integration from Track O wired to subscription tiers, tier enforcement flips to hard mode here |
| 329 | Self-service signup flow (web) | 🔲 | Trial signup, credit card capture, email verification, onboarding trigger |
| 330 | Onboarding wizard for new shops | 🔲 | Bike inventory import, staff setup, initial data entry, first diagnostic walkthrough |
| 331 | Data migration: Mitchell 1 → MotoDiag | 🔲 | Import existing shop data from Mitchell 1 (auto/moto), vehicle history, work orders |
| 332 | Data migration: ShopKey → MotoDiag | 🔲 | Import from Snap-on ShopKey, SMS Pro |
| 333 | Data migration: ALLDATA → MotoDiag | 🔲 | Import from AutoZone ALLDATA Shop Management |
| 334 | Terms of Service + liability framework | 🔲 | Legal ToS, liability disclaimers on repair recommendations, mechanic indemnification |
| 335 | MotoDiag Certified training track | 🔲 | Online courses, video tutorials, structured learning paths for new mechanics |
| 336 | Certification exam system | 🔲 | Proctored exam, certificate generation, renewal requirements, certification tiers |
| 337 | In-app mechanic community | 🔲 | Q&A forum, moderated discussion, expert verification, reputation system |
| 338 | Customer referral program | 🔲 | Referral codes, tracking, credits/discounts, viral acquisition |
| 339 | Promotional codes + discounts | 🔲 | Coupon system, time-limited promotions, tier upgrade incentives |
| 340 | Enterprise sales portal | 🔲 | B2B lead management, custom quotes, multi-year contracts, SLA negotiation |
| 341 | SLA + uptime dashboard | 🔲 | Public status page, uptime metrics, incident communication, enterprise SLAs |
| 342 | Gate 20 — Launch readiness check | 🔲 | Billing → signup → onboarding → data migration → first diagnostic under paid plan |

## Track T — Operational Infrastructure (Phases 343–352)

Running MotoDiag as a production service: observability, support, backup, feature flags, admin tools.

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 343 | Telemetry + crash reporting (Sentry) | 🔲 | Sentry integration, crash reports, performance monitoring, user session replay |
| 344 | In-app support channel | 🔲 | Chat widget or ticketing, support team routing, knowledge base integration |
| 345 | Cloud backup + sync | 🔲 | S3 encrypted backups, automatic sync, point-in-time restore |
| 346 | Multi-location / franchise support | 🔲 | Shared customers across locations, inter-location parts transfer, centralized reporting |
| 347 | Real-time sync between devices | 🔲 | Desktop ↔ mobile ↔ web real-time data sync, conflict resolution |
| 348 | Audit log (who did what, when) | 🔲 | Immutable event log, security audit trail, compliance readiness |
| 349 | Feature flags / gradual rollout | 🔲 | LaunchDarkly-style feature flags, percentage rollouts, kill switches |
| 350 | A/B testing framework | 🔲 | Experiment tracking, conversion metrics, feature comparison |
| 351 | Admin panel for support staff | 🔲 | Customer lookup, account management, subscription changes, impersonation for debugging |
| 352 | Gate 21 — Operational readiness | 🔲 | Telemetry → support → backup → multi-location → audit log → admin panel end-to-end |

---

## Architecture Overview

```
moto-diag/
├── pyproject.toml
├── src/
│   └── motodiag/
│       ├── __init__.py
│       ├── core/           ← config, database, base models (Track A)
│       ├── vehicles/       ← vehicle registry, specs (Track A/B, expanded Retrofit 110)
│       ├── knowledge/      ← DTC codes, symptoms, known issues (Track B, expanded Retrofit 111)
│       ├── engine/         ← AI diagnostic engine (Track C)
│       ├── media/          ← video/audio diagnostic analysis (Track C2, annotations Retrofit 119)
│       ├── cli/            ← terminal interface (Track D)
│       ├── auth/           ← users, roles, permissions (Retrofit 112)
│       ├── crm/            ← customers, customer-bikes join (Retrofit 113)
│       ├── hardware/       ← OBD adapter interface (Track E)
│       ├── advanced/       ← fleet, maintenance, prediction (Track F)
│       ├── shop/           ← shop management, work orders, triage, scheduling (Track G)
│       ├── api/            ← REST API + web layer (Track H)
│       ├── billing/        ← Stripe integration, subscriptions, payments (Retrofit 118, Track O/S)
│       ├── accounting/     ← QuickBooks/Xero export, financial reports (Retrofit 118, Track O)
│       ├── inventory/      ← parts inventory, reorder points, vendor integration (Retrofit 118, Track O)
│       ├── scheduling/     ← appointments, calendar sync (Retrofit 118, Track O)
│       ├── workflows/      ← PPI, tire service, winterization templates (Retrofit 114, Track N)
│       ├── i18n/           ← translations, locale handling (Retrofit 115, Track Q)
│       ├── reference/      ← manual refs, parts diagrams, video tutorials (Retrofit 117, Track P)
│       ├── feedback/       ← diagnostic feedback, override tracking (Retrofit 116, Track R)
│       ├── ai_advanced/    ← human-in-loop learning, tuning, knowledge graph (Track R)
│       ├── launch/         ← signup, onboarding, migration, community (Track S)
│       └── ops/            ← telemetry, support, backups, feature flags, admin (Track T)
├── data/
│   ├── dtc_codes/          ← fault code databases
│   ├── vehicles/           ← make/model specs
│   └── knowledge/          ← known issues, repair procedures
├── tests/
├── output/
├── main.py
└── docs/
    └── phases/             ← per-phase implementation + log docs
```

## Design Principles

1. **Core depends on nothing** — no framework imports in business logic
2. **Hardware is optional** — full diagnostic value without plugging anything in
3. **Mechanic-first UX** — terse, useful output; no fluff
4. **Every diagnosis is traceable** — session → symptoms → reasoning → conclusion
5. **Knowledge compounds** — each diagnostic session can improve future diagnoses
6. **Cost-aware AI** — track tokens, use cheapest model that works, cache aggressively
