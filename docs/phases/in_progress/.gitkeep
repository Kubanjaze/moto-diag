Plan docs for the phase currently being built live here, then move to
../completed/ at close. Kept in git so the directory survives an empty
state — it was briefly deleted at the 2026-09-02 cleanup when the last
in-progress phase (196) moved out.
