# Handoff — Phase 264 closed: Track N batch 2 (winterization, de-winterization, engine break-in, valve adjustment)

**Written 2026-09-26** by the Opus session that built and closed 264 on
`phase-264`. Every figure below comes from tool output in that session.
The full record is `docs/phases/completed/264_implementation.md`,
`264_phase_log.md` and `264_step0.md`.

## What shipped

**Four protocols on the workflow door**, one per ROADMAP row, seeded by
**migration 070** (schema 69 → 70):

| row | slug | powertrains | items |
|---|---|---|---|
| 264 | `winterization_v1` | all | 7: service and protection before storage; fuel (the makers disagree); carburetor; oil and cylinders; the 12-V battery; the traction battery; stand, tires, cover, place |
| 265 | `de_winterization_v1` | all | 7: uncover and clean; 12-V battery; traction battery; fuel and oil; the maker's before-use checks; brakes and tire pressures; test ride |
| 266 | `engine_break_in_v1` | ICE, hybrid | 6: which break-in applies; the limits in each maker's own measure; varying the load; cool-down; first oil change and service; trouble |
| 268 | `valve_adjustment_v1` | ICE, hybrid | 8: cold by the maker's definition; TDC; feeler gauge; screw and lock nut; shims; close up; V-twin; inline-four, boxer and desmodromic (no figure in the library) |

**Two live re-points in the same migration**, the operator's scope:
- `generic_winterization_v1`'s description names `winterization_v1`
  instead of "Track N phase 264 expands" (F158);
- `ppi_chassis_v1`'s 8 "Zuma" mentions in 3 items become "YW125Y", the
  title page's model code (F160). F160 said nine; the ninth is in 068's
  Python description, which is never seeded.

- **106 claims** and 285 anchors, all on their pages; 242 cited pages, 0
  unclaimed.
- **Three Opus refute rounds.** 84 of 85 round-1 claims kept (W16 killed
  on its page only). N2 and N3 narrowed: the Yamaha XC50J prescribes a
  cool-down, and SYM speaks to a replaced engine. About 70 text fixes.
- **Rule 2 on its primary route.** Subconscious GLM-5.3 ran the first
  pass in a sandbox proven by planted writes.
- **F161 filed:** the generic winterization items' unsupported figures.
  **F162 filed:** two defects in 260's live chassis text, outside F160's
  name-only scope.
- **Regression** **9446 passed, 0 failed, 0 skipped, 0 errors** at `717dc47` (34 min 41 s wall, `python -m pytest -n auto --dist load`, exit 0).

## What is open

- **The live apply and the merge wait for the operator** (rule 1). The
  backup is `~/backups/motodiag/motodiag_pre264_20260926_131946.db`.
  The dry run on a copy of it changed exactly the scoped rows. The merge
  waits too, because `init_db` applies pending migrations.
- **F160 closes at the live apply.** F161 and F162 are live-row changes
  of their own.
- **W30** has been checked mechanically only.
- **Process kink for the clean-up pass:** every refute round found
  defects the previous round's fixes had introduced. A corrected sentence
  needs the same read as a first draft.

## Next: batch 3

**Phase 262, Track N batch 3**: rows 262 (crash and insurance claim
support), 263 (track-day and race prep) and 267 (emissions and smog
compliance, California first), under the same convention, with 262
carrying the batch. **Then gate 272** (the specialized-workflows
integration test: PPI → tire service → winterization → valve adjust →
brake service).

Carry forward from 264:
- read the refute's fixes again before shipping them;
- a Subconscious `machine` field is never a name source;
- `init_db` applies migrations, so hold a merge while a live apply
  waits.

## Deploy outcome (added after the deploy, 2026-09-26)

- **The operator's approval, scoped, recorded as given:** "apply
  migration 070 live only if it changes exactly what your dry-run diff
  showed — generic_winterization_v1's description (the "Track N phase
  264 expands" rewrite, plus its updated_at), and "Zuma 125" → "YW125Y"
  in checklist items 18, 19 and 21 — plus 264's own new rows (4
  templates, 28 items, the schema_version row). If the live apply shows
  any other existing row changing, stop before committing it and ask
  me."
- **How the condition was met before the commit.**
  `apply_pending_migrations` commits as it runs, so a diff after it
  could not stop anything. The apply step therefore did three things
  first:
  1. proved live row-for-row identical to the backup, in every table;
  2. re-ran the migration on a fresh copy of that backup with the merged
     code;
  3. checked a scope made as strict as the operator's words. The only
     changed template is rowid 2, with only `description` and
     `updated_at` differing. The only changed items are rowids 18, 19 and
     21, and each changed field differs only by the Zuma → YW125Y
     rename.

  It would have refused otherwise. Its controls went red on a planted
  extra word in item 19 and on a drifted `known_issues` row.
- **Backup:** `~/backups/motodiag/motodiag_pre264_20260926_131946.db`,
  taken before anything touched live. Five backups are kept; retain 5
  removed `motodiag_pre354_20260924_163732.db`. No process had the live
  file open.
- **Merged** as `987c818` on `master`, and the push went through
  (`270fcad..987c818`).
- **Live apply:** `[70]`. Pre-apply scope problems: none. Post-apply
  scope problems: none. The live diff against the backup is identical to
  the dry-run diff, timestamps aside.
  - **Changed:** template rowid 2 (description and `updated_at`), and
    items 18, 19 and 21 (8 mentions in 7 fields).
  - **Added:** 4 templates, 28 items and 1 `schema_version` row.
- **After:** schema 70, 1,060 `known_issues` (hash unchanged), 12
  templates, 79 items, integrity ok. Against live, `motodiag workflow
  list --category winterization` lists `winterization_v1` beside the
  generic template, and `motodiag workflow show ppi_chassis_v1` prints
  "YW125Y" 8 times and "Zuma" 0 times.
- **F160 is closed.** Nothing new was started. Next is batch 3, phase
  262.
