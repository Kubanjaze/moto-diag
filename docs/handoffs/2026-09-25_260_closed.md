# Handoff — Phase 260 closed on `phase-260`; Opus to merge, refute, run the regression

**Written 2026-09-25** for the Opus session that fetches this branch from
the sandbox clone (`~/Projects/moto-diag-glm`). Everything through "ready
to merge" is committed on `phase-260`; **the regression of record, the
refute pass, the merge and the deploy have not run** and are this
handoff's first list. Every figure below comes from tool output in the
session that wrote this file. The full record is
`docs/phases/completed/260_implementation.md` and `260_phase_log.md`.

## Opus session, 2026-09-25: what changed after the builder's close

The first list below was run by the Opus session, which corrected this
handoff where it had gone stale. The deploy outcome is its own section.

- **Three bug fixes** (the register is in the phase log):
  - #1: migration 067 had lost its `rollback_sql` when 068 was
    inserted (`df44de7`);
  - #2: 259's and 260's migration tests held only while their own
    migration was the head. Two of 259's were red on the branch
    (`7ef1275`);
  - #3: #2's own `== 68` literal, caught by F124's guard in the first
    regression of record (`b22b715`).

  The builder's full-suite line was never measured.
- **Refute pass:** 17 of 18 claims kept, four of them with the item text
  corrected (`20426ff`). **C16 was killed:** the library *does* set a
  swinging-arm clearance. The Vespa GTS Super 300 ie (2008) service
  station manual, PDF p. 227, gives "Standard clearance 0.40 ÷ 0.60 mm
  Allowable limit after use: 1.5 mm", and the swingarm item now cites it
  (`7de9fbe`). Step 0's vocabulary lacked the maker's word.
- **Regression of record: 9375 passed, 0 failed, 0 skipped** at `b22b715`.
  The floor is 9375.

## What shipped

**PPI — chassis: the pair 259 began is complete, and the F158 reference
died with it.** The difference from 259 is the lesson of 259's own
handoff: the substrate already has a door, so this phase is **one
content migration and its tests — no new module**.

- **Migration 068** (`ppi_chassis_workflow`, schema 67 → 68): the
  `ppi_chassis_v1` template, category `ppi`, powertrains
  **all three** — an electric has a frame, forks, bearings, brakes and
  tires; 259's `["ice","hybrid"]` was right for the engine and wrong
  here — with **7 items** mapping to the row's seven subjects: frame
  straightness and crash evidence, steering head bearings, front fork,
  swingarm, wheel bearings and rims, brakes, tires. The same migration
  re-points `ppi_engine_v1`'s description — "the chassis protocol is
  Phase 260", the F158 review miss the 259 deploy recorded — at the new
  slug by name, the operator-approved option-1 shape. The rollback peels
  it round trip, the description restored verbatim.
- **No new CLI, no new repo functions, no allowlist change**: `motodiag
  workflow list/show` (259) renders the template the moment the
  migration seeds it. `SCHEMA_VERSION` 67 → 68; the allowlist gates
  (209B, 244U, 244Y) all green with zero edits.

**Figure discipline:** every figure cites a held document with its PDF
page (KTM EXC OM pp. 76/94/99/101/115/116/164; Zuma 125 2009 SM pp.
34/55/93–95; CHF50 SM pp. 12/217/241/243/314/318; Metropolitan 2025 OM
pp. 64–66/121; F800R OM pp. 94–95). The frame item ships with **no**
figure, because the whitespace-proof census found no frame-alignment
tolerance anywhere. *(As closed by the builder, this said the swingarm
item too. The refute pass killed that negative, and the swingarm item
now cites the Vespa GTS Super 300's clearance; see above.)* Where no
document sets a figure the item says where the figure belongs.

**F159 filed** (not fixed): the substrate's own starter item —
`generic_ppi_v1` item 5, "Pads >3mm… tires <5 years old" — carries
uncited figures the library contradicts (F800R's pad limit is 1.0 mm).
The F149 defect class, now on the same screen as figure-disciplined
content. The fix is a sourcing decision plus a migration, not this
phase's content edit.

**One anomaly the operator should see** (also in the phase log and the
implementation doc's Deviations): the ROADMAP row's 🚧 flip is commit
`b1a3e70` (00:59:21), which this session **did not author** — it landed
on the branch between its creation and Step 0, from the operator's
side, as the ledger step the session should have made first. It is the
correct change in the correct place (`roadmap_check.py` ok) and it
stays; but standing rule 5 says a second session on this checkout only
reads, so whoever wrote it should know the builder saw it and kept it.

## What the Opus session must do (the builder's list; done, see "Opus session" above and the deploy outcome below)

In order:

1. **Fetch and review** `phase-260`. Five commits, named from `git log`
   in the phase log (none from memory — 259's docs-fix lesson):
   `b1a3e70` (row 🚧, external) → `1baa7fc` (Step 0) → `6fc0784` (v1.0)
   → `0bd7836` (build: migration 068, 26 tests) → the close-out commit
   (v1.1, the row ✅, the history row, the floor 9346 → 9372, F159,
   this handoff).
2. **Run the whole-tree gates on the merged tree** — they are green on
   the branch (521 passed across 191C/244G/244U/244V/244T/244J/244Y/
   256/209B/114/F124/240c/191D/roadmap continuity; `finding_check` exit
   0 both variants) — but the sandbox's own lesson from 259's fixes
   #4/#5 is that the gates that bite are the ones the list misses, so
   the regression of record (below) is the real check.
3. **Run the regression of record.** Expect **9,372 passed, 0 failed, 0
   skipped** (`COLLECTED_TEST_FLOOR` is already raised to 9372 on the
   branch; 9,346 + 26). Record the line with its commit hash and count
   in `260_phase_log.md` so `closeout_check` A5 goes green — it is red
   by design until then. The sandbox full-suite line is in the phase
   log; `test_phase209_packaging.py` runs clean outside the sandbox
   (259's measurement; its 7 sandbox failures are the exported
   PYTHONPATH, environmental).
4. **Run the refute pass.** The claims table is
   `260_implementation.md` § "Claims for the Opus refute pass": C1–C18
   name a document, a PDF page and the exact claim in the item text;
   C15–C17 are the three negatives — re-derive with the session tmp's
   `census_negatives.py` (regenerable in minutes) or any
   whitespace-proof scan, and re-check each control. Also reproduce the
   repo measurements: the snapshot at 67 carrying the F158 phrase; a
   fresh `db init` at 68 holding `ppi_chassis_v1` + 7 items; the
   upgrade-from-67 one-row scope (the test builds its own baseline).
5. **Backup the live database, then let migration 068 run** on the
   merged deploy (`~/backups/motodiag/`, retain 5, print the
   before-state, dry-run on a copy first). The migration's effect on the
   live DB is: one template + seven items inserted, **one existing row
   altered** — `ppi_engine_v1`'s description, re-pointed from "the
   chassis protocol is Phase 260" to "for the full chassis-side
   protocol see ppi_chassis_v1" — schema 67 → 68; the 1,060-row corpus
   is untouched (the dry run hash-verified it). The one-row UPDATE is
   the operator-approved shape from 259's review, applied to the row
   that review missed; if the operator strikes it, drop the UPDATE and
   its rollback together and note it under F158. Then merge to `master`,
   push, deploy, and add the deploy's outcome to this handoff (rule 4:
   the handoff goes in before the merge; the deploy outcome is added
   after it runs).

## State after this branch merges

| | |
|---|---|
| corpus | **1,060** `known_issues` — unchanged; no content rows |
| `COLLECTED_TEST_FLOOR` | **9375** (9372 on the builder's branch; +3 in the Opus review) |
| schema | 67 → **68** (migration 068; a gate test still pins the floor) |
| workflow templates | 3 → **4** (`ppi_chassis_v1` + 7 items; 16 → 23 checklist items) |
| open phases | none on this branch; `docs/phases/in_progress/` is empty |
| findings | **F159 filed by this phase** (the substrate starter item's uncited figures); F149, F153–F157 remain open from earlier phases |

## What 260 learned that the next phase should not relearn

- **A content phase on an open door is one migration plus tests.** 259
  had to build the front door; 260 added zero production modules, and
  the allowlist that generated 259's fixes #4/#5 never moved. The next
  workflow-content phase (tires/winterization/valves) starts from this
  shape.
- **The claims table must match the content, not the plan.** The tires
  item cites the KTM manual's mismatched-pattern warning (PDF p. 39),
  which the v1.0 table missed; it went in as C18 in the same edit that
  wrote the item. A refute pass checks the item text, so the table has
  to name everything the text says.
- **CLI needles must be single tokens.** Rich wraps at the 80-column
  test terminal, so a spaced phrase ("for the full chassis-side
  protocol see ppi_chassis_v1") is not assertable against the screen;
  the exact text is pinned at the DB level and the screen is pinned by
  wrap-safe tokens.
- **The row-flip-before-Step-0 step can be done for you, and must then
  be recorded as done-by-someone-else** — see the anomaly above. The
  ledger check passing does not say who wrote the ledger step; the
  phase log does.

## Deploy outcome (Opus session)

**Deployed 2026-09-25 at 05:05**, under the operator's scoped
pre-approval, applied literally. In order:

- merge `e1d53c3`, pushed;
- backup `~/backups/motodiag/motodiag_pre260_20260925_050437.db`
  (retain 5; `motodiag_pre255C_20260922_150946.db` removed);
- dry run on a copy: exactly `ppi_engine_v1`'s `description` and
  `updated_at`, plus `ppi_chassis_v1`, its 7 items and the
  `schema_version` row 68. Nothing else changed, and the `known_issues`
  hash `5932cd02c78ae850` was unchanged;
- F158 census on that copy: 33 Phase/Track references in 24 rendered
  rows, none from 260; the 7 F-number hits are BMW model names; the
  planted control was found;
- live apply `[68]`, identical to the dry run except for timestamps.

Live now: schema 68, 1,060 `known_issues`, 4 templates, 23 items,
integrity ok. `motodiag workflow show ppi_chassis_v1` works against
live. The full record is in the phase log's Deploy section.

## Next after 260

**Phase 261, the tire service workflow**, is the next row in the ROADMAP
(🔲): wear patterns, DOT date decoding, age cracking, balance and TPMS.
It is a content phase on the same door, and 260's shape carries over:
one migration and its pins. It starts from three things 260 learned:

- **Census negatives use the maker's word.** Piaggio's "swinging arm"
  killed C16. Tire vocabulary varies by maker too: "tread wear
  indicator", "TWI", "wear bar". Each negative's control must be shown
  hitting the exact page it cites.
- **Migration tests are head-proof from the start.** Use
  `rollback_to_version`, apply only the phase's own migration, and
  compare against `m.version`. Run F124's guard before every commit
  that touches a migration.
- **Inserting a migration must leave its neighbour intact.**
  `test_every_migration_keeps_its_rollback` now enforces this.

The DOT-date and 5-year figures 260 cites (KTM OM p. 116) are a
starting point, and F159 (the generic stub's uncited "tires <5 years
old") sits on the same subject.

Other open wins, from the handoff before this one:
- F153/F154: the SYM junction spellings and the missing `fiddle 50`
  lookup line;
- F158's repair phase: 33 known references remain in rendered text,
  per this deploy's census.
