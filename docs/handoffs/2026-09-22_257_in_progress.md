# Handoff — Phase 257 in progress, to a `subc claude` session

**Written 2026-09-22 23:35** by the Opus desktop session, for the operator's
next session run as `subc claude` (GLM-5.3 Marathon) from the repository
root. Every fact below is a commit hash or a measured result.

## Start here

1. `docs/phases/in_progress/257_implementation.md` — plan **v1.1**; the v1.1
   amendment at the end is the one that governs (source stages through
   Subconscious, refute on Opus).
2. `docs/phases/in_progress/257_step0.md` — the measurements and proofs.
3. `docs/phases/in_progress/257_phase_log.md` — what each build slice did.
4. `.claude/skills/source-transmission/SKILL.md` — the procedure.

## State

| | |
|---|---|
| branch | `phase-257-orchestrator`, pushed, tip `a8e236e` |
| master | `d6e330e` (255D closed; F139, F140 filed) |
| 257 tests | 55 passing across `test_phase257_*.py`; each check broken on purpose and seen to fail |
| regression of record | 8,145 at `486e847` (255D) — **257 has not run one yet** |
| `COLLECTED_TEST_FLOOR` | 8145 — must rise to include the 55 before 257's regression |

## Built

| slice | commit | what |
|---|---|---|
| 1 | `26784c6` | `sandbox.sb.tmpl`, `orchestrate.py` routes + guards; boundary test (12), guard test (30) |
| 2 | `b9f0167` | `census.py` (reproduces **605**), `entry_check.py` E1–E8 + fixtures; test (13) |
| 3 | `a8e236e` | `orchestrate.py batch MAKE [SPELLING…]`, stage prompts, schemas, SKILL.md, CHANGELOG |

## Running when this was written (may have finished)

Runs live in `~/.cache/motodiag/source-runs/`; each has `summary.json`.

* **Tranche 1** — three batches in sequence: `SYM` (Wolf Classic 150, Wolf
  CR300i), `Kymco` (K-Pipe), `Honda` (Grom, Grom 125). Log:
  the desktop session's scratchpad `tranche1.log`; results are in each run's
  `summary.json` regardless.
* **D7 acquisition** — `D7_acquisition_*/d7.json`: per maker, how a maker
  document can be fetched (endpoint / spec page / needs-browser / blocked),
  one real fetch each.

If they were killed with the desktop session, re-run:
`.venv/bin/python -B .claude/skills/source-transmission/orchestrate.py batch SYM "Wolf Classic 150" "Wolf CR300i"`
(and the same for `Kymco "K-Pipe"`, `Honda "Grom" "Grom 125"`).

## Next, in order

1. **Read each `summary.json`.** `stops` empty and `ready_to_write` non-empty
   → write. Any stop → do not write; the stop is the finding.
2. **Write** (plan D4): one entry per model in `TRANSMISSION_LOOKUP`,
   `src/motodiag/knowledge/transmission.py`, **one make per commit**, with a
   test that the spelling now resolves and a before/after table in the log.
   Wolf Classic 150 is an **alias on the existing Wolf 150 entry**, not a new
   entry. **The Grom goes in only if refute kept it from the page images.**
3. **D7 table** into the phase log from `d7.json`.
4. Raise `COLLECTED_TEST_FLOOR` (tests/test_phase255B_collected_test_floor.py)
   **before** the full regression; record hash and count.
5. `/closeout` — the seven artefacts and `verify_phase.sh 257 REG TIP`.

## Rules that bit this project — keep them

* The model's account is never evidence: `entry_check` against the document,
  refute re-opens the page, leak checks run independently. In the first
  sandboxed GLM run a control failed and the model explained it wrongly.
* Credentials never cross routes; the Subconscious route carries none.
* Targeted edits on source and tests; commit messages via quoted heredoc.
* Read `git status` after `add -A`: a stray untracked file was swept into a
  commit once tonight.

---

## State at hand-off — 2026-09-22 23:54, subc claude (GLM-5.3 Marathon) session

Written when the operator called the session short: Subconscious quota
is about to run out, so nothing below spent a model call after the
three tranche batches.

### Commits since `a8e236e` (this session; `0d7fe1a` was the desktop session's)

| commit | one line |
|---|---|
| `129b242` | Tranche 1 write: SYM — Wolf Classic 150 as an alias on Wolf 150, Wolf CR300i new entry; census 605 → 603; `tests/test_phase257_tranche1_writes.py` created (7) |
| `97a3dc3` | Tranche 1 write: Kymco — K-Pipe new entry (refute verdict from its own pypdfium2 render of spec page 57); census 603 → 602 |
| `16928bb` | Tranche 1 write: Honda — one Grom 125 entry (MSX125 codes as aliases), cited from Honda's own digital spec pages, not the service-manual OCR; census 602 → **600** (tranche complete: five spellings, as planned in D8). `test_repeated_retrievals_accumulate` moved its example machine Grom → CBR1000RR (still unknown; the counter assertion untouched) |
| `b95fb6d` | **F141 filed and CLOSED**: new entry_check class E9 — an evidence copy must carry `<file>.provenance.json` (original + its sha256, `fetched_as` for a URL); E3 now extracts the ORIGINAL's text in entry_check itself (HTML→text, `.txt`, PDF via pypdf) and matches the quote against that, never the saved copy. Fixtures: doctored copy / no sidecar / stale pin / two good cases. Both breaks seen to fail. `test_phase257_source_checks.py` 13 → 19 |

All three batch summaries were clean (`stops` empty, every refute verdict
`kept`) before their writes. Branch tip `b95fb6d`, pushed.

### Half-done: the token-budget redesign — analysed, zero code

The operator ordered it mid-session (the agent-loop source stage costs
~2.4M tokens per spelling; Kymco measured 7.7M for one) and then
called the session short before any was written. Nothing in the tree.

1. New **`candidates.py`** in the skill folder, no model: for each
   spelling, alias-match the library (`~/research/motodiag`; `*.txt` and
   HTML→text — reuse `entry_check._extract_text`) and extract only the
   matching excerpts, **±40 lines, with file path and page**, capped per
   spelling so the prompt stays small.
2. The source stage becomes **ONE turn with NO tools** — the prompt
   contains only those excerpts, GLM returns the JSON findings, and the
   quote must come from the excerpts. No excerpt → `outcome:
   no_evidence` **without calling the model**. (Web acquire leaves the
   source stage with this change; findings will cite library files by
   absolute path, which E3 already verifies directly. Refute is
   unchanged — it keeps tools.)
3. **Token accounting**: sum each stage's usage from the `claude` JSON
   output into `summary.json`, and stop a batch above **150K tokens per
   spelling** (add to `stops()`; it is a stop, red not tunable — the
   operator set the ceiling).
4. Test against a fixture library (match, ±40 lines, page, no-match →
   no_evidence, the 150K stop with a planted over-budget number);
   break-it per the house rule.
5. Re-run `batch Kymco "K-Pipe"` and report tokens before/after —
   before ≈ **7.7M** (the operator's figure; the run itself recorded
   nothing, which is why item 3 exists).

### Not done, in order after the redesign

* **D7 acquisition**: the driver was killed with the desktop session
  mid-run — no `d7.json` was written. The driver script is at
  `~/.cache/motodiag/d7_subc.py` (moved 2026-09-23 from the desktop
  session's /private/tmp scratchpad, which a reboot clears; a one-shot
  script, not committed). **Re-run it only after the token redesign** —
  it is open-ended web research, the most expensive kind of agent loop.
  Then the D7 table into the phase log.
* **`COLLECTED_TEST_FLOOR`** (`tests/test_phase255B_collected_test_floor.py`)
  must rise **before** the regression. 257's count has grown since the
  handoff table said 55: `test_phase257_source_checks.py` is now 19 and
  `tests/test_phase257_tranche1_writes.py` adds 14 — recount, then raise.
* **Full regression** with hash and count.
* **`/closeout`**: the seven artefacts and `verify_phase.sh 257 REG TIP`.
  The plan's verification checklist (257_implementation.md) still has
  open boxes: tranche 1 resolves (done — the tranche tests), the
  end-to-end run (done — three batches), D7 table (not done), Grom
  refuted not from OCR (done, better than planned — see `16928bb`).

### Census of record at hand-off

**600** unknown spellings (was 605). Tranche 1 moved five: Wolf Classic
150, Wolf CR300i, K-Pipe, Grom, Grom 125 — all `manual`, all
`model-sourced`, each with its maker's quote in the lookup.
