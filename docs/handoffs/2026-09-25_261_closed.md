# Handoff — Phase 261 closed: Track N batch 1 (tire, brake, suspension, drivetrain service)

**Written 2026-09-25** by the Opus session that built and closed 261 on
`phase-261`. Every figure below comes from tool output in that session.
The full record is `docs/phases/completed/261_implementation.md`,
`261_phase_log.md` and `261_step0.md`.

## What shipped

**Four service protocols on the workflow door**, one per ROADMAP row,
seeded by **migration 069** (schema 68 → 69, inserts only):

| row | slug | items |
|---|---|---|
| 261 | `tire_service_v1` | 7: the old tire's wear, damage and age cracking, date code, TPMS/RDC (optional), fitting, balance, pressure and run-in |
| 269 | `brake_service_v1` | 7: pads, discs, caliper and master-cylinder overhaul (both optional), fluid, bleeding, reassembly |
| 270 | `suspension_service_v1` | 7: sag, spring rate, fork oil (optional), seals and springs, fork air bleed (optional), rear shock, damping and preload |
| 271 | `drivetrain_service_v1` | 7, all optional: chain slack, wear and sprockets, cleaning, adjustment; belt; shaft oil; universal joints |

- All three powertrains. `motodiag workflow list --category <each>` and
  `motodiag workflow show <slug>` reach them.
- **Every figure names its machine** and cites that machine's document
  and PDF page: 212 claims and 329 verbatim anchors, all on their pages.
  All 358 cited pages resolve to a claim.
- **Machines are named as their title pages name them:** KTM 2022
  250/300 EXC TPI, Honda PCX150 (2013–2017), Yamaha YW125Y, Yamaha
  XVS95CL and XVS13AF, and the 2010 KTM 690 Enduro. A test pins this.
- **Bug fix #1:** `workflow list` elided the new slugs at 80 columns. The
  slug and category columns now never shrink.
- **F160 filed:** 260's live `ppi_chassis_v1` text calls the YW125Y
  manual "Zuma 125" in nine places. That word is on none of the
  document's 338 pages.

**Refute:**
- **Round 1 (160 rows):** 156 kept and 4 killed. N1 was narrowed:
  Kymco's People S owner's manual names "significant flat spots". B1, S4
  and S29 were claim-record errors.
- **Round 2 (53 new claims):** all kept.
- **About 58 item-text defects fixed.** Five of them would have given a
  mechanic a wrong verdict:
  - the PCX150 caliper bores were unlabelled;
  - chain slack was compared across three measuring methods;
  - a disc over its run-out limit was to be cleaned, not replaced;
  - the XR650L owner's notice was read as a workshop ban;
  - the "Zuma" name.

## What is open

- **Subconscious is suspended.** It returned 403, "organization access is
  suspended; the entitlement must be restored". Step 0's extraction ran
  on rule 2's fallback route (`claude-opus-5-5@medium`, same sandbox).
  The operator's account needs its entitlement restored before the next
  GLM run. Before this call the day's usage read 51.3M of the 60M plan.
- **F160** needs a migration that re-points nine live mentions, and that
  is a rule-1 live-row change.
- **Five claims have had no adversarial read** (B62, D54, S51–S53). They
  came from round 2's quotes and are only mechanically verified.
- **Process kink for the clean-up pass:** R6 counted exactly one close
  for this batch, as the convention intended. But ✅ rows with no CLOSED
  date and no documents (269–271) are invisible to every ledger rule.
  Nothing checks that "folded into 261" names a phase that exists and
  closed.

## Next: batch 2

**Phase 264, Track N batch 2**: rows 264 (winterization), 265
(de-winterization), 266 (engine break-in) and 268 (valve adjustment),
under the same convention, with 264 carrying the batch. Carry forward:
- **Write the claims before the text**, and cross-check every cited page
  back to a claim (`s0/claims.py` and `xcheck.py` in this session's
  scratchpad; regenerate them, since the scratchpad does not persist).
- **Name every machine from its title page**, never from the file name:
  YW125Y, not Zuma; PCX150, not PCX.
- **A figure carries its method.** Winterization and break-in figures
  (intervals, RPM limits, heat cycles) vary by maker. Valve clearances
  also depend on cold or hot, and on the engine's configuration.
- **Run a second refute round** whenever round 1's corrections add claims.
- `generic_winterization_v1` still carries "Track N phase 264 expands"
  (F158). Batch 2 owns that text, and changing it is a live-row change:
  ask before the live apply.
